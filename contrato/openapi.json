{
  "components" : {
    "schemas" : {
      "Activo" : {
        "description" : "Un equipo o una herramienta del negocio. Comprarlo cambia plata por un bien: baja la caja y no toca la utilidad ni el patrimonio (05 §6.1, BDD-15-1)",
        "properties" : {
          "estado" : {
            "description" : "El único estado que el modelo nombra (04 §4.6). Vender un activo o darlo de baja no está en ningún documento: cuando exista, este enum crece y sube la MINOR",
            "enum" : [ "en_uso" ],
            "type" : "string"
          },
          "fechaCompra" : {
            "format" : "date",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "nombre" : {
            "type" : "string"
          },
          "valorCompra" : {
            "description" : "Pesos enteros",
            "format" : "int64",
            "type" : "integer"
          }
        },
        "required" : [ "estado", "fechaCompra", "id", "nombre", "valorCompra" ],
        "type" : "object"
      },
      "Adelanto" : {
        "description" : "Plata entregada a cuenta del salario. Baja la caja, crea una cuenta por cobrar y no registra gasto: el gasto se reconoce entero en la liquidación (RN-11, 06 §7)",
        "properties" : {
          "antiguedadDias" : {
            "description" : "Cuántos días lleva sin descontarse. Ausente cuando ya se descontó. Es lo que el panel muestra junto a la cuenta por cobrar viva (06 §7)",
            "format" : "int32",
            "type" : "integer"
          },
          "anulacion" : {
            "$ref" : "#/components/schemas/Anulacion",
            "description" : "Ausente mientras el adelanto esté vigente"
          },
          "cuenta" : {
            "description" : "El nombre de la cuenta de donde salió",
            "type" : "string"
          },
          "cuentaId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "descontadoEn" : {
            "description" : "La liquidación que lo descontó. Ausente mientras siga pendiente: es lo que el índice parcial idx_adelantos_pendientes vigila para que no se descuente dos veces",
            "format" : "uuid",
            "type" : "string"
          },
          "descripcion" : {
            "type" : "string"
          },
          "empleada" : {
            "description" : "El nombre de la empleada, para pintar la fila sin pedir otra consulta",
            "type" : "string"
          },
          "empleadaId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "fecha" : {
            "description" : "El día en que se entregó",
            "format" : "date",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "movimientoId" : {
            "description" : "El movimiento de tipo adelanto_empleada que lo registró en el libro",
            "format" : "uuid",
            "type" : "string"
          },
          "valor" : {
            "description" : "Pesos enteros, mayor que cero",
            "format" : "int64",
            "type" : "integer"
          }
        },
        "required" : [ "empleadaId", "fecha", "id", "movimientoId", "valor" ],
        "type" : "object"
      },
      "AdelantoDescontado" : {
        "description" : "Un adelanto que esta liquidación canceló, con su fecha, que es como sale en el desprendible (06 §8)",
        "properties" : {
          "fecha" : {
            "description" : null,
            "format" : "date",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "valor" : {
            "description" : null,
            "format" : "int64",
            "type" : "integer"
          }
        },
        "required" : [ "fecha", "id", "valor" ],
        "type" : "object"
      },
      "Adjunto" : {
        "description" : "Un soporte del movimiento: la foto del recibo o el PDF de la factura",
        "properties" : {
          "bytes" : {
            "description" : "Cuánto pesa el archivo",
            "format" : "int64",
            "type" : "integer"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "nombre" : {
            "description" : "El nombre con que se subió",
            "type" : "string"
          },
          "tipo" : {
            "description" : "El tipo de contenido del archivo",
            "enum" : [ "image/jpeg", "image/png", "image/webp", "application/pdf" ],
            "type" : "string"
          }
        },
        "required" : [ "bytes", "id", "nombre", "tipo" ],
        "type" : "object"
      },
      "AlternativaDeContratacion" : {
        "description" : "Lo que el sistema propone cuando no alcanza, en vez de quedarse en «no» (06 §5)",
        "properties" : {
          "clave" : {
            "description" : "Cuál de las cinco del 06 §5 es",
            "enum" : [ "medio_tiempo", "por_obra", "aumentar_ventas", "subir_precios", "reducir_ocio" ],
            "type" : "string"
          },
          "detalle" : {
            "description" : "Los números de esa alternativa, no el consejo genérico",
            "type" : "string"
          },
          "titulo" : {
            "type" : "string"
          }
        },
        "required" : [ "clave", "detalle", "titulo" ],
        "type" : "object"
      },
      "AnticipoMinimo" : {
        "description" : "Si el anticipo cubre el costo directo del pedido, y cuánto falta si no (RF-38, RF-39, CU-12, 05 §5). El front pinta la lectura y no calcula ninguna cifra (ADR-018)",
        "properties" : {
          "anticipoConfigurado" : {
            "description" : "El porcentaje configurado sobre el valor total, en pesos",
            "format" : "int64",
            "type" : "integer"
          },
          "anticipoConfiguradoPct" : {
            "description" : "El que traía la consulta",
            "format" : "int32",
            "type" : "integer"
          },
          "anticipoMinimoPct" : {
            "description" : "Costo directo sobre valor total, en puntos",
            "format" : "int32",
            "type" : "integer"
          },
          "anticipoSugeridoPct" : {
            "description" : "El mínimo redondeado hacia arriba a múltiplo de 5, con 5 puntos de holgura (05 §5.2)",
            "format" : "int32",
            "type" : "integer"
          },
          "costoDirecto" : {
            "description" : "La suma del costo de las líneas: insumo, consumibles y tiempo, al costo vigente de cada producto",
            "format" : "int64",
            "type" : "integer"
          },
          "cubre" : {
            "description" : "El anticipo configurado alcanza para el costo directo. Con true no hay advertencia (BDD-12-2)",
            "type" : "boolean"
          },
          "faltante" : {
            "description" : "Costo directo menos el anticipo configurado. Cero cuando cubre. Es la plata que el negocio pone de su bolsillo hasta la entrega",
            "format" : "int64",
            "type" : "integer"
          },
          "lectura" : {
            "$ref" : "#/components/schemas/Lectura",
            "description" : "El aviso con sus montos, como «Este pedido tiene 35% de margen. Con el 50% de anticipo pones $78.000 de tu bolsillo hasta la entrega. Anticipo sugerido: 70%.» (05 §5.3)"
          },
          "valorTotal" : {
            "description" : "Pesos enteros. Sale de las líneas",
            "format" : "int64",
            "type" : "integer"
          }
        },
        "required" : [ "anticipoConfigurado", "anticipoConfiguradoPct", "anticipoMinimoPct", "anticipoSugeridoPct", "costoDirecto", "cubre", "faltante", "lectura", "valorTotal" ],
        "type" : "object"
      },
      "Anulacion" : {
        "description" : "Un registro anulado no se borra: guarda cuándo, quién y por qué (RN-13)",
        "properties" : {
          "en" : {
            "format" : "date-time",
            "type" : "string"
          },
          "motivo" : {
            "type" : "string"
          },
          "por" : {
            "description" : "El nombre completo de quien la hizo",
            "type" : "string"
          }
        },
        "required" : [ "en", "motivo", "por" ],
        "type" : "object"
      },
      "Aporte" : {
        "description" : "Plata que entra al negocio desde afuera: sube la caja y el patrimonio, y no la utilidad (05 §2)",
        "properties" : {
          "cuentaId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "fecha" : {
            "format" : "date",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "nota" : {
            "description" : "Ausente si se registró sin ella",
            "type" : "string"
          },
          "valor" : {
            "description" : "Pesos enteros",
            "format" : "int64",
            "type" : "integer"
          }
        },
        "required" : [ "cuentaId", "fecha", "id", "valor" ],
        "type" : "object"
      },
      "AvisoDeVistaPrevia" : {
        "properties" : {
          "texto" : {
            "description" : "Para qué sirve la vista previa y para qué no (RF-94)",
            "type" : "string"
          },
          "titulo" : {
            "type" : "string"
          }
        },
        "required" : [ "texto", "titulo" ],
        "type" : "object"
      },
      "CambioDeClave" : {
        "description" : "El formulario «cambio-de-clave». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"cambio-de-clave\"}",
        "properties" : {
          "claveNueva" : {
            "type" : "string"
          },
          "repeticion" : {
            "description" : "La misma contraseña otra vez. Que coincidan lo comprueba la API: 42210",
            "type" : "string"
          }
        },
        "required" : [ "claveNueva", "repeticion" ],
        "type" : "object"
      },
      "Cancelacion" : {
        "description" : "El formulario «cancelacion». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"cancelacion\"}",
        "properties" : {
          "cuentaId" : {
            "description" : "De dónde sale la plata. Solo con destinoDelAnticipo devolucion",
            "format" : "uuid",
            "type" : "string"
          },
          "destinoDelAnticipo" : {
            "description" : "Qué se hace con lo ya cobrado: devolvérselo al cliente o convertirlo en ingreso (RF-26, CU-07 A3). Obligatorio si el pedido tiene anticipos por devengar; si no tiene, sobra",
            "enum" : [ "devolucion", "ingreso" ],
            "type" : "string"
          },
          "motivo" : {
            "description" : "Por qué se cancela. Queda en el pedido",
            "type" : "string"
          }
        },
        "required" : [ "motivo" ],
        "type" : "object"
      },
      "Cargo" : {
        "properties" : {
          "activo" : {
            "type" : "boolean"
          },
          "desactivacion" : {
            "$ref" : "#/components/schemas/Desactivacion",
            "description" : "Solo en un cargo inactivo"
          },
          "descripcion" : {
            "description" : "Qué hace quien lo tiene. Ausente si no tiene",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "nombre" : {
            "type" : "string"
          },
          "orden" : {
            "description" : "Su lugar en el catálogo, de menor a mayor",
            "format" : "int32",
            "type" : "integer"
          },
          "personasActivas" : {
            "description" : "Cuántas personas activas lo tienen. Con alguna, no se desactiva",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "activo", "id", "nombre", "orden", "personasActivas" ],
        "type" : "object"
      },
      "CargoAsignable" : {
        "properties" : {
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "nombre" : {
            "type" : "string"
          }
        },
        "required" : [ "id", "nombre" ],
        "type" : "object"
      },
      "Categoria" : {
        "properties" : {
          "esFijo" : {
            "description" : "Gasto fijo mensual —arriendo, servicios, internet—, el que cuenta para la caja libre",
            "type" : "boolean"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "naturaleza" : {
            "enum" : [ "ingreso", "gasto" ],
            "type" : "string"
          },
          "nombre" : {
            "type" : "string"
          },
          "padreId" : {
            "description" : "La categoría madre. Ausente en una categoría principal",
            "format" : "uuid",
            "type" : "string"
          }
        },
        "required" : [ "esFijo", "id", "naturaleza", "nombre" ],
        "type" : "object"
      },
      "ClaveTemporal" : {
        "description" : "El formulario «clave-temporal». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"clave-temporal\"}",
        "properties" : {
          "claveTemporal" : {
            "type" : "string"
          }
        },
        "required" : [ "claveTemporal" ],
        "type" : "object"
      },
      "Cliente" : {
        "properties" : {
          "anulacion" : {
            "$ref" : "#/components/schemas/Anulacion",
            "description" : "Ausente mientras el cliente esté vigente"
          },
          "correo" : {
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "nombre" : {
            "type" : "string"
          },
          "notas" : {
            "type" : "string"
          },
          "telefono" : {
            "type" : "string"
          }
        },
        "required" : [ "id", "nombre" ],
        "type" : "object"
      },
      "ColumnaEsperada" : {
        "description" : "Una de las columnas que el importador necesita. El front arma el mapeo con estas y con las del archivo, y no trae ninguna lista propia (ADR-018)",
        "properties" : {
          "clave" : {
            "description" : "El campo del movimiento: tipo, valor, fechaMovimiento, cuenta, categoria o descripcion",
            "type" : "string"
          },
          "etiqueta" : {
            "description" : "Cómo se llama en pantalla",
            "type" : "string"
          },
          "obligatoria" : {
            "description" : "Sin ella el mapeo no sirve, y responde 42270",
            "type" : "boolean"
          },
          "tipo" : {
            "description" : "Cómo se lee lo que venga en esa columna",
            "enum" : [ "texto", "dinero", "fecha", "lista" ],
            "type" : "string"
          }
        },
        "required" : [ "clave", "etiqueta", "obligatoria", "tipo" ],
        "type" : "object"
      },
      "ConfiguracionDeSobres" : {
        "description" : "Cómo se reparte cada peso que entra en efectivo (05 §11). No se sobrescribe: cada cambio es una fila de sobres_config con su vigencia, y así se conserva el historial (RF-51, 04 §4.8)",
        "properties" : {
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "pctCostoDirecto" : {
            "format" : "int32",
            "type" : "integer"
          },
          "pctGastosFijos" : {
            "format" : "int32",
            "type" : "integer"
          },
          "pctReserva" : {
            "format" : "int32",
            "type" : "integer"
          },
          "pctRetiro" : {
            "format" : "int32",
            "type" : "integer"
          },
          "vigenteDesde" : {
            "description" : "El día en que se guardó, en Bogotá. La vigente es la de la fecha mayor y, si hay varias del mismo día, la última registrada",
            "format" : "date",
            "type" : "string"
          }
        },
        "required" : [ "id", "pctCostoDirecto", "pctGastosFijos", "pctReserva", "pctRetiro", "vigenteDesde" ],
        "type" : "object"
      },
      "ConsultaDeAdelantos" : {
        "description" : "El cuerpo de POST /api/v0/consultas/adelantos. Todo es opcional: sin nada, los pendientes de descontar",
        "properties" : {
          "empleadaId" : {
            "description" : "Solo los de esa persona",
            "format" : "uuid",
            "type" : "string"
          },
          "incluirAnulados" : {
            "description" : "Con true salen también los anulados",
            "type" : "boolean"
          },
          "incluirDescontados" : {
            "description" : "Con true salen también los que ya se descontaron en una liquidación",
            "type" : "boolean"
          }
        },
        "type" : "object"
      },
      "ConsultaDeAnticipoMinimo" : {
        "description" : "El cuerpo de POST /api/v0/consultas/anticipo-minimo: las líneas que la persona lleva escritas y el anticipo que piensa cobrar, para que la API responda si alcanza. Se consulta desde el formulario de pedido y desde el cotizador, y se recalcula con cada línea, antes de que exista nada que guardar. Es la misma forma que POST /api/v0/consultas/costeo",
        "properties" : {
          "anticipoPct" : {
            "default" : 50,
            "description" : "El que se piensa cobrar. Sin él, 50, que es la política del negocio (05 §3.1)",
            "format" : "int32",
            "maximum" : 100,
            "minimum" : 0,
            "type" : "integer"
          },
          "lineas" : {
            "description" : "Al menos una",
            "items" : {
              "$ref" : "#/components/schemas/LineaDePedido"
            },
            "minItems" : 1,
            "type" : "array"
          }
        },
        "required" : [ "lineas" ],
        "type" : "object"
      },
      "ConsultaDeBitacora" : {
        "description" : "El cuerpo de POST /api/v0/consultas/bitacora",
        "properties" : {
          "limite" : {
            "description" : "Cuántas de las más recientes. Sin él, todas",
            "format" : "int32",
            "minimum" : 1,
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "ConsultaDeCosteo" : {
        "description" : "El cuerpo de POST /api/v0/consultas/costeo: lo que la persona lleva escrito en el formulario de producto, para que la API devuelva las cifras sin que el front calcule ninguna (ADR-018)",
        "properties" : {
          "costoConsumibles" : {
            "format" : "int64",
            "minimum" : 0,
            "type" : "integer"
          },
          "costoInsumo" : {
            "format" : "int64",
            "minimum" : 0,
            "type" : "integer"
          },
          "margenObjetivo" : {
            "description" : "Sobre qué margen se quiere el precio sugerido, en puntos (RF-33). El mockup lo mueve entre 20 y 80 de cinco en cinco. Sin él, la respuesta no trae precio sugerido",
            "format" : "int32",
            "maximum" : 99,
            "minimum" : 1,
            "type" : "integer"
          },
          "minutosTrabajo" : {
            "minimum" : 0,
            "type" : "number"
          },
          "precioVenta" : {
            "format" : "int64",
            "minimum" : 0,
            "type" : "integer"
          },
          "productoId" : {
            "description" : "El producto que se está editando, para dejarlo fuera de la mediana con la que se compara: un producto metido en su propia referencia se disimula. Ausente mientras todavía no exista",
            "format" : "uuid",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ConsultaDeCotizaciones" : {
        "description" : "El cuerpo de POST /api/v0/consultas/cotizaciones. Todo es opcional: sin nada, las vigentes",
        "properties" : {
          "clienteId" : {
            "description" : "Solo las de ese cliente",
            "format" : "uuid",
            "type" : "string"
          },
          "desde" : {
            "description" : "Por fecha de emisión, incluida",
            "format" : "date",
            "type" : "string"
          },
          "estado" : {
            "description" : "Solo las que están en ese estado",
            "enum" : [ "vigente", "vencida", "aceptada", "anulada" ],
            "type" : "string"
          },
          "hasta" : {
            "description" : "Por fecha de emisión, incluida",
            "format" : "date",
            "type" : "string"
          },
          "incluirAnuladas" : {
            "description" : "Con true salen también las anuladas",
            "type" : "boolean"
          }
        },
        "type" : "object"
      },
      "ConsultaDeDesprendible" : {
        "description" : "El cuerpo de POST /api/v0/consultas/desprendible",
        "properties" : {
          "liquidacionId" : {
            "description" : "La liquidación de la que se quiere el desprendible. Una que no sea suya, a Operación le responde 40400: la política nom_lectura no se la devuelve, así que para la API no existe (ADR-006)",
            "format" : "uuid",
            "type" : "string"
          }
        },
        "required" : [ "liquidacionId" ],
        "type" : "object"
      },
      "ConsultaDeDivisionDeRetiro" : {
        "description" : "El cuerpo de POST /api/v0/consultas/division-de-retiro: lo que la persona lleva escrito en el formulario de retiro, para que la API proponga cómo partirlo sin que el front calcule nada (ADR-018)",
        "properties" : {
          "fecha" : {
            "description" : "La fecha del retiro, que dice de qué mes es el pro-labore que se completa. Sin ella, hoy en Bogotá; posterior a hoy responde 42290",
            "format" : "date",
            "type" : "string"
          },
          "valor" : {
            "description" : "Todo lo que se va a sacar, en pesos enteros",
            "format" : "int64",
            "minimum" : 1,
            "type" : "integer"
          }
        },
        "required" : [ "valor" ],
        "type" : "object"
      },
      "ConsultaDeEmpleadas" : {
        "description" : "El cuerpo de POST /api/v0/consultas/empleados. Todo es opcional: sin nada, las que siguen en nómina",
        "properties" : {
          "incluirAnuladas" : {
            "description" : "Con true salen también las fichas anuladas",
            "type" : "boolean"
          },
          "incluirRetiradas" : {
            "description" : "Con true salen también las que ya se retiraron",
            "type" : "boolean"
          }
        },
        "type" : "object"
      },
      "ConsultaDeFormulario" : {
        "description" : "El cuerpo de POST /api/v0/consultas/formularios",
        "properties" : {
          "nombre" : {
            "description" : "El nombre del formulario: «gasto», «acceso», «usuario»…",
            "type" : "string"
          }
        },
        "required" : [ "nombre" ],
        "type" : "object"
      },
      "ConsultaDeMovimientos" : {
        "description" : "El cuerpo de POST /api/v0/consultas/movimientos. Todo es opcional: sin nada, el libro vigente entero",
        "properties" : {
          "categoriaId" : {
            "description" : "Solo los de esa categoría",
            "format" : "uuid",
            "type" : "string"
          },
          "cuentaId" : {
            "description" : "Solo los que tocan esa cuenta, sea la de origen o la de destino",
            "format" : "uuid",
            "type" : "string"
          },
          "desde" : {
            "description" : "Por fecha_movimiento, incluida. No por la de digitación (RN-01)",
            "format" : "date",
            "type" : "string"
          },
          "hasta" : {
            "description" : "Por fecha_movimiento, incluida",
            "format" : "date",
            "type" : "string"
          },
          "incluirAnulados" : {
            "description" : "Con true salen también los anulados, que es el modo «ver anulados» del 04 §5.5, solo de Gerencia",
            "type" : "boolean"
          },
          "limite" : {
            "description" : "Cuántos de los más recientes. Sin él, todos",
            "format" : "int32",
            "minimum" : 1,
            "type" : "integer"
          },
          "tipos" : {
            "description" : "Solo esos tipos. Sin él, todos",
            "items" : {
              "enum" : [ "ingreso", "gasto", "transferencia", "inversion", "aporte", "retiro_prolabore", "retiro_distribucion", "anticipo_recibido", "adelanto_empleada" ],
              "type" : "string"
            },
            "type" : "array"
          }
        },
        "type" : "object"
      },
      "ConsultaDeNavegacion" : {
        "description" : "El cuerpo de POST /api/v0/consultas/navegacion",
        "properties" : {
          "vista" : {
            "description" : "Solo Gerencia: la navegación de Operación, para la vista previa",
            "enum" : [ "operacion" ],
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ConsultaDeNomina" : {
        "description" : "El cuerpo de POST /api/v0/consultas/nomina. Todo es opcional: sin nada, las liquidaciones del período abierto más reciente. A Operación, nomina_detalle solo le devuelve las suyas (RF-63)",
        "properties" : {
          "anio" : {
            "description" : "Junto con mes, en vez de periodoId",
            "format" : "int32",
            "type" : "integer"
          },
          "empleadaId" : {
            "description" : "Solo las de esa persona",
            "format" : "uuid",
            "type" : "string"
          },
          "mes" : {
            "description" : "De 1 a 12",
            "format" : "int32",
            "type" : "integer"
          },
          "periodoId" : {
            "description" : "El período que se quiere",
            "format" : "uuid",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "ConsultaDePedidos" : {
        "description" : "El cuerpo de POST /api/v0/consultas/pedidos. Todo es opcional: sin nada, los pedidos vigentes",
        "properties" : {
          "clienteId" : {
            "description" : "Solo los de ese cliente",
            "format" : "uuid",
            "type" : "string"
          },
          "desde" : {
            "description" : "Por fecha_pedido, incluida",
            "format" : "date",
            "type" : "string"
          },
          "estado" : {
            "description" : "Solo los que están en ese estado",
            "enum" : [ "cotizado", "en_proceso", "parcial", "entregado", "cancelado" ],
            "type" : "string"
          },
          "hasta" : {
            "description" : "Por fecha_pedido, incluida",
            "format" : "date",
            "type" : "string"
          },
          "incluirAnulados" : {
            "description" : "Con true salen también los anulados, que es el panel de anulados del 10 §4.2",
            "type" : "boolean"
          }
        },
        "type" : "object"
      },
      "ConsultaDePeriodosDeNomina" : {
        "description" : "El cuerpo de POST /api/v0/consultas/periodos-de-nomina. Todo es opcional: sin nada, los períodos abiertos",
        "properties" : {
          "anio" : {
            "description" : "Solo los de ese año",
            "format" : "int32",
            "type" : "integer"
          },
          "incluirCerrados" : {
            "description" : "Con true salen también los meses ya cerrados",
            "type" : "boolean"
          }
        },
        "type" : "object"
      },
      "ConsultaDeProductividad" : {
        "description" : "El cuerpo de POST /api/v0/consultas/productividad. Sin nada, el mes en curso en Bogotá (RNF-08)",
        "properties" : {
          "anio" : {
            "description" : null,
            "format" : "int32",
            "type" : "integer"
          },
          "mes" : {
            "description" : "De 1 a 12",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "type" : "object"
      },
      "ConsultaDeProductos" : {
        "description" : "El cuerpo de POST /api/v0/consultas/productos. Todo es opcional: sin nada, lo que se vende hoy ordenado por margen por hora",
        "properties" : {
          "incluirInactivos" : {
            "description" : "Con true salen también los desactivados, que es el panel «Productos desactivados» del mockup. Solo para Gerencia",
            "type" : "boolean"
          },
          "orden" : {
            "description" : "Los dos órdenes del cuadro de 10 §4.4, y los dos los decide la API. Sin él, margen_por_hora, que es el que orienta la decisión cuando hay más pedidos que tiempo (05 §7.2). A una sesión de Operación el catálogo le llega por nombre: ordenar por margen exige leer lo que la base no le deja ver",
            "enum" : [ "margen_por_hora", "margen_porcentual" ],
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "Costeo" : {
        "description" : "Lo que cuesta producir una unidad y desde cuándo cuesta eso (05 §7.1). Un costeo no se sobrescribe nunca: cada cambio agrega una fila con su vigencia (04 §4.5), y así un pedido viejo conserva el costo que tenía cuando se produjo",
        "properties" : {
          "costoConsumibles" : {
            "description" : "Tinta, papel transfer, hilo, energía",
            "format" : "int64",
            "type" : "integer"
          },
          "costoInsumo" : {
            "description" : "El material que se convierte en el producto",
            "format" : "int64",
            "type" : "integer"
          },
          "costoUnitario" : {
            "description" : "Materia más tiempo: lo que cuesta producir una unidad (05 §7.1). Lo calcula la API, y viaja aquí y no en los márgenes porque un producto sin precio también lo tiene",
            "format" : "int64",
            "type" : "integer"
          },
          "minutosTrabajo" : {
            "description" : "Los minutos de persona que lleva una unidad",
            "type" : "number"
          },
          "precioVenta" : {
            "description" : "A cuánto se vendía la unidad con este costeo",
            "format" : "int64",
            "type" : "integer"
          },
          "tarifaHora" : {
            "description" : "Lo que costaba una hora de quien produce cuando se registró este costeo: el costo mensual dividido entre las horas productivas del mes (05 §7.1). La pone la API y se congela aquí, porque recalcularla con el pro-labore de hoy cambiaría los costos de los pedidos viejos",
            "format" : "int64",
            "type" : "integer"
          },
          "vigenteDesde" : {
            "description" : "Desde qué día rige. El costeo vigente es el de la fecha mayor y, si hay varios del mismo día, el último registrado",
            "format" : "date",
            "type" : "string"
          }
        },
        "required" : [ "costoConsumibles", "costoInsumo", "costoUnitario", "minutosTrabajo", "precioVenta", "tarifaHora", "vigenteDesde" ],
        "type" : "object"
      },
      "CosteoCalculado" : {
        "description" : "Lo que el formulario de producto pinta en vivo: el costo, los márgenes, la lectura frente al resto del taller y el precio que habría que cobrar para el margen objetivo",
        "properties" : {
          "costoUnitario" : {
            "description" : "Materia más tiempo (05 §7.1)",
            "format" : "int64",
            "type" : "integer"
          },
          "lectura" : {
            "$ref" : "#/components/schemas/LecturaDeMargen"
          },
          "margenes" : {
            "$ref" : "#/components/schemas/Margenes",
            "description" : "Ausente cuando el precio de venta es cero: sin precio no hay márgenes que calcular"
          },
          "precioSugerido" : {
            "description" : "El precio que deja el margen objetivo sobre este costo (RF-33). Ausente si la consulta no trae margenObjetivo",
            "format" : "int64",
            "type" : "integer"
          },
          "tarifaHora" : {
            "description" : "La que la API tomó del pro-labore vigente para cobrar los minutos de trabajo",
            "format" : "int64",
            "type" : "integer"
          }
        },
        "required" : [ "costoUnitario", "lectura", "tarifaHora" ],
        "type" : "object"
      },
      "Cotizacion" : {
        "description" : "Lo que se le pasa al cliente antes de que haya pedido. Sus líneas son las mismas de un pedido, así que comparten esquema: LineaDePedido",
        "properties" : {
          "anticipoPct" : {
            "description" : "El anticipo que se le propone al cliente",
            "format" : "int32",
            "type" : "integer"
          },
          "anulacion" : {
            "$ref" : "#/components/schemas/Anulacion",
            "description" : "Ausente mientras la cotización esté vigente"
          },
          "cliente" : {
            "description" : "El nombre del cliente, para pintar la fila sin pedir otra consulta",
            "type" : "string"
          },
          "clienteId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "estado" : {
            "description" : "vigente mientras no venza; vencida cuando pasó validaHasta; aceptada cuando se convirtió en pedido; anulada cuando se anuló con motivo",
            "enum" : [ "vigente", "vencida", "aceptada", "anulada" ],
            "type" : "string"
          },
          "fechaEmision" : {
            "description" : null,
            "format" : "date",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "lineas" : {
            "description" : null,
            "items" : {
              "$ref" : "#/components/schemas/LineaDePedido"
            },
            "type" : "array"
          },
          "notas" : {
            "type" : "string"
          },
          "numero" : {
            "description" : "El número visible, como C-0134. Lo pone la API y es único en toda la base",
            "type" : "string"
          },
          "pedidoId" : {
            "description" : "El pedido en que se convirtió. Ausente mientras no se acepte",
            "format" : "uuid",
            "type" : "string"
          },
          "validaHasta" : {
            "description" : "Hasta cuándo se sostiene el precio. Ausente si no se dijo",
            "format" : "date",
            "type" : "string"
          },
          "valorTotal" : {
            "description" : "Pesos enteros. Sale de las líneas",
            "format" : "int64",
            "type" : "integer"
          }
        },
        "required" : [ "clienteId", "estado", "fechaEmision", "id", "lineas", "numero", "valorTotal" ],
        "type" : "object"
      },
      "Cuenta" : {
        "properties" : {
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "nombre" : {
            "type" : "string"
          },
          "tipo" : {
            "description" : "banco, billetera o efectivo",
            "enum" : [ "banco", "billetera", "efectivo" ],
            "type" : "string"
          }
        },
        "required" : [ "id", "nombre", "tipo" ],
        "type" : "object"
      },
      "DatosDeCargo" : {
        "description" : "El formulario «cargo», el mismo para crear y para renombrar",
        "properties" : {
          "descripcion" : {
            "description" : "Puede quedar vacía",
            "type" : "string"
          },
          "nombre" : {
            "type" : "string"
          }
        },
        "required" : [ "nombre" ],
        "type" : "object"
      },
      "DatosDeProducto" : {
        "description" : "El formulario «producto», el mismo para crear y para editar. Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"producto\"}",
        "properties" : {
          "costoConsumibles" : {
            "description" : "Tinta, papel transfer, hilo, energía: lo que se gasta produciendo la unidad (05 §7.1). Sin él, cero",
            "format" : "int64",
            "minimum" : 0,
            "type" : "integer"
          },
          "costoInsumo" : {
            "description" : "Lo que cuesta el material que se convierte en el producto (05 §7.1). Sin él, cero",
            "format" : "int64",
            "minimum" : 0,
            "type" : "integer"
          },
          "minutosTrabajo" : {
            "description" : "Los minutos de persona que lleva una unidad, con dos decimales como máximo, que es lo que guarda NUMERIC(6,2) (04 §4.1); más decimales responden 42240. En cero cuando el ítem no consume tiempo del taller: entonces su margen por hora no existe, y viaja vacío en vez de en cero",
            "minimum" : 0,
            "type" : "number"
          },
          "nombre" : {
            "description" : "Cómo se llama en el taller",
            "type" : "string"
          },
          "precioVenta" : {
            "description" : "Pesos enteros (ADR-003). En cero mientras no se decida a cuánto se vende: el producto existe y todavía no tiene márgenes que calcular",
            "format" : "int64",
            "minimum" : 0,
            "type" : "integer"
          },
          "tipo" : {
            "description" : "Los dos valores del ENUM tipo_item. Los dos se costean igual; la diferencia es que un servicio no tiene unidades en bodega",
            "enum" : [ "producto", "servicio" ],
            "type" : "string"
          },
          "unidad" : {
            "description" : "En qué se cuenta: unidad, docena, metro. Sin ella, «unidad»",
            "type" : "string"
          }
        },
        "required" : [ "nombre", "tipo" ],
        "type" : "object"
      },
      "Desactivacion" : {
        "properties" : {
          "en" : {
            "format" : "date-time",
            "type" : "string"
          },
          "motivo" : {
            "type" : "string"
          },
          "por" : {
            "description" : "El nombre completo de quien la hizo",
            "type" : "string"
          }
        },
        "required" : [ "en", "motivo", "por" ],
        "type" : "object"
      },
      "DescriptorDeCampo" : {
        "properties" : {
          "ayuda" : {
            "description" : "La línea gris debajo de la caja. Ausente si no hace falta",
            "type" : "string"
          },
          "campo" : {
            "description" : "El nombre con el que viaja el dato, y con el que vuelve un error en data.errores",
            "type" : "string"
          },
          "etiqueta" : {
            "description" : "Lo que se pinta encima de la caja",
            "type" : "string"
          },
          "maximo" : {
            "description" : "Límite superior: pesos en dinero, caracteres en texto y en clave, la cantidad en numero. Ausente si no hay",
            "format" : "int64",
            "type" : "integer"
          },
          "mensajes" : {
            "$ref" : "#/components/schemas/MensajesDeCampo"
          },
          "minimo" : {
            "description" : "Límite inferior: pesos en dinero, caracteres en texto y en clave, la cantidad en numero. Ausente si no hay",
            "format" : "int64",
            "type" : "integer"
          },
          "obligatorio" : {
            "description" : "Si puede quedar vacío",
            "type" : "boolean"
          },
          "opciones" : {
            "description" : "Solo en una lista cerrada: las opciones en el orden en que se pintan. Ausente si la lista trae origen",
            "items" : {
              "$ref" : "#/components/schemas/OpcionDeCampo"
            },
            "type" : "array"
          },
          "origen" : {
            "description" : "Solo en una lista que sale de datos: la ruta de consulta de este contrato que la llena. Su data es una lista de objetos con id y nombre; el valor es el id y la etiqueta, el nombre. Ausente si la lista trae opciones",
            "type" : "string"
          },
          "teclado" : {
            "description" : "Qué teclado abre el celular. Ausente en fecha, lista y casilla, que no abren teclado",
            "enum" : [ "numerico", "texto" ],
            "type" : "string"
          },
          "tipo" : {
            "description" : "clave es un texto que se pinta oculto, con un botón para mostrarlo; teclado de texto. numero es una cantidad que no es plata —los minutos de trabajo, un porcentaje—, con teclado numérico",
            "enum" : [ "dinero", "texto", "fecha", "lista", "casilla", "clave", "numero" ],
            "type" : "string"
          }
        },
        "required" : [ "campo", "etiqueta", "mensajes", "obligatorio", "tipo" ],
        "type" : "object"
      },
      "DescriptorDeFormulario" : {
        "properties" : {
          "campos" : {
            "description" : "En el orden en que se pintan. El front no los reordena",
            "items" : {
              "$ref" : "#/components/schemas/DescriptorDeCampo"
            },
            "type" : "array"
          },
          "formulario" : {
            "type" : "string"
          }
        },
        "required" : [ "campos", "formulario" ],
        "type" : "object"
      },
      "DivisionDeRetiro" : {
        "description" : "Cómo propone la API partir un retiro (CU-16): pro-labore hasta completar el del mes y el resto distribución. Es una propuesta: la persona la puede cambiar antes de guardar",
        "properties" : {
          "distribucion" : {
            "description" : "Lo que queda como distribución",
            "format" : "int64",
            "type" : "integer"
          },
          "explicacion" : {
            "description" : "De dónde salen las dos cifras, con los montos: el pro-labore del mes, lo que ya se retiró como pro-labore y lo que falta. Sin pro-labore definido, dice que todo es distribución y por qué eso esconde el trabajo propio (05 §6.3)",
            "type" : "string"
          },
          "prolabore" : {
            "description" : "Lo que propone tomar como pro-labore",
            "format" : "int64",
            "type" : "integer"
          }
        },
        "required" : [ "distribucion", "explicacion", "prolabore" ],
        "type" : "object"
      },
      "Documento" : {
        "description" : "Un archivo que la API entrega: el desprendible, la cotización o la remisión. Viaja dentro del sobre, en data, como cualquier otra respuesta, y no como bytes crudos: así una misma ruta no tiene dos formas de contestar, una para cuando sale bien y otra para el 40300 o el 40400 (tarea 8.11). El contenido va en base64, que cuesta un 33% de tamaño, y por eso el tope es de 5 MB, el mismo del adjunto de subida",
        "properties" : {
          "bytes" : {
            "description" : "Cuánto pesa el archivo ya decodificado. Nunca más de 5.242.880",
            "format" : "int64",
            "type" : "integer"
          },
          "contenido" : {
            "description" : "El archivo entero en base64, sin saltos de línea ni prefijo de tipo",
            "format" : "byte",
            "type" : "string"
          },
          "nombre" : {
            "description" : "El nombre con que conviene guardarlo, como desprendible-2026-09-marcela-rios.pdf",
            "type" : "string"
          },
          "tipo" : {
            "description" : "El tipo de contenido del archivo",
            "enum" : [ "application/pdf" ],
            "type" : "string"
          }
        },
        "required" : [ "bytes", "contenido", "nombre", "tipo" ],
        "type" : "object"
      },
      "EdicionDeEmpleada" : {
        "description" : "El formulario «edicion-de-empleada». La fecha de ingreso no está: cambiarla movería de mes liquidaciones ya hechas",
        "properties" : {
          "documento" : {
            "type" : "string"
          },
          "horasMensuales" : {
            "description" : null,
            "minimum" : 0,
            "type" : "number"
          },
          "nombre" : {
            "type" : "string"
          },
          "salarioAcordado" : {
            "description" : "El aumento se escribe encima: empleados no guarda historial de salario, a diferencia de costos_producto (04 §4.5). Las liquidaciones ya hechas conservan el suyo en nomina_detalle.salario_base",
            "format" : "int64",
            "minimum" : 1,
            "type" : "integer"
          },
          "usuarioId" : {
            "description" : "Con null se desconecta de su usuario",
            "format" : "uuid",
            "type" : "string"
          }
        },
        "required" : [ "horasMensuales", "nombre", "salarioAcordado" ],
        "type" : "object"
      },
      "EdicionDeUsuario" : {
        "description" : "El formulario «edicion-de-usuario». El usuario no está: es la identidad con la que la persona entra, y no se edita",
        "properties" : {
          "cargoId" : {
            "description" : "Uno de POST /api/v0/consultas/cargos-asignables, o el que la persona ya tenía aunque esté inactivo",
            "format" : "uuid",
            "type" : "string"
          },
          "nombreCompleto" : {
            "type" : "string"
          },
          "tipo" : {
            "enum" : [ "gerencia", "operacion" ],
            "type" : "string"
          }
        },
        "required" : [ "cargoId", "nombreCompleto", "tipo" ],
        "type" : "object"
      },
      "Empleada" : {
        "description" : "Quien está en nómina. No es lo mismo que un usuario: se puede estar en nómina sin entrar al sistema y al revés (04 §4.2). usuarioId las conecta cuando son la misma persona",
        "properties" : {
          "activa" : {
            "description" : "Sigue en nómina: no tiene fecha de retiro ni está anulada",
            "type" : "boolean"
          },
          "anulacion" : {
            "$ref" : "#/components/schemas/Anulacion",
            "description" : "Ausente mientras la ficha esté vigente"
          },
          "documento" : {
            "description" : "La cédula, si se registró. Sale en el desprendible (06 §8)",
            "type" : "string"
          },
          "fechaIngreso" : {
            "description" : "Desde cuándo trabaja",
            "format" : "date",
            "type" : "string"
          },
          "fechaRetiro" : {
            "description" : "Desde cuándo dejó de estar en nómina. Ausente mientras siga",
            "format" : "date",
            "type" : "string"
          },
          "horasMensuales" : {
            "description" : "Las horas pactadas al mes, con las que se calcula el valor de la hora ordinaria (06 §6.1). 192 por omisión",
            "minimum" : 0,
            "type" : "number"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "nombre" : {
            "description" : "La fuente de verdad para la nómina es esta, no el nombre del usuario (04 §4.2)",
            "type" : "string"
          },
          "salarioAcordado" : {
            "description" : "Pesos enteros, mayor que cero. Solo Gerencia lo ve de otra persona: empleados lleva RLS y cada quien alcanza su propia ficha (04 §7)",
            "format" : "int64",
            "type" : "integer"
          },
          "usuario" : {
            "description" : "El nombre de usuario con que entra, si entra",
            "type" : "string"
          },
          "usuarioId" : {
            "description" : "El usuario al que corresponde, si alguno. Es UNIQUE: dos fichas no cuelgan del mismo usuario",
            "format" : "uuid",
            "type" : "string"
          }
        },
        "required" : [ "activa", "fechaIngreso", "horasMensuales", "id", "nombre", "salarioAcordado" ],
        "type" : "object"
      },
      "EntradaDeBitacora" : {
        "properties" : {
          "autor" : {
            "description" : "El nombre completo de quien hizo el cambio",
            "type" : "string"
          },
          "detalle" : {
            "description" : "El «de → a» del cambio, o qué deshizo una reversión. Ausente si no hay",
            "type" : "string"
          },
          "en" : {
            "format" : "date-time",
            "type" : "string"
          },
          "etiqueta" : {
            "description" : "Lo que dice la columna «Qué pasó»: Cargo cambiado, Desactivado, Reversión…",
            "type" : "string"
          },
          "evento" : {
            "enum" : [ "usuario_creado", "nombre_cambiado", "cargo_cambiado", "tipo_cambiado", "clave_restablecida", "desactivado", "reactivado", "cargo_creado", "cargo_renombrado", "cargo_desactivado", "cargo_reactivado", "reversion" ],
            "type" : "string"
          },
          "id" : {
            "format" : "int64",
            "type" : "integer"
          },
          "motivo" : {
            "description" : "Ausente si el cambio no pedía motivo",
            "type" : "string"
          },
          "reversion" : {
            "$ref" : "#/components/schemas/Reversion"
          },
          "revertidaPor" : {
            "description" : "La entrada que la deshizo. Ausente si no se ha revertido",
            "format" : "int64",
            "type" : "integer"
          },
          "revierteA" : {
            "description" : "Solo en una reversión: la entrada que deshace",
            "format" : "int64",
            "type" : "integer"
          },
          "sobre" : {
            "description" : "Sobre quién o sobre qué fue el cambio, ya redactado: una persona, o «Cargo «Domiciliaria»»",
            "type" : "string"
          }
        },
        "required" : [ "autor", "en", "etiqueta", "evento", "id", "reversion", "sobre" ],
        "type" : "object"
      },
      "Entrega" : {
        "description" : "El formulario «entrega». Es lo que recibe fn_entregar_pedido (04 §10) y nada más: declarar más campos sería invitar a la API a rehacer los pasos de la función",
        "properties" : {
          "cuentaId" : {
            "description" : "Dónde entra el saldo. Sin ella se entrega igual y el saldo queda por cobrar (CU-07 A1)",
            "format" : "uuid",
            "type" : "string"
          },
          "fecha" : {
            "description" : "La fecha de entrega, que es la que causa la venta (RN-06). Sin ella, hoy en Bogotá",
            "format" : "date",
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "FilaDeImportacion" : {
        "description" : "Qué pasó con una fila del archivo. El informe las trae todas, no solo las malas (RF-65, BDD-21-2)",
        "properties" : {
          "campo" : {
            "description" : "En qué columna está el problema, cuando el estado es invalida",
            "type" : "string"
          },
          "estado" : {
            "description" : "valida se puede escribir; invalida no; duplicada coincide con un movimiento que ya existe —misma fecha, valor, cuenta y descripción (BDD-21-3)—; escrita es lo que la confirmación guardó",
            "enum" : [ "valida", "invalida", "duplicada", "escrita" ],
            "type" : "string"
          },
          "motivo" : {
            "description" : "Por qué se rechaza, en español y ya listo para mostrar",
            "type" : "string"
          },
          "movimientoId" : {
            "description" : "El movimiento que se escribió, cuando el estado es escrita; o el que ya existía, cuando es duplicada",
            "format" : "uuid",
            "type" : "string"
          },
          "numero" : {
            "description" : "La línea del archivo, contando el encabezado como 1",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "estado", "numero" ],
        "type" : "object"
      },
      "Identidad" : {
        "description" : "Quién tiene la sesión abierta: lo que el topbar muestra todo el tiempo (RF-83)",
        "properties" : {
          "cargo" : {
            "description" : "El nombre de su cargo. Ausente si no tiene",
            "type" : "string"
          },
          "debeCambiarClave" : {
            "description" : "Mientras sea true, todo lo que no sea cambiar la contraseña, renovar o cerrar la sesión, o pedir un formulario, responde 40302",
            "type" : "boolean"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "nombreCompleto" : {
            "type" : "string"
          },
          "tipo" : {
            "description" : "Lo único que decide qué puede ver. El cargo describe el oficio",
            "enum" : [ "gerencia", "operacion" ],
            "type" : "string"
          },
          "usuario" : {
            "type" : "string"
          }
        },
        "required" : [ "debeCambiarClave", "id", "nombreCompleto", "tipo", "usuario" ],
        "type" : "object"
      },
      "InformeDeImportacion" : {
        "description" : "Lo que trae un CSV y qué pasaría —o qué pasó— con cada fila. La misma forma la devuelven la consulta, que no escribe nada, y la confirmación, que sí (CU-21)",
        "properties" : {
          "columnas" : {
            "description" : "Las columnas que trae el archivo, en su orden, para que la persona las mapee (BDD-21-1)",
            "items" : {
              "type" : "string"
            },
            "type" : "array"
          },
          "columnasEsperadas" : {
            "description" : "Las que el importador necesita",
            "items" : {
              "$ref" : "#/components/schemas/ColumnaEsperada"
            },
            "type" : "array"
          },
          "duplicadas" : {
            "description" : "Cuántas coinciden con un movimiento que ya existe",
            "format" : "int32",
            "type" : "integer"
          },
          "escritas" : {
            "description" : "Cuántas se guardaron. Cero en la consulta, que no escribe nada",
            "format" : "int32",
            "type" : "integer"
          },
          "filas" : {
            "description" : "Ausente mientras la consulta no traiga mapeo: sin él no hay nada que juzgar",
            "items" : {
              "$ref" : "#/components/schemas/FilaDeImportacion"
            },
            "type" : "array"
          },
          "invalidas" : {
            "description" : null,
            "format" : "int32",
            "type" : "integer"
          },
          "total" : {
            "description" : "Cuántas filas de datos trae el archivo, sin contar el encabezado",
            "format" : "int32",
            "type" : "integer"
          },
          "validas" : {
            "description" : null,
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "columnas", "columnasEsperadas", "total" ],
        "type" : "object"
      },
      "Lectura" : {
        "description" : "Lo que dice una cifra, con su texto y su nivel. La decide la API y el front la pinta sin calcularla (ADR-018)",
        "properties" : {
          "detalle" : {
            "description" : "La explicación con los montos, porque «Retiras 78 centavos de cada peso que genera el negocio» orienta y «revisa tus retiros» no (10 §6)",
            "type" : "string"
          },
          "nivel" : {
            "description" : "De qué color se pinta (10 §3.1): critica en rojo, alta en ámbar e informativa sin alarma",
            "enum" : [ "critica", "alta", "informativa" ],
            "type" : "string"
          },
          "titulo" : {
            "description" : "La frase corta que la encabeza",
            "type" : "string"
          }
        },
        "required" : [ "detalle", "nivel", "titulo" ],
        "type" : "object"
      },
      "LecturaDeMargen" : {
        "description" : "Lo que dice el margen de un producto cuando se lo pone al lado del resto del taller. La decide la API con su texto y su nivel; el front la pinta y no la calcula (ADR-018)",
        "properties" : {
          "detalle" : {
            "description" : "La explicación con los montos de este producto, porque «Deja $26.000 por hora contra $48.000 de la mediana» orienta y «revisa tu margen» no (10 §6)",
            "type" : "string"
          },
          "medianaDelResto" : {
            "description" : "La mediana del margen por hora de los demás productos del cuadro, contra la que se leyó este. Ausente si este no tiene margen por hora o si ningún otro lo tiene",
            "format" : "int64",
            "type" : "integer"
          },
          "nivel" : {
            "description" : "De qué color se pinta (10 §3.1): vender a pérdida es crítica y rendir menos que el resto del taller, alta",
            "enum" : [ "critica", "alta", "informativa" ],
            "type" : "string"
          },
          "titulo" : {
            "description" : "La frase corta que encabeza la lectura: «Rinde bien por hora de trabajo», «Rinde menos que el resto del taller», «A este precio vendes a pérdida», «No consume tiempo de taller» o «Todavía no tiene precio»",
            "type" : "string"
          }
        },
        "required" : [ "detalle", "nivel", "titulo" ],
        "type" : "object"
      },
      "LineaDePedido" : {
        "description" : "Un renglón del pedido: qué producto, cuántos y a qué precio",
        "properties" : {
          "cantidad" : {
            "description" : "Mayor que cero. Lo impone el CHECK pedido_lineas_cantidad_positiva",
            "format" : "int32",
            "minimum" : 1,
            "type" : "integer"
          },
          "precioUnitario" : {
            "description" : "Pesos enteros. Viaja en el pedido y no se toma del catálogo al leerlo: es el precio del día en que se pidió, y el catálogo cambia",
            "format" : "int64",
            "type" : "integer"
          },
          "productoId" : {
            "format" : "uuid",
            "type" : "string"
          }
        },
        "required" : [ "cantidad", "precioUnitario", "productoId" ],
        "type" : "object"
      },
      "Liquidacion" : {
        "description" : "Lo que se le liquida a una empleada en un período. Las cifras salen del 06 §6.1 y las escribe fn_liquidar_nomina en una sola transacción, junto con el movimiento del pago y la marca de los adelantos (04 §10)",
        "properties" : {
          "adelantosDescontados" : {
            "description" : "Los adelantos que esta liquidación canceló. Cada uno se descuenta una sola vez (RN-11)",
            "items" : {
              "$ref" : "#/components/schemas/AdelantoDescontado"
            },
            "type" : "array"
          },
          "anio" : {
            "description" : "El año del período",
            "format" : "int32",
            "type" : "integer"
          },
          "cuentaId" : {
            "description" : "De dónde salió el pago",
            "format" : "uuid",
            "type" : "string"
          },
          "diasTrabajados" : {
            "description" : "De 0 a 31",
            "format" : "int32",
            "type" : "integer"
          },
          "empleada" : {
            "description" : "El nombre de la empleada",
            "type" : "string"
          },
          "empleadaId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "fechaPago" : {
            "description" : null,
            "format" : "date",
            "type" : "string"
          },
          "horasExtra" : {
            "description" : "Las horas extra del período",
            "minimum" : 0,
            "type" : "number"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "liquidadaEn" : {
            "description" : null,
            "format" : "date-time",
            "type" : "string"
          },
          "liquidadaPor" : {
            "description" : "El nombre completo de quien la hizo",
            "type" : "string"
          },
          "mes" : {
            "description" : "El mes del período, para pintar la fila sin pedir otra consulta",
            "format" : "int32",
            "type" : "integer"
          },
          "movimientoId" : {
            "description" : "El movimiento de tipo gasto que reconoce el gasto de personal, completo y una sola vez (06 §6.3)",
            "format" : "uuid",
            "type" : "string"
          },
          "netoPagado" : {
            "description" : "Total devengado menos total de descuentos. Es lo que salió de la cuenta",
            "format" : "int64",
            "type" : "integer"
          },
          "otrosDescuentos" : {
            "description" : null,
            "format" : "int64",
            "type" : "integer"
          },
          "otrosDevengados" : {
            "description" : null,
            "format" : "int64",
            "type" : "integer"
          },
          "periodoId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "recargoExtraPct" : {
            "description" : "El recargo con que se pagó la hora extra, en puntos",
            "format" : "int32",
            "type" : "integer"
          },
          "salarioBase" : {
            "description" : "El salario del período, proporcional a los días trabajados",
            "format" : "int64",
            "type" : "integer"
          },
          "totalDescuentos" : {
            "description" : "Adelantos descontados más otros descuentos",
            "format" : "int64",
            "type" : "integer"
          },
          "totalDevengado" : {
            "description" : "Salario del período más el total de las horas extra más otros devengados. No suma las tarifas por hora: esas son el precio de una hora, no un monto que se devengue (06 §6.2)",
            "format" : "int64",
            "type" : "integer"
          },
          "valorHoraExtra" : {
            "description" : "Valor de la hora ordinaria por uno más el recargo. Es una tarifa, no un devengo",
            "format" : "int64",
            "type" : "integer"
          },
          "valorHoraOrdinaria" : {
            "description" : "Salario acordado dividido entre las horas mensuales pactadas. Es una tarifa, no un devengo",
            "format" : "int64",
            "type" : "integer"
          },
          "valorHorasExtra" : {
            "description" : "Las horas extra por el valor de la hora extra. Esto sí se devenga",
            "format" : "int64",
            "type" : "integer"
          }
        },
        "required" : [ "adelantosDescontados", "anio", "diasTrabajados", "empleadaId", "horasExtra", "id", "mes", "netoPagado", "otrosDescuentos", "otrosDevengados", "periodoId", "recargoExtraPct", "salarioBase", "totalDescuentos", "totalDevengado", "valorHoraExtra", "valorHoraOrdinaria", "valorHorasExtra" ],
        "type" : "object"
      },
      "Margenes" : {
        "description" : "Los tres márgenes de 05 §7.2: margen en pesos = precio − costo unitario; porcentual = margen en pesos ÷ precio; por hora = margen en pesos ÷ (minutos ÷ 60). El tercero es el que guía la decisión cuando hay más pedidos que tiempo",
        "properties" : {
          "enPesos" : {
            "description" : "Precio de venta menos costo unitario. Negativo cuando se vende por debajo de lo que cuesta producirlo",
            "format" : "int64",
            "type" : "integer"
          },
          "porHora" : {
            "description" : "Lo que deja una hora de trabajo. Ausente —y no en cero— cuando el ítem no consume tiempo del taller: en cero quedaría de último en el cuadro como si fuera el peor negocio",
            "format" : "int64",
            "type" : "integer"
          },
          "porcentual" : {
            "description" : "El margen en pesos sobre el precio de venta, en puntos y con dos decimales",
            "type" : "number"
          }
        },
        "required" : [ "enPesos", "porcentual" ],
        "type" : "object"
      },
      "MensajesDeCampo" : {
        "properties" : {
          "maximo" : {
            "type" : "string"
          },
          "minimo" : {
            "type" : "string"
          },
          "obligatorio" : {
            "type" : "string"
          }
        },
        "type" : "object"
      },
      "Motivo" : {
        "description" : "Los once formularios de motivo —«desactivacion-de-usuario», «reactivacion-de-usuario», «desactivacion-de-cargo», «desactivacion-de-producto», «reversion», «anulacion-de-pedido», «anulacion-de-cliente», «anulacion-de-movimiento», «anulacion-de-empleada», «anulacion-de-adelanto» y «anulacion-de-cotizacion»—: el mismo campo, con la etiqueta y los mensajes de cada uno",
        "properties" : {
          "motivo" : {
            "description" : "Qué pasó y por qué. Queda en la ficha, en la bitácora o junto al registro",
            "type" : "string"
          }
        },
        "required" : [ "motivo" ],
        "type" : "object"
      },
      "Movimiento" : {
        "properties" : {
          "adjuntos" : {
            "description" : "Las fotos y los PDF que lo soportan. Vacío si no tiene",
            "items" : {
              "$ref" : "#/components/schemas/Adjunto"
            },
            "type" : "array"
          },
          "anulacion" : {
            "$ref" : "#/components/schemas/Anulacion",
            "description" : "Ausente mientras el movimiento esté vigente. Uno anulado no suma en ninguna cifra"
          },
          "categoria" : {
            "description" : "El nombre de la categoría, para pintar la fila sin pedir otra consulta. Ausente si no tiene",
            "type" : "string"
          },
          "categoriaId" : {
            "description" : "Ausente si no tiene categoría",
            "format" : "uuid",
            "type" : "string"
          },
          "cuenta" : {
            "description" : "El nombre de la cuenta. En una transferencia, la de origen",
            "type" : "string"
          },
          "cuentaDestino" : {
            "description" : "El nombre de la cuenta de destino. Solo en una transferencia",
            "type" : "string"
          },
          "cuentaDestinoId" : {
            "description" : "Solo en una transferencia",
            "format" : "uuid",
            "type" : "string"
          },
          "cuentaId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "descripcion" : {
            "description" : "Ausente si se registró sin ella",
            "type" : "string"
          },
          "fechaMovimiento" : {
            "description" : "Cuándo ocurrió de verdad. Es la que manda en todos los reportes (RN-01)",
            "format" : "date",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "registradoEn" : {
            "description" : "Cuándo se digitó. Manda en la auditoría y nunca en los reportes",
            "format" : "date-time",
            "type" : "string"
          },
          "registradoPor" : {
            "description" : "El nombre completo de quien lo registró",
            "type" : "string"
          },
          "registroTardio" : {
            "description" : "Se digitó más de 7 días después de ocurrido (RN-14). Lo decide la API: el front pinta la marca, no cuenta los días",
            "type" : "boolean"
          },
          "tipo" : {
            "description" : "Los nueve valores del ENUM tipo_movimiento. Qué le suma o le resta a cada cifra está en 05 §2",
            "enum" : [ "ingreso", "gasto", "transferencia", "inversion", "aporte", "retiro_prolabore", "retiro_distribucion", "anticipo_recibido", "adelanto_empleada" ],
            "type" : "string"
          },
          "valor" : {
            "description" : "Pesos enteros y siempre positivo (RN-02)",
            "format" : "int64",
            "type" : "integer"
          }
        },
        "required" : [ "adjuntos", "cuenta", "cuentaId", "fechaMovimiento", "id", "registradoEn", "registradoPor", "registroTardio", "tipo", "valor" ],
        "type" : "object"
      },
      "Navegacion" : {
        "properties" : {
          "puedeGestionarUsuarios" : {
            "description" : "Si el menú de la sesión lleva «Gestión de usuarios». Con false la entrada no existe: ni atenuada ni deshabilitada",
            "type" : "boolean"
          },
          "puedeVerComoOperacion" : {
            "description" : "Si el menú de la sesión lleva el interruptor «Ver como Operación». Sale del tipo real de la sesión, nunca del que se simula",
            "type" : "boolean"
          },
          "secciones" : {
            "description" : "El menú lateral, en el orden en que se pinta",
            "items" : {
              "$ref" : "#/components/schemas/Seccion"
            },
            "type" : "array"
          },
          "vistaPrevia" : {
            "$ref" : "#/components/schemas/AvisoDeVistaPrevia",
            "description" : "Solo con vista=operacion: el aviso de la franja fija, que no se puede cerrar"
          }
        },
        "required" : [ "puedeGestionarUsuarios", "puedeVerComoOperacion", "secciones" ],
        "type" : "object"
      },
      "NuevaCategoria" : {
        "description" : "El formulario «categoria». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"categoria\"}",
        "properties" : {
          "esFijo" : {
            "description" : "Solo una categoría de gasto puede ser fija",
            "type" : "boolean"
          },
          "naturaleza" : {
            "enum" : [ "ingreso", "gasto" ],
            "type" : "string"
          },
          "nombre" : {
            "type" : "string"
          },
          "padreId" : {
            "description" : "La categoría madre, de la misma naturaleza. Ausente para una categoría principal",
            "format" : "uuid",
            "type" : "string"
          }
        },
        "required" : [ "naturaleza", "nombre" ],
        "type" : "object"
      },
      "NuevaConfiguracionDeSobres" : {
        "description" : "El formulario «sobres». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"sobres\"}",
        "properties" : {
          "pctCostoDirecto" : {
            "description" : "Los insumos del próximo pedido. Entre 0 y 100, en puntos enteros: es el dominio porcentaje (04 §4.1)",
            "format" : "int32",
            "maximum" : 100,
            "minimum" : 0,
            "type" : "integer"
          },
          "pctGastosFijos" : {
            "description" : "Arriendo, servicios, internet, pro-labore y nómina",
            "format" : "int32",
            "maximum" : 100,
            "minimum" : 0,
            "type" : "integer"
          },
          "pctReserva" : {
            "description" : "El colchón del negocio para imprevistos",
            "format" : "int32",
            "maximum" : 100,
            "minimum" : 0,
            "type" : "integer"
          },
          "pctRetiro" : {
            "description" : "Lo que se reparte la propiedad. Si los cuatro no suman 100, el aviso llega aquí, con 42293",
            "format" : "int32",
            "maximum" : 100,
            "minimum" : 0,
            "type" : "integer"
          }
        },
        "required" : [ "pctCostoDirecto", "pctGastosFijos", "pctReserva", "pctRetiro" ],
        "type" : "object"
      },
      "NuevaCotizacion" : {
        "description" : "El formulario «cotizacion». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"cotizacion\"}, salvo las líneas, que el descriptor no sabe expresar (20 §4.4)",
        "properties" : {
          "anticipoPct" : {
            "description" : "Qué porcentaje se le propone al cliente. Que alcance para pagar el material lo responde POST /api/v0/consultas/anticipo-minimo",
            "format" : "int32",
            "maximum" : 100,
            "minimum" : 0,
            "type" : "integer"
          },
          "clienteId" : {
            "description" : "Sale de POST /api/v0/consultas/clientes",
            "format" : "uuid",
            "type" : "string"
          },
          "fechaEmision" : {
            "description" : "Sin ella, hoy en Bogotá (RNF-08)",
            "format" : "date",
            "type" : "string"
          },
          "lineas" : {
            "description" : "Al menos una. Sin líneas la cotización no tiene valor, y responde 42280",
            "items" : {
              "$ref" : "#/components/schemas/LineaDePedido"
            },
            "minItems" : 1,
            "type" : "array"
          },
          "notas" : {
            "description" : "Opcional. Sale en el PDF",
            "type" : "string"
          },
          "validaHasta" : {
            "description" : "Anterior a la emisión responde 42281",
            "format" : "date",
            "type" : "string"
          }
        },
        "required" : [ "clienteId", "lineas" ],
        "type" : "object"
      },
      "NuevaCuenta" : {
        "description" : "El formulario «cuenta». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"cuenta\"}",
        "properties" : {
          "nombre" : {
            "description" : "Cómo se llama en el taller: Davivienda, Bancolombia ahorros, Bold",
            "type" : "string"
          },
          "saldoInicial" : {
            "description" : "Pesos enteros. Entra como saldo inicial de la cuenta, no como ingreso: no afecta la utilidad del mes",
            "format" : "int64",
            "minimum" : 0,
            "type" : "integer"
          },
          "tipo" : {
            "enum" : [ "banco", "billetera", "efectivo" ],
            "type" : "string"
          }
        },
        "required" : [ "nombre", "saldoInicial", "tipo" ],
        "type" : "object"
      },
      "NuevaEmpleada" : {
        "description" : "El formulario «empleada». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"empleada\"}",
        "properties" : {
          "documento" : {
            "description" : "La cédula. Opcional",
            "type" : "string"
          },
          "fechaIngreso" : {
            "description" : "Desde cuándo trabaja",
            "format" : "date",
            "type" : "string"
          },
          "horasMensuales" : {
            "default" : 192,
            "description" : "Las horas pactadas al mes. Sin ella, 192",
            "minimum" : 0,
            "type" : "number"
          },
          "nombre" : {
            "description" : "Nombre y apellido",
            "type" : "string"
          },
          "salarioAcordado" : {
            "description" : "Pesos enteros, mayor que cero",
            "format" : "int64",
            "minimum" : 1,
            "type" : "integer"
          },
          "usuarioId" : {
            "description" : "El usuario con que entra al sistema, si entra. Uno que ya tiene ficha responde 42253",
            "format" : "uuid",
            "type" : "string"
          }
        },
        "required" : [ "fechaIngreso", "nombre", "salarioAcordado" ],
        "type" : "object"
      },
      "NuevaLiquidacion" : {
        "description" : "El formulario «liquidacion». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"liquidacion\"}",
        "properties" : {
          "cuentaId" : {
            "description" : "De dónde sale el pago. Sale de POST /api/v0/consultas/cuentas",
            "format" : "uuid",
            "type" : "string"
          },
          "diasTrabajados" : {
            "default" : 30,
            "description" : "De 0 a 31. Sin ellos, 30",
            "format" : "int32",
            "maximum" : 31,
            "minimum" : 0,
            "type" : "integer"
          },
          "empleadaId" : {
            "description" : "Sale de POST /api/v0/consultas/empleados. Liquidarla dos veces en el mismo período responde 40952",
            "format" : "uuid",
            "type" : "string"
          },
          "fechaPago" : {
            "description" : "Sin ella, hoy en Bogotá (RNF-08)",
            "format" : "date",
            "type" : "string"
          },
          "horasExtra" : {
            "default" : 0,
            "description" : "Las horas extra del período. Sin ellas, cero",
            "minimum" : 0,
            "type" : "number"
          },
          "otrosDescuentos" : {
            "default" : 0,
            "description" : "Pesos enteros. Los adelantos no van aquí: los toma la liquidación sola",
            "format" : "int64",
            "minimum" : 0,
            "type" : "integer"
          },
          "otrosDevengados" : {
            "default" : 0,
            "description" : "Pesos enteros",
            "format" : "int64",
            "minimum" : 0,
            "type" : "integer"
          },
          "periodoId" : {
            "description" : "Sale de PUT /api/v0/nomina/periodos/{id}. Uno ya cerrado responde 40951",
            "format" : "uuid",
            "type" : "string"
          },
          "recargoExtraPct" : {
            "default" : 25,
            "description" : "Con cuánto recargo se paga la hora extra, en puntos. Sin él, 25, que es la cifra del ejemplo del 06 §6.2. Viaja aquí y no en la ficha porque empleados no tiene columna para él (tarea 8.11)",
            "format" : "int32",
            "maximum" : 200,
            "minimum" : 0,
            "type" : "integer"
          }
        },
        "required" : [ "cuentaId", "empleadaId", "periodoId" ],
        "type" : "object"
      },
      "NuevaSesion" : {
        "description" : "El formulario «acceso». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"acceso\"}",
        "properties" : {
          "clave" : {
            "description" : "Viaja solo en esta petición. No se guarda, no se registra y nunca llega a la auditoría",
            "type" : "string"
          },
          "usuario" : {
            "description" : "Tal como la persona lo escribe: la API lo pasa a minúsculas",
            "type" : "string"
          }
        },
        "required" : [ "clave", "usuario" ],
        "type" : "object"
      },
      "NuevoActivo" : {
        "description" : "El formulario «activo». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"activo\"}",
        "properties" : {
          "cuentaId" : {
            "description" : "De dónde salió la plata. Sale de POST /api/v0/consultas/cuentas, y una que no existe o está anulada responde 42291. Sin ella el activo entra sin movimiento: es como se registran los equipos que el taller ya tenía el día del corte, cuya plata ya no está en ningún saldo (09 §4.1 y §4.3)",
            "format" : "uuid",
            "type" : "string"
          },
          "fechaCompra" : {
            "description" : "Cuándo se compró de verdad. Sin ella, hoy en Bogotá (RNF-08); posterior a hoy responde 42290",
            "format" : "date",
            "type" : "string"
          },
          "nombre" : {
            "description" : "Cómo se le dice en el taller: prensa de calor 40×50, máquina bordadora",
            "type" : "string"
          },
          "valorCompra" : {
            "description" : "Pesos enteros y mayor que cero (ADR-003): es el dominio dinero_positivo de activos.valor_compra",
            "format" : "int64",
            "minimum" : 1,
            "type" : "integer"
          }
        },
        "required" : [ "nombre", "valorCompra" ],
        "type" : "object"
      },
      "NuevoAdelanto" : {
        "description" : "El formulario «adelanto». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"adelanto\"}",
        "properties" : {
          "cuentaId" : {
            "description" : "De dónde sale la plata. Sale de POST /api/v0/consultas/cuentas",
            "format" : "uuid",
            "type" : "string"
          },
          "descripcion" : {
            "description" : "Opcional",
            "type" : "string"
          },
          "empleadaId" : {
            "description" : "Sale de POST /api/v0/consultas/empleados",
            "format" : "uuid",
            "type" : "string"
          },
          "fecha" : {
            "description" : "El día en que se entrega. Sin ella, hoy en Bogotá (RNF-08). Futura responde 42223, que es el CHECK fecha_no_futura del movimiento",
            "format" : "date",
            "type" : "string"
          },
          "valor" : {
            "description" : "Pesos enteros, mayor que cero",
            "format" : "int64",
            "minimum" : 1,
            "type" : "integer"
          }
        },
        "required" : [ "cuentaId", "empleadaId", "valor" ],
        "type" : "object"
      },
      "NuevoAnticipo" : {
        "description" : "El formulario «anticipo». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"anticipo\"}",
        "properties" : {
          "cuentaId" : {
            "description" : "Dónde entra la plata. Sale de POST /api/v0/consultas/cuentas",
            "format" : "uuid",
            "type" : "string"
          },
          "fecha" : {
            "description" : "Cuándo se recibió de verdad. Sin ella, hoy en Bogotá (RNF-08)",
            "format" : "date",
            "type" : "string"
          },
          "valor" : {
            "description" : "Pesos enteros, mayor que cero. Sumado a lo ya cobrado no puede pasar del valor del pedido",
            "format" : "int64",
            "type" : "integer"
          }
        },
        "required" : [ "cuentaId", "valor" ],
        "type" : "object"
      },
      "NuevoAporte" : {
        "description" : "El formulario «aporte». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"aporte\"}",
        "properties" : {
          "cuentaId" : {
            "description" : "Dónde entró la plata. Sale de POST /api/v0/consultas/cuentas, y una que no existe o está anulada responde 42291. Es obligatoria porque todo aporte es un movimiento: aportes_retiros.movimiento_id no admite nulo (04 §4.6)",
            "format" : "uuid",
            "type" : "string"
          },
          "fecha" : {
            "description" : "Cuándo entró de verdad. Sin ella, hoy en Bogotá (RNF-08); posterior a hoy responde 42290",
            "format" : "date",
            "type" : "string"
          },
          "nota" : {
            "description" : "De dónde salió la plata, si sirve recordarlo. Opcional",
            "type" : "string"
          },
          "valor" : {
            "description" : "Pesos enteros y mayor que cero (ADR-003)",
            "format" : "int64",
            "minimum" : 1,
            "type" : "integer"
          }
        },
        "required" : [ "cuentaId", "valor" ],
        "type" : "object"
      },
      "NuevoCliente" : {
        "description" : "El formulario «cliente». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"cliente\"}",
        "properties" : {
          "correo" : {
            "description" : "Opcional",
            "type" : "string"
          },
          "nombre" : {
            "description" : "Como lo conocen en el taller: la persona, el colegio o la empresa",
            "type" : "string"
          },
          "notas" : {
            "description" : "Opcional. Lo que haya que recordar de ese cliente",
            "type" : "string"
          },
          "telefono" : {
            "description" : "Opcional. Es por donde se le escribe",
            "type" : "string"
          }
        },
        "required" : [ "nombre" ],
        "type" : "object"
      },
      "NuevoMovimiento" : {
        "description" : "El formulario «movimiento». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"movimiento\"}",
        "properties" : {
          "categoriaId" : {
            "description" : "Una de POST /api/v0/consultas/categorias. Una transferencia no lleva categoría",
            "format" : "uuid",
            "type" : "string"
          },
          "cuentaDestinoId" : {
            "description" : "Una de POST /api/v0/consultas/cuentas, distinta de cuentaId y solo en una transferencia. Las tres situaciones que no cuadran responden 42226",
            "format" : "uuid",
            "type" : "string"
          },
          "cuentaId" : {
            "description" : "Una de POST /api/v0/consultas/cuentas. En una transferencia es la de origen",
            "format" : "uuid",
            "type" : "string"
          },
          "descripcion" : {
            "description" : "En qué se fue la plata, en palabras. Puede quedar vacía",
            "type" : "string"
          },
          "fechaMovimiento" : {
            "description" : "Cuándo ocurrió de verdad, no cuándo se digita (RN-01). En America/Bogota, y posterior a hoy responde 42223",
            "format" : "date",
            "type" : "string"
          },
          "tipo" : {
            "description" : "Los otros seis tipos del libro se registran desde sus pantallas y llegan con sus contratos",
            "enum" : [ "ingreso", "gasto", "transferencia" ],
            "type" : "string"
          },
          "valor" : {
            "description" : "Pesos enteros y mayor que cero (RN-02, ADR-003)",
            "format" : "int64",
            "minimum" : 1,
            "type" : "integer"
          }
        },
        "required" : [ "cuentaId", "fechaMovimiento", "tipo", "valor" ],
        "type" : "object"
      },
      "NuevoPedido" : {
        "description" : "El formulario «pedido». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"pedido\"}, salvo las líneas, que el descriptor no sabe expresar (20 §4.4)",
        "properties" : {
          "anticipoPct" : {
            "description" : "Qué porcentaje del valor se cobra al confirmar. La política del negocio es 50 % (05 §3.1). Que ese porcentaje alcance para pagar el material es otra pregunta, y la responde el validador de anticipo mínimo (RN-15, tarea 8.11)",
            "format" : "int32",
            "maximum" : 100,
            "minimum" : 0,
            "type" : "integer"
          },
          "clienteId" : {
            "description" : "Sale de POST /api/v0/consultas/clientes",
            "format" : "uuid",
            "type" : "string"
          },
          "fechaEntregaPrevista" : {
            "description" : "Cuándo se prometió, si se prometió",
            "format" : "date",
            "type" : "string"
          },
          "fechaPedido" : {
            "description" : "Cuándo se pidió. Sin ella, hoy en Bogotá (RNF-08)",
            "format" : "date",
            "type" : "string"
          },
          "lineas" : {
            "description" : "Al menos una. Sin líneas el pedido no tiene valor, y responde 42231",
            "items" : {
              "$ref" : "#/components/schemas/LineaDePedido"
            },
            "minItems" : 1,
            "type" : "array"
          },
          "notas" : {
            "description" : "Opcional",
            "type" : "string"
          }
        },
        "required" : [ "clienteId", "lineas" ],
        "type" : "object"
      },
      "NuevoPeriodoDeNomina" : {
        "description" : "El formulario «periodo-de-nomina»",
        "properties" : {
          "anio" : {
            "description" : "Entre 2020 y 2100",
            "format" : "int32",
            "maximum" : 2100,
            "minimum" : 2020,
            "type" : "integer"
          },
          "mes" : {
            "description" : "De 1 a 12",
            "format" : "int32",
            "maximum" : 12,
            "minimum" : 1,
            "type" : "integer"
          }
        },
        "required" : [ "anio", "mes" ],
        "type" : "object"
      },
      "NuevoProlabore" : {
        "description" : "El formulario «prolabore». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"prolabore\"}",
        "properties" : {
          "horasMensuales" : {
            "description" : "Las horas del mes que la gerencia trabaja produciendo, con dos decimales como máximo, que es lo que guarda NUMERIC(6,2) (04 §4.1); más decimales responden 42294. Sin ellas, cero: el trabajo no le pone precio a ningún costeo, y costear responde 40940",
            "minimum" : 0,
            "type" : "number"
          },
          "justificacion" : {
            "description" : "Por qué ese valor: con qué se comparó (05 §6.3). La columna admite nulo, y el formulario la pide",
            "type" : "string"
          },
          "valorMensual" : {
            "description" : "Lo que costaría pagarle a alguien por el trabajo que la gerencia hace en el taller, en pesos enteros (RN-08). Puede ser cero: entonces todo retiro es distribución",
            "format" : "int64",
            "minimum" : 0,
            "type" : "integer"
          }
        },
        "required" : [ "justificacion", "valorMensual" ],
        "type" : "object"
      },
      "NuevoRetiro" : {
        "description" : "El formulario «retiro». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"retiro\"}",
        "properties" : {
          "cuentaId" : {
            "description" : "De dónde salió la plata. Sale de POST /api/v0/consultas/cuentas, y una que no existe o está anulada responde 42291",
            "format" : "uuid",
            "type" : "string"
          },
          "fecha" : {
            "description" : "Cuándo salió de verdad. Sin ella, hoy en Bogotá (RNF-08); posterior a hoy responde 42290. De ella sale el mes cuyo pro-labore se completa",
            "format" : "date",
            "type" : "string"
          },
          "nota" : {
            "description" : "Opcional",
            "type" : "string"
          },
          "prolabore" : {
            "description" : "Cuánto de este retiro paga el trabajo de la gerencia en el taller, que es gasto (RN-08); el resto es distribución y no toca la utilidad (RN-07). Mayor que el retiro responde 42292. Sin él, la API lo parte sola: pro-labore hasta completar el del mes, vigente en la fecha del retiro, y el resto distribución (CU-16, BDD-25-1). Es lo mismo que propone POST /api/v0/consultas/division-de-retiro",
            "format" : "int64",
            "minimum" : 0,
            "type" : "integer"
          },
          "valor" : {
            "description" : "Todo lo que se saca, en pesos enteros y mayor que cero (ADR-003)",
            "format" : "int64",
            "minimum" : 1,
            "type" : "integer"
          }
        },
        "required" : [ "cuentaId", "valor" ],
        "type" : "object"
      },
      "NuevoUsuario" : {
        "description" : "El formulario «usuario». Sus reglas y sus mensajes están en POST /api/v0/consultas/formularios con {\"nombre\": \"usuario\"}",
        "properties" : {
          "cargoId" : {
            "description" : "Uno de POST /api/v0/consultas/cargos-asignables",
            "format" : "uuid",
            "type" : "string"
          },
          "claveTemporal" : {
            "description" : "Se muestra en claro a propósito: Gerencia la dicta en persona. No vuelve en ninguna respuesta",
            "type" : "string"
          },
          "nombreCompleto" : {
            "type" : "string"
          },
          "tipo" : {
            "enum" : [ "gerencia", "operacion" ],
            "type" : "string"
          },
          "usuario" : {
            "description" : "La API lo pasa a minúsculas antes de comprobar el formato y si ya existe",
            "type" : "string"
          }
        },
        "required" : [ "cargoId", "claveTemporal", "nombreCompleto", "tipo", "usuario" ],
        "type" : "object"
      },
      "OpcionDeCampo" : {
        "properties" : {
          "etiqueta" : {
            "description" : "Lo que se pinta en la lista",
            "type" : "string"
          },
          "valor" : {
            "description" : "Lo que viaja en el dato cuando se elige",
            "type" : "string"
          }
        },
        "required" : [ "etiqueta", "valor" ],
        "type" : "object"
      },
      "OrdenDeCargos" : {
        "properties" : {
          "cargos" : {
            "description" : "Los ids de todos los cargos del catálogo, activos e inactivos, en el orden nuevo",
            "items" : {
              "format" : "uuid",
              "type" : "string"
            },
            "type" : "array"
          }
        },
        "required" : [ "cargos" ],
        "type" : "object"
      },
      "PaginaDeBitacora" : {
        "properties" : {
          "entradas" : {
            "description" : "Las más recientes primero",
            "items" : {
              "$ref" : "#/components/schemas/EntradaDeBitacora"
            },
            "type" : "array"
          },
          "total" : {
            "description" : "Cuántas entradas hay en total, para decir cuántas faltan",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "entradas", "total" ],
        "type" : "object"
      },
      "PaginaDeMovimientos" : {
        "description" : "El libro es la única lista que crece sin tope, así que dice cuántos hay además de traer los primeros",
        "properties" : {
          "movimientos" : {
            "description" : "Los más recientes primero, por fecha del movimiento",
            "items" : {
              "$ref" : "#/components/schemas/Movimiento"
            },
            "type" : "array"
          },
          "total" : {
            "description" : "Cuántos hay en total con esos filtros, para decir cuántos faltan",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "movimientos", "total" ],
        "type" : "object"
      },
      "Patrimonio" : {
        "description" : "Lo que se ha construido y lo que se ha sacado (05 §6.4 y §9.3). Todas las cifras las calcula la API; el front no suma ni divide (ADR-018)",
        "properties" : {
          "aportes" : {
            "description" : "Todo lo aportado desde afuera, desde el primer registro",
            "format" : "int64",
            "type" : "integer"
          },
          "distribuciones" : {
            "description" : "Todo lo retirado como distribución desde el primer registro, en positivo. El pro-labore no entra: es gasto y ya está descontado de las utilidades",
            "format" : "int64",
            "type" : "integer"
          },
          "distribucionesDoceMeses" : {
            "description" : "Lo retirado como distribución en los últimos 12 meses",
            "format" : "int64",
            "type" : "integer"
          },
          "lectura" : {
            "$ref" : "#/components/schemas/Lectura",
            "description" : "Qué dice la tasa de retiro. En nivel critica es la alerta de descapitalización (RF-49, CU-24, BDD-24-1): las distribuciones de los últimos 12 meses superan las utilidades del mismo período, «Estás retirando más de lo que el negocio genera». Si no, informativa"
          },
          "patrimonio" : {
            "description" : "Aportes menos distribuciones más utilidades acumuladas (05 §6.4). Puede ser negativo",
            "format" : "int64",
            "type" : "integer"
          },
          "retiroDelMes" : {
            "$ref" : "#/components/schemas/RetiroDelMes",
            "description" : "Cómo se partió lo retirado en el mes en curso, en Bogotá"
          },
          "tasaDeRetiro" : {
            "description" : "Las distribuciones de los últimos 12 meses entre las utilidades de los últimos 12 meses, en puntos y con dos decimales (05 §9.3). Ausente cuando las utilidades del período no son positivas: dividir entre cero o entre una pérdida no dice nada, y la lectura lo dice en palabras",
            "type" : "number"
          },
          "utilidadesAcumuladas" : {
            "description" : "Las utilidades causadas desde el primer registro, con el pro-labore ya descontado. Negativas si el negocio acumula pérdidas",
            "format" : "int64",
            "type" : "integer"
          },
          "utilidadesDoceMeses" : {
            "description" : "Las utilidades causadas de los últimos 12 meses. Puede ser negativa",
            "format" : "int64",
            "type" : "integer"
          }
        },
        "required" : [ "aportes", "distribuciones", "distribucionesDoceMeses", "lectura", "patrimonio", "retiroDelMes", "utilidadesAcumuladas", "utilidadesDoceMeses" ],
        "type" : "object"
      },
      "Pedido" : {
        "properties" : {
          "anticipado" : {
            "description" : "Lo ya cobrado en anticipos vigentes de este pedido",
            "format" : "int64",
            "type" : "integer"
          },
          "anticipoPct" : {
            "format" : "int32",
            "type" : "integer"
          },
          "anulacion" : {
            "$ref" : "#/components/schemas/Anulacion",
            "description" : "Ausente mientras el pedido esté vigente"
          },
          "cliente" : {
            "description" : "El nombre del cliente, para pintar la fila sin pedir otra consulta",
            "type" : "string"
          },
          "clienteId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "estado" : {
            "description" : "Los cinco valores del ENUM estado_pedido. entregado y cancelado son finales: lo que haya que corregir después va por contra-asiento (04 §5.3)",
            "enum" : [ "cotizado", "en_proceso", "parcial", "entregado", "cancelado" ],
            "type" : "string"
          },
          "estancado" : {
            "description" : "Tiene anticipo cobrado y lleva 15 días o más sin entregarse (RF-24). Lo decide la API: el front pinta el ámbar, no cuenta los días",
            "type" : "boolean"
          },
          "fechaEntregaPrevista" : {
            "format" : "date",
            "type" : "string"
          },
          "fechaEntregaReal" : {
            "description" : "Solo cuando se entregó. Es el CHECK entregado_con_fecha",
            "format" : "date",
            "type" : "string"
          },
          "fechaPedido" : {
            "format" : "date",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "lineas" : {
            "items" : {
              "$ref" : "#/components/schemas/LineaDePedido"
            },
            "type" : "array"
          },
          "notas" : {
            "type" : "string"
          },
          "numero" : {
            "description" : "El número visible del pedido, como P-0287. Lo pone la API y es único en toda la base",
            "type" : "string"
          },
          "saldoPendiente" : {
            "description" : "El valor total menos lo anticipado (RF-23)",
            "format" : "int64",
            "type" : "integer"
          },
          "valorTotal" : {
            "description" : "Pesos enteros. Sale de las líneas",
            "format" : "int64",
            "type" : "integer"
          }
        },
        "required" : [ "anticipado", "anticipoPct", "clienteId", "estado", "estancado", "fechaPedido", "id", "lineas", "numero", "saldoPendiente", "valorTotal" ],
        "type" : "object"
      },
      "PeriodoDeNomina" : {
        "description" : "El mes que se liquida. Es único por año y mes: lo impone nomina_periodos_anio_mes_key",
        "properties" : {
          "anio" : {
            "description" : "Entre 2020 y 2100",
            "format" : "int32",
            "type" : "integer"
          },
          "cerrado" : {
            "description" : "Ya no admite más liquidaciones",
            "type" : "boolean"
          },
          "cerradoEn" : {
            "description" : null,
            "format" : "date-time",
            "type" : "string"
          },
          "cerradoPor" : {
            "description" : "El nombre completo de quien lo cerró",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "liquidaciones" : {
            "description" : "Cuántas empleadas se liquidaron en él",
            "format" : "int32",
            "type" : "integer"
          },
          "mes" : {
            "description" : "De 1 a 12",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "anio", "cerrado", "id", "mes" ],
        "type" : "object"
      },
      "Productividad" : {
        "description" : "Horas pagadas contra horas facturadas, y lo que cuesta la diferencia (06 §9, CU-27)",
        "properties" : {
          "anio" : {
            "description" : null,
            "format" : "int32",
            "type" : "integer"
          },
          "costoDelOcio" : {
            "description" : "Horas ociosas por la tarifa de la hora",
            "format" : "int64",
            "type" : "integer"
          },
          "horasFacturadas" : {
            "description" : "Los minutos por unidad de lo entregado en el mes, divididos entre 60",
            "minimum" : 0,
            "type" : "number"
          },
          "horasOciosas" : {
            "description" : "Pagadas menos facturadas",
            "minimum" : 0,
            "type" : "number"
          },
          "horasPagadas" : {
            "description" : "Nómina más pro-labore",
            "minimum" : 0,
            "type" : "number"
          },
          "horasPagadasNomina" : {
            "description" : "Las horas pactadas de quienes se liquidaron en el mes",
            "minimum" : 0,
            "type" : "number"
          },
          "horasPagadasProlabore" : {
            "description" : "Las horas del pro-labore vigente",
            "minimum" : 0,
            "type" : "number"
          },
          "lectura" : {
            "$ref" : "#/components/schemas/Lectura",
            "description" : "Cuál de los tres tramos del 06 §9 es —menos del 15%, entre 15 y 35, o más de 35— y qué conviene hacer"
          },
          "mes" : {
            "description" : null,
            "format" : "int32",
            "type" : "integer"
          },
          "porcentajeOcioso" : {
            "description" : "Horas ociosas sobre horas pagadas, en puntos",
            "type" : "number"
          },
          "tarifaHora" : {
            "description" : "La que la API tomó del pro-labore vigente",
            "format" : "int64",
            "type" : "integer"
          }
        },
        "required" : [ "anio", "costoDelOcio", "horasFacturadas", "horasOciosas", "horasPagadas", "horasPagadasNomina", "horasPagadasProlabore", "lectura", "mes", "porcentajeOcioso", "tarifaHora" ],
        "type" : "object"
      },
      "Producto" : {
        "properties" : {
          "activo" : {
            "description" : "Si se sigue ofreciendo. Uno inactivo no entra en pedidos nuevos y sigue entero en los viejos",
            "type" : "boolean"
          },
          "costeo" : {
            "$ref" : "#/components/schemas/Costeo",
            "description" : "El costo con el que se vende hoy. Solo a Gerencia: costos_producto lleva la política costos_solo_gerencia, así que a una sesión de Operación PostgreSQL no le devuelve ni una fila (04 §7, RF-34)"
          },
          "desactivacion" : {
            "$ref" : "#/components/schemas/Desactivacion",
            "description" : "Solo en un producto inactivo: cuándo, quién y por qué se dejó de ofrecer"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "lectura" : {
            "$ref" : "#/components/schemas/LecturaDeMargen",
            "description" : "Qué dice el margen de este producto frente al resto del taller. Solo a Gerencia"
          },
          "margenes" : {
            "$ref" : "#/components/schemas/Margenes",
            "description" : "Los tres márgenes de 05 §7.2. Solo a Gerencia, y ausente también para ella cuando el producto todavía no tiene precio"
          },
          "nombre" : {
            "type" : "string"
          },
          "precioActual" : {
            "description" : "A cuánto se vende hoy. Es el precio de la ficha; el costeo guarda además el que tenía cuando se registró",
            "format" : "int64",
            "type" : "integer"
          },
          "tipo" : {
            "enum" : [ "producto", "servicio" ],
            "type" : "string"
          },
          "unidad" : {
            "description" : "En qué se cuenta: unidad, docena, metro",
            "type" : "string"
          }
        },
        "required" : [ "activo", "id", "nombre", "precioActual", "tipo", "unidad" ],
        "type" : "object"
      },
      "Prolabore" : {
        "description" : "El sueldo de la gerencia por su trabajo en el taller, que es gasto de personal y baja la utilidad (RN-08, 05 §6.3). No se sobrescribe: cada definición es una fila de prolabore_config con su vigencia (04 §4.6)",
        "properties" : {
          "horasMensuales" : {
            "description" : "Las horas productivas del mes",
            "type" : "number"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "justificacion" : {
            "description" : "Ausente en una definición que se guardó sin ella: la columna lo permite",
            "type" : "string"
          },
          "tarifaHora" : {
            "description" : "Lo que cuesta una hora de ese trabajo: el valor mensual entre las horas productivas, redondeado a peso (05 §7.1). Es la que toman los costeos nuevos desde que esta definición rige. Ausente con cero horas",
            "format" : "int64",
            "type" : "integer"
          },
          "valorMensual" : {
            "description" : "Pesos enteros",
            "format" : "int64",
            "type" : "integer"
          },
          "vigenteDesde" : {
            "description" : "El día en que se guardó, en Bogotá. La vigente es la de la fecha mayor y, si hay varias del mismo día, la última registrada",
            "format" : "date",
            "type" : "string"
          }
        },
        "required" : [ "horasMensuales", "id", "valorMensual", "vigenteDesde" ],
        "type" : "object"
      },
      "Retiro" : {
        "description" : "Plata que la gerencia saca del negocio, partida en sus dos naturalezas (05 §6.3): las dos partes bajan la caja y el patrimonio, y solo el pro-labore baja la utilidad. Cada parte que no es cero es un movimiento y una fila de aportes_retiros",
        "properties" : {
          "cuentaId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "distribucion" : {
            "description" : "Lo que se reparte por ser propietario (RN-07). Cero si todo fue pro-labore",
            "format" : "int64",
            "type" : "integer"
          },
          "fecha" : {
            "format" : "date",
            "type" : "string"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "nota" : {
            "description" : "Ausente si se registró sin ella",
            "type" : "string"
          },
          "prolabore" : {
            "description" : "Lo que paga el trabajo en el taller (RN-08). Cero si todo fue distribución",
            "format" : "int64",
            "type" : "integer"
          },
          "valor" : {
            "description" : "El retiro entero: pro-labore más distribución",
            "format" : "int64",
            "type" : "integer"
          }
        },
        "required" : [ "cuentaId", "distribucion", "fecha", "id", "prolabore", "valor" ],
        "type" : "object"
      },
      "RetiroDeEmpleada" : {
        "description" : "El formulario «retiro-de-empleada». No anula la ficha: la deja con sus liquidaciones y fuera de la nómina de los meses siguientes",
        "properties" : {
          "fechaRetiro" : {
            "description" : "Anterior a la fecha de ingreso responde 42250",
            "format" : "date",
            "type" : "string"
          }
        },
        "required" : [ "fechaRetiro" ],
        "type" : "object"
      },
      "RetiroDelMes" : {
        "description" : "Cómo se partió lo retirado en un mes: el bloque «Cómo se parte el retiro» de 10 §4.5",
        "properties" : {
          "anio" : {
            "format" : "int32",
            "type" : "integer"
          },
          "distribucion" : {
            "description" : "No es gasto: no toca la utilidad (RN-07)",
            "format" : "int64",
            "type" : "integer"
          },
          "mes" : {
            "description" : "De 1 a 12",
            "format" : "int32",
            "type" : "integer"
          },
          "prolabore" : {
            "description" : "Es gasto: baja la utilidad (RN-08)",
            "format" : "int64",
            "type" : "integer"
          },
          "total" : {
            "description" : "Pro-labore más distribución. Cero si el mes no tiene retiros",
            "format" : "int64",
            "type" : "integer"
          }
        },
        "required" : [ "anio", "distribucion", "mes", "prolabore", "total" ],
        "type" : "object"
      },
      "Reversion" : {
        "properties" : {
          "posible" : {
            "description" : "Si hoy se puede revertir",
            "type" : "boolean"
          },
          "texto" : {
            "description" : "Si se puede, qué va a pasar: «El cargo volverá de Domiciliaria a Empleada de producción». Si no, por qué. Se muestra antes de pedir el motivo",
            "type" : "string"
          }
        },
        "required" : [ "posible", "texto" ],
        "type" : "object"
      },
      "Seccion" : {
        "properties" : {
          "clave" : {
            "description" : "Qué pantalla es: dashboard, pedidos, movimientos, productos, capital, reportes, nomina o cotizador. Una clave que el front no conoce no se pinta",
            "type" : "string"
          },
          "etiqueta" : {
            "description" : "Lo que dice el menú",
            "type" : "string"
          }
        },
        "required" : [ "clave", "etiqueta" ],
        "type" : "object"
      },
      "Sesion" : {
        "properties" : {
          "claveDeFirma" : {
            "description" : "32 bytes al azar en base64. Firma cada petición de esta sesión y vive solo en la memoria del front: nunca en localStorage, en una cookie ni en disco (ADR-021)",
            "type" : "string"
          },
          "identidad" : {
            "$ref" : "#/components/schemas/Identidad"
          },
          "token" : {
            "description" : "El token de acceso. Viaja en Authorization: Bearer en cada petición con sesión",
            "type" : "string"
          },
          "tokenExpiraEn" : {
            "description" : "Cuándo deja de servir el token. Antes de eso, el front renueva la sesión",
            "format" : "date-time",
            "type" : "string"
          }
        },
        "required" : [ "claveDeFirma", "identidad", "token", "tokenExpiraEn" ],
        "type" : "object"
      },
      "Simulacion" : {
        "description" : "El formulario «simulacion». Es el primer descriptor que alimenta una lectura y no una escritura: sus tres controles tienen mínimos y máximos, y si no los manda la API los decide el front (ADR-018)",
        "properties" : {
          "horasMensuales" : {
            "default" : 192,
            "description" : "La jornada que se evalúa contratar, en horas al mes. Sin ella, 192",
            "minimum" : 0,
            "type" : "number"
          },
          "mesesDeHistoria" : {
            "default" : 6,
            "description" : "Cuántos meses de utilidad se promedian. Sin ellos, 6, que es lo que dice el 06 §3.1",
            "format" : "int32",
            "maximum" : 24,
            "minimum" : 1,
            "type" : "integer"
          },
          "reservaPct" : {
            "default" : 20,
            "description" : "La reserva de seguridad, en puntos. Sin ella, 20, que es la del ejemplo del 06 §3.3",
            "format" : "int32",
            "maximum" : 90,
            "minimum" : 0,
            "type" : "integer"
          },
          "salarioTentativo" : {
            "description" : "El salario mensual que se está evaluando, en pesos enteros",
            "format" : "int64",
            "minimum" : 1,
            "type" : "integer"
          }
        },
        "required" : [ "salarioTentativo" ],
        "type" : "object"
      },
      "SimulacionDeContratacion" : {
        "description" : "La respuesta del simulador (06 §3 y §4). El front la pinta y no calcula ninguna de estas cifras (ADR-018)",
        "properties" : {
          "alternativas" : {
            "description" : "Vacía cuando sí es viable",
            "items" : {
              "$ref" : "#/components/schemas/AlternativaDeContratacion"
            },
            "type" : "array"
          },
          "avisoDeHistoria" : {
            "description" : "Presente cuando hay menos meses de los pedidos: la proyección es menos confiable (CU-18 A1)",
            "type" : "string"
          },
          "diferencia" : {
            "description" : "Presupuesto menos costo mensual. Positiva es lo que sobra; negativa, lo que falta",
            "format" : "int64",
            "type" : "integer"
          },
          "horasRequeridas" : {
            "description" : "Cuánto trabajo piden las ventas adicionales, por el camino más barato",
            "minimum" : 0,
            "type" : "number"
          },
          "lectura" : {
            "$ref" : "#/components/schemas/Lectura",
            "description" : "El veredicto, con sus montos. Cuando las horas requeridas son menos del 20% de la jornada evaluada, dice que la restricción no es la capacidad sino la demanda (06 §4.2)"
          },
          "mesesPedidos" : {
            "description" : "Cuántos se pidieron",
            "format" : "int32",
            "type" : "integer"
          },
          "mesesUsados" : {
            "description" : "Cuántos meses de historia se promediaron de verdad",
            "format" : "int32",
            "type" : "integer"
          },
          "presupuestoDisponible" : {
            "description" : "Utilidad promedio menos la reserva. Es el presupuesto mensual para personal",
            "format" : "int64",
            "type" : "integer"
          },
          "reserva" : {
            "description" : "Lo que se aparta",
            "format" : "int64",
            "type" : "integer"
          },
          "reservaPct" : {
            "description" : "En puntos",
            "format" : "int32",
            "type" : "integer"
          },
          "salarioTentativo" : {
            "description" : "El salario que se evaluó",
            "format" : "int64",
            "type" : "integer"
          },
          "utilidadPromedio" : {
            "description" : "La utilidad operativa promedio del período, obligatoriamente con el pro-labore ya descontado como gasto (RN-09). Sin pro-labore definido no hay respuesta: es el 40950",
            "format" : "int64",
            "type" : "integer"
          },
          "ventasAdicionales" : {
            "description" : "Vacía cuando sí es viable",
            "items" : {
              "$ref" : "#/components/schemas/VentaAdicional"
            },
            "type" : "array"
          },
          "viable" : {
            "description" : "El presupuesto alcanza para el salario evaluado",
            "type" : "boolean"
          }
        },
        "required" : [ "alternativas", "diferencia", "lectura", "mesesPedidos", "mesesUsados", "presupuestoDisponible", "reserva", "reservaPct", "salarioTentativo", "utilidadPromedio", "ventasAdicionales", "viable" ],
        "type" : "object"
      },
      "SobreActivo" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Activo",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreAdelanto" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Adelanto",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreAdjunto" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Adjunto",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreAnticipoMinimo" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/AnticipoMinimo",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreAporte" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Aporte",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreCargo" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Cargo",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreCategoria" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Categoria",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreCliente" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Cliente",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreConfiguracionDeSobres" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/ConfiguracionDeSobres",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreCosteoCalculado" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/CosteoCalculado",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreCotizacion" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Cotizacion",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreCuenta" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Cuenta",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreDescriptorDeFormulario" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/DescriptorDeFormulario",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreDivisionDeRetiro" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/DivisionDeRetiro",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreDocumento" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Documento",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreEmpleada" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Empleada",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreEntradaDeBitacora" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/EntradaDeBitacora",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreIdentidad" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Identidad",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreInformeDeImportacion" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/InformeDeImportacion",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreLiquidacion" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Liquidacion",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreListActivo" : {
        "properties" : {
          "data" : {
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver",
            "items" : {
              "$ref" : "#/components/schemas/Activo"
            },
            "type" : "array"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreListAdelanto" : {
        "properties" : {
          "data" : {
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver",
            "items" : {
              "$ref" : "#/components/schemas/Adelanto"
            },
            "type" : "array"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreListCargo" : {
        "properties" : {
          "data" : {
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver",
            "items" : {
              "$ref" : "#/components/schemas/Cargo"
            },
            "type" : "array"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreListCargoAsignable" : {
        "properties" : {
          "data" : {
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver",
            "items" : {
              "$ref" : "#/components/schemas/CargoAsignable"
            },
            "type" : "array"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreListCategoria" : {
        "properties" : {
          "data" : {
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver",
            "items" : {
              "$ref" : "#/components/schemas/Categoria"
            },
            "type" : "array"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreListCliente" : {
        "properties" : {
          "data" : {
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver",
            "items" : {
              "$ref" : "#/components/schemas/Cliente"
            },
            "type" : "array"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreListCotizacion" : {
        "properties" : {
          "data" : {
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver",
            "items" : {
              "$ref" : "#/components/schemas/Cotizacion"
            },
            "type" : "array"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreListCuenta" : {
        "properties" : {
          "data" : {
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver",
            "items" : {
              "$ref" : "#/components/schemas/Cuenta"
            },
            "type" : "array"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreListEmpleada" : {
        "properties" : {
          "data" : {
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver",
            "items" : {
              "$ref" : "#/components/schemas/Empleada"
            },
            "type" : "array"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreListLiquidacion" : {
        "properties" : {
          "data" : {
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver",
            "items" : {
              "$ref" : "#/components/schemas/Liquidacion"
            },
            "type" : "array"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreListPedido" : {
        "properties" : {
          "data" : {
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver",
            "items" : {
              "$ref" : "#/components/schemas/Pedido"
            },
            "type" : "array"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreListPeriodoDeNomina" : {
        "properties" : {
          "data" : {
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver",
            "items" : {
              "$ref" : "#/components/schemas/PeriodoDeNomina"
            },
            "type" : "array"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreListProducto" : {
        "properties" : {
          "data" : {
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver",
            "items" : {
              "$ref" : "#/components/schemas/Producto"
            },
            "type" : "array"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreListUsuario" : {
        "properties" : {
          "data" : {
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver",
            "items" : {
              "$ref" : "#/components/schemas/Usuario"
            },
            "type" : "array"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreMovimiento" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Movimiento",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreNavegacion" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Navegacion",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobrePaginaDeBitacora" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/PaginaDeBitacora",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobrePaginaDeMovimientos" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/PaginaDeMovimientos",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobrePatrimonio" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Patrimonio",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobrePedido" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Pedido",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobrePeriodoDeNomina" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/PeriodoDeNomina",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreProductividad" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Productividad",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreProducto" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Producto",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreProlabore" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Prolabore",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreRetiro" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Retiro",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreSesion" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Sesion",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreSimulacionDeContratacion" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/SimulacionDeContratacion",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreUsuario" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Usuario",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreVersion" : {
        "properties" : {
          "data" : {
            "$ref" : "#/components/schemas/Version",
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "SobreVoid" : {
        "properties" : {
          "data" : {
            "description" : "Los datos de la operación. La clave viaja siempre; su valor es null cuando no hay nada que devolver"
          },
          "mensaje" : {
            "description" : "Texto en español para mostrarle tal cual a la persona del taller",
            "type" : "string"
          },
          "status" : {
            "description" : "Código de cinco dígitos: HTTP(3) + caso(2). Su significado está en x-prisma-codigos",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "mensaje", "status" ],
        "type" : "object"
      },
      "Usuario" : {
        "properties" : {
          "activo" : {
            "type" : "boolean"
          },
          "cargo" : {
            "description" : "El nombre del cargo. Ausente si no tiene",
            "type" : "string"
          },
          "cargoId" : {
            "description" : "Ausente si no tiene cargo",
            "format" : "uuid",
            "type" : "string"
          },
          "debeCambiarClave" : {
            "description" : "Entró o va a entrar con una contraseña temporal",
            "type" : "boolean"
          },
          "desactivacion" : {
            "$ref" : "#/components/schemas/Desactivacion",
            "description" : "Solo en una persona desactivada: la desactivación vigente. La historia completa está en la bitácora"
          },
          "id" : {
            "format" : "uuid",
            "type" : "string"
          },
          "nombreCompleto" : {
            "type" : "string"
          },
          "tipo" : {
            "enum" : [ "gerencia", "operacion" ],
            "type" : "string"
          },
          "ultimoAcceso" : {
            "description" : "Ausente si nunca ha entrado",
            "format" : "date-time",
            "type" : "string"
          },
          "usuario" : {
            "type" : "string"
          }
        },
        "required" : [ "activo", "debeCambiarClave", "id", "nombreCompleto", "tipo", "usuario" ],
        "type" : "object"
      },
      "VentaAdicional" : {
        "description" : "Cuánto hay que vender de un producto para cubrir el faltante. El margen de contribución no incluye el costo del tiempo: el tiempo es justo el gasto fijo que se trata de cubrir, y contarlo sería contarlo dos veces (RN-17, 06 §4.1)",
        "properties" : {
          "horas" : {
            "description" : "Cuánto trabajo piden esas unidades",
            "minimum" : 0,
            "type" : "number"
          },
          "margenContribucion" : {
            "description" : "Precio menos insumo y consumibles",
            "format" : "int64",
            "type" : "integer"
          },
          "producto" : {
            "description" : "El nombre del producto",
            "type" : "string"
          },
          "productoId" : {
            "format" : "uuid",
            "type" : "string"
          },
          "unidades" : {
            "description" : "Cuántas unidades más al mes",
            "format" : "int32",
            "type" : "integer"
          }
        },
        "required" : [ "horas", "margenContribucion", "producto", "productoId", "unidades" ],
        "type" : "object"
      },
      "Version" : {
        "properties" : {
          "ambiente" : {
            "description" : "dev, qa, uat o prod",
            "enum" : [ "dev", "qa", "uat", "prod" ],
            "type" : "string"
          },
          "api" : {
            "description" : "Versión de prisma_api, SemVer. Antes del go-live siempre 0.y.z",
            "type" : "string"
          },
          "esquema" : {
            "description" : "Versión del esquema de base de datos",
            "type" : "string"
          }
        },
        "required" : [ "ambiente", "api", "esquema" ],
        "type" : "object"
      }
    }
  },
  "info" : {
    "description" : "Toda respuesta llega con el sobre {status, mensaje, data}. El status es de cinco dígitos, HTTP(3) + caso(2), y su catálogo completo está en x-prisma-codigos. Con sesión, cada petición lleva Authorization: Bearer con el token y va firmada con X-Prisma-Nonce, X-Prisma-Timestamp y X-Prisma-Firma (ADR-021, 20 §6). Sin sesión solo responden POST /api/v0/consultas/version, POST /api/v0/sesiones, POST /api/v0/sesiones/renovacion y el formulario «acceso». Ninguna operación usa GET: toda lectura viaja por POST bajo /api/v0/consultas y lleva Idempotency-Key como cualquier otra petición (ADR-030).",
    "title" : "PRISMA · prisma_api",
    "version" : "0.15.0"
  },
  "openapi" : "3.1.0",
  "paths" : {
    "/api/v0/activos/{id}" : {
      "put" : {
        "description" : "Registra la compra de un equipo o una herramienta del negocio —una prensa, una bordadora— sin tocar la utilidad (RF-45, CU-15, BDD-15-1). Con cuenta, escribe en la misma transacción el activo y un movimiento de tipo inversion, que baja la caja de esa cuenta y no toca la utilidad ni el patrimonio (05 §6.1). Sin cuenta, escribe solo el activo: es como entran los equipos que el taller ya tenía el día del corte, cuya plata ya no está en ningún saldo (09 §4.1 y §4.3). El id va en la ruta por lo mismo que en movimientos (ADR-020): el mismo dos veces responde 40900. Solo Gerencia, y lo rechaza la base con activos_solo_gerencia (04 §7). El formulario es «activo»; la fecha futura y la cuenta que no sirve no caben en el descriptor y responden 42290 y 42291. Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40900, 40901, 40902, 42200 con data.errores, 42290 y 42291.",
        "operationId" : "registrarInversion",
        "parameters" : [ {
          "description" : "El id del activo, que genera quien lo registra",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevoActivo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreActivo"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Registrar una inversión en un activo",
        "tags" : [ "Capital" ]
      }
    },
    "/api/v0/adelantos/{id}" : {
      "put" : {
        "description" : "Entrega plata a cuenta del salario (RF-58, CU-26, BDD-26-1). Escribe en la misma transacción el adelanto y un movimiento de tipo adelanto_empleada, que baja la caja de esa cuenta, crea la cuenta por cobrar y no registra gasto: el gasto se reconoce entero en la liquidación, y registrarlo aquí contaría el mismo dinero dos veces (RN-11, 06 §6.3). El id va en la ruta por lo mismo que en movimientos (ADR-020): el mismo dos veces responde 40900. Solo Gerencia, y lo rechaza la base con adelantos_insercion. El formulario es «adelanto»; la fecha futura la rechaza el CHECK fecha_no_futura del movimiento con 42223, y la cuenta que no sirve, 42224. Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40900, 40901, 40902, 42200 con data.errores, 42223, 42224 y 42251.",
        "operationId" : "registrarAdelanto",
        "parameters" : [ {
          "description" : "El id del adelanto, que genera quien lo registra",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevoAdelanto"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreAdelanto"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Entregar un adelanto a una empleada",
        "tags" : [ "Nómina" ]
      }
    },
    "/api/v0/adelantos/{id}/anulacion" : {
      "post" : {
        "description" : "Anula el adelanto con motivo escrito y no borra nada (RN-13). Anula también el movimiento que lo registró, porque son el mismo hecho. Uno ya descontado en una liquidación no se puede anular y responde 40900: lo que haya que corregir después va por contra-asiento (04 §5.3). Solo Gerencia. El formulario es «anulacion-de-adelanto». Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901, 40902 y 42200 con data.errores.",
        "operationId" : "anularAdelanto",
        "parameters" : [ {
          "description" : "El id del adelanto",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/Motivo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreAdelanto"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Anular un adelanto",
        "tags" : [ "Nómina" ]
      }
    },
    "/api/v0/aportes/{id}" : {
      "put" : {
        "description" : "Registra plata que entra al negocio desde afuera (RF-46, CU-15): sube la caja y el patrimonio, y no la utilidad (05 §2). Escribe en una sola transacción el movimiento de tipo aporte y su fila de aportes_retiros con clase aporte. El mismo id dos veces responde 40900 (ADR-020). Solo Gerencia, y lo rechaza la base con ret_solo_gerencia (04 §7). El formulario es «aporte»; la fecha futura y la cuenta que no sirve no caben en el descriptor y responden 42290 y 42291. Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40900, 40901, 40902, 42200 con data.errores, 42290 y 42291.",
        "operationId" : "registrarAporte",
        "parameters" : [ {
          "description" : "El id del aporte, que genera quien lo registra",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevoAporte"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreAporte"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Registrar un aporte de capital",
        "tags" : [ "Capital" ]
      }
    },
    "/api/v0/bitacora/{id}/reversion" : {
      "post" : {
        "description" : "Deshace un cambio con un cambio nuevo y motivo escrito: la entrada original no se borra ni se edita, queda marcada como revertida, y responde la entrada nueva, de tipo «Reversión» (RF-89, CU-35). Revertir una reversión se puede. No se revierte una clave restablecida (40912), la creación del propio usuario (40913), lo que dejaría el sistema sin Gerencia activa (40910, RF-91) ni lo que sacaría del catálogo un cargo que tienen personas activas (40911). Una entrada ya revertida responde 40900. Solo Gerencia. El formulario es «reversion». Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901, 40902, 40910, 40911, 40912, 40913 y 42200 con data.errores.",
        "operationId" : "revertirCambio",
        "parameters" : [ {
          "description" : "El id de la entrada que se revierte",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "int64",
            "type" : "integer"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/Motivo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreEntradaDeBitacora"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Revertir un cambio",
        "tags" : [ "Bitácora" ]
      }
    },
    "/api/v0/cargos" : {
      "post" : {
        "description" : "Agrega un cargo al final del catálogo (RF-81, CU-33). El nombre no se repite, sin distinguir mayúsculas: 42214. El cargo describe el oficio y no da permisos; eso lo decide el tipo. Solo Gerencia, y lo rechaza la base (04 §7). El formulario es «cargo». Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40901, 40902, 42200 y 42214; los dos últimos con data.errores.",
        "operationId" : "crearCargo",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/DatosDeCargo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreCargo"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Crear un cargo",
        "tags" : [ "Cargos" ]
      }
    },
    "/api/v0/cargos/orden" : {
      "put" : {
        "description" : "Recibe los ids de todos los cargos del catálogo en el orden nuevo y responde el catálogo reordenado (RF-81). Si la lista no trae exactamente los cargos que hay —porque alguien agregó uno mientras tanto— responde 40900 y no cambia nada. Solo Gerencia. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40900, 40901 y 40902.",
        "operationId" : "reordenarCargos",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/OrdenDeCargos"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreListCargo"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Reordenar el catálogo de cargos",
        "tags" : [ "Cargos" ]
      }
    },
    "/api/v0/cargos/{id}" : {
      "put" : {
        "description" : "Cambia el nombre y la descripción (RF-81). Quien tiene el cargo lo sigue teniendo con el nombre nuevo, y no se le abre ni se le cierra ninguna pantalla. Solo Gerencia. El formulario es «cargo». Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40901, 40902, 42200 y 42214; los dos últimos con data.errores.",
        "operationId" : "renombrarCargo",
        "parameters" : [ {
          "description" : "El id del cargo",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/DatosDeCargo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreCargo"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Renombrar un cargo",
        "tags" : [ "Cargos" ]
      }
    },
    "/api/v0/cargos/{id}/desactivacion" : {
      "post" : {
        "description" : "Lo saca del catálogo con motivo escrito: deja de ofrecerse al crear usuarios y quien lo tuvo lo conserva (RF-81, BDD-33-1). Un cargo que todavía tienen personas activas no se desactiva: 40911. Uno ya inactivo responde 40900. Solo Gerencia. El formulario es «desactivacion-de-cargo». Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901, 40902, 40911 y 42200 con data.errores.",
        "operationId" : "desactivarCargo",
        "parameters" : [ {
          "description" : "El id del cargo",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/Motivo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreCargo"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Desactivar un cargo",
        "tags" : [ "Cargos" ]
      }
    },
    "/api/v0/cargos/{id}/reactivacion" : {
      "post" : {
        "description" : "Lo devuelve al catálogo para volver a asignarlo, como el botón «Reactivar» del mockup, y deja la entrada «Cargo reactivado» en la bitácora. No pide motivo, igual que en el mockup. Uno activo responde 40900. Solo Gerencia. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901 y 40902.",
        "operationId" : "reactivarCargo",
        "parameters" : [ {
          "description" : "El id del cargo",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreCargo"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Reactivar un cargo",
        "tags" : [ "Cargos" ]
      }
    },
    "/api/v0/categorias" : {
      "post" : {
        "description" : "Crea una categoría, principal o dentro de otra de la misma naturaleza (RF-17). Solo Gerencia. Solo un gasto puede ser fijo: es lo que la caja libre descuenta cada mes (05 §3.3). El formulario es «categoria». Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40901, 40902, 42200, 42220, 42221 y 42222; los tres últimos, también con data.errores.",
        "operationId" : "crearCategoria",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevaCategoria"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreCategoria"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Crear una categoría",
        "tags" : [ "Categorías" ]
      }
    },
    "/api/v0/clientes/{id}" : {
      "put" : {
        "description" : "Registra un cliente con nombre, contacto y notas (RF-18, CU-05). El id va en la ruta y no en el cuerpo: lo genera quien pide al decidir la acción, junto con la clave de idempotencia (ADR-020), así que reintentar la misma petición no crea dos clientes. Si ese id ya existe responde 40900. Solo el nombre es obligatorio: en el taller un cliente entra por teléfono y el correo llega después. El formulario es «cliente». Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40302, 40900, 40901, 40902 y 42200 con data.errores.",
        "operationId" : "crearCliente",
        "parameters" : [ {
          "description" : "El id del cliente, que genera quien pide",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevoCliente"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreCliente"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Crear un cliente",
        "tags" : [ "Clientes" ]
      }
    },
    "/api/v0/clientes/{id}/anulacion" : {
      "post" : {
        "description" : "Saca al cliente del catálogo con motivo escrito y no borra nada (RN-13): la fila guarda cuándo, quién y por qué, y sus pedidos siguen enteros. Solo Gerencia. Un cliente ya anulado responde 40900. El formulario es «anulacion-de-cliente». Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901, 40902 y 42200 con data.errores.",
        "operationId" : "anularCliente",
        "parameters" : [ {
          "description" : "El id del cliente",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/Motivo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreCliente"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Anular un cliente",
        "tags" : [ "Clientes" ]
      }
    },
    "/api/v0/consultas/activos" : {
      "post" : {
        "description" : "Los equipos y las herramientas del negocio sin anular, del de mayor valor de compra al de menor, que es el orden de la tabla de 10 §4.5 (RF-45). A una sesión de Operación la base no le devuelve ninguna fila, por activos_solo_gerencia (P-20), así que le llega la lista vacía y no un 40300. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40901 y 40902.",
        "operationId" : "activos",
        "parameters" : [ {
          "description" : "UUID v4 nuevo en cada consulta; solo el reintento de esa misma consulta lo reutiliza (ADR-020, ADR-030). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreListActivo"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Los activos del negocio",
        "tags" : [ "Capital" ]
      }
    },
    "/api/v0/consultas/adelantos" : {
      "post" : {
        "description" : "La cuenta por cobrar viva: los adelantos sin descontar, con su antigüedad en días (06 §7). Sin filtros, solo los pendientes. Lo que vuelve lo decide PostgreSQL: adelantos lleva RLS y la política adelantos_lectura le devuelve a cada persona los suyos —los que se le van a descontar— y a Gerencia, todos. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40901 y 40902.",
        "operationId" : "adelantos",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ConsultaDeAdelantos"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreListAdelanto"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Los adelantos pendientes de descontar",
        "tags" : [ "Nómina" ]
      }
    },
    "/api/v0/consultas/anticipo-minimo" : {
      "post" : {
        "description" : "El validador que protege cada pedido (RF-38, RF-39, CU-12, BDD-12-1, BDD-12-2, 05 §5). Calcula el costo directo de las líneas al costo vigente de cada producto, lo divide entre el valor total y compara contra el anticipo que se piensa cobrar; si no alcanza, devuelve el faltante en pesos y el porcentaje sugerido, redondeado hacia arriba a múltiplo de 5 con 5 puntos de holgura. Es una consulta y no un campo de otra respuesta porque la piden dos pantallas —el formulario de pedido y el cotizador— y se recalcula con cada línea, antes de que exista nada que guardar. Un producto de las líneas sin costo registrado responde 40982: sin costo la cifra mentiría. Lo consultan los dos tipos de usuario, pero a Operación costos_producto no le devuelve nada y por eso también responde 40982 (ADR-006). Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40901, 40902, 40982 y 42200 con data.errores.",
        "operationId" : "anticipoMinimo",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ConsultaDeAnticipoMinimo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreAnticipoMinimo"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Si el anticipo cubre el costo directo",
        "tags" : [ "Cotizaciones" ]
      }
    },
    "/api/v0/consultas/bitacora" : {
      "post" : {
        "description" : "Los cambios de administración sobre usuarios y cargos, los más recientes primero, con cuándo, quién, sobre quién, qué pasó, el detalle y el motivo (RF-88, 10 §5.5). No trae los inicios de sesión. Cada entrada dice si hoy se puede revertir y, en palabras, qué pasaría o por qué no. Con limite trae solo las más recientes, y total dice cuántas hay. Es de Gerencia, y la base no le muestra la auditoría a nadie más (04 §7). Puede devolver 20000, 40100, 40101, 40102, 40103 y 40302.",
        "operationId" : "bitacora",
        "parameters" : [ {
          "description" : "UUID v4 nuevo en cada consulta; solo el reintento de esa misma consulta lo reutiliza (ADR-020, ADR-030). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ConsultaDeBitacora"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobrePaginaDeBitacora"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "La bitácora de usuarios y cargos",
        "tags" : [ "Bitácora" ]
      }
    },
    "/api/v0/consultas/cargos" : {
      "post" : {
        "description" : "Todos los cargos, activos e inactivos, en su orden, con cuántas personas activas tiene cada uno y, si está inactivo, cuándo, quién y por qué (RF-81, 10 §5.4). Es el catálogo de la pantalla de Gestión de usuarios: solo Gerencia. Puede devolver 20000, 40100, 40101, 40102, 40103, 40300 y 40302.",
        "operationId" : "cargos",
        "parameters" : [ {
          "description" : "UUID v4 nuevo en cada consulta; solo el reintento de esa misma consulta lo reutiliza (ADR-020, ADR-030). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreListCargo"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "El catálogo de cargos",
        "tags" : [ "Cargos" ]
      }
    },
    "/api/v0/consultas/cargos-asignables" : {
      "post" : {
        "description" : "Solo los activos, en su orden, con id y nombre: es el origen de la lista cargoId de los formularios «usuario» y «edicion-de-usuario». Un cargo inactivo no se reparte más, y quien ya lo tenía lo conserva (BDD-33-1). Solo Gerencia. Puede devolver 20000, 40100, 40101, 40102, 40103, 40300 y 40302.",
        "operationId" : "cargosAsignables",
        "parameters" : [ {
          "description" : "UUID v4 nuevo en cada consulta; solo el reintento de esa misma consulta lo reutiliza (ADR-020, ADR-030). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreListCargoAsignable"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Los cargos que se pueden asignar",
        "tags" : [ "Cargos" ]
      }
    },
    "/api/v0/consultas/categorias" : {
      "post" : {
        "description" : "El árbol de categorías de ingreso y gasto sin anular (RF-17), en el orden en que se pintan: cada una dice su naturaleza, si es un gasto fijo y, si tiene, su categoría madre. Las piden los dos tipos de usuario, porque los dos registran movimientos. Puede devolver 20000, 40100, 40101, 40102, 40103 y 40302.",
        "operationId" : "categorias",
        "parameters" : [ {
          "description" : "UUID v4 nuevo en cada consulta; solo el reintento de esa misma consulta lo reutiliza (ADR-020, ADR-030). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreListCategoria"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Las categorías de ingreso y gasto",
        "tags" : [ "Categorías" ]
      }
    },
    "/api/v0/consultas/clientes" : {
      "post" : {
        "description" : "Los clientes que todavía valen, ordenados por nombre; los anulados no salen. Es el origen del campo clienteId del formulario «pedido», así que quien registra un pedido elige de esta lista y no escribe el nombre a mano. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40901 y 40902.",
        "operationId" : "clientes",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreListCliente"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Los clientes del taller",
        "tags" : [ "Clientes" ]
      }
    },
    "/api/v0/consultas/costeo" : {
      "post" : {
        "description" : "La vista previa que el formulario de producto pinta en vivo: el costo unitario, los tres márgenes, la lectura frente al resto del taller y el precio que habría que cobrar para el margen objetivo (RF-30, RF-31, RF-33). Las cuatro son plata o son reglas, así que las calcula la API y el front solo las pinta (ADR-018). No escribe nada: sirve para ver antes de guardar, y es también la sugerencia de precio de la tarea 5.7. Con productoId, ese producto queda fuera de la mediana con la que se compara. Solo Gerencia. Sin pro-labore definido responde 40940, y un tiempo con más de dos decimales, 42240. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40901, 40902, 40940, 42200 con data.errores y 42240.",
        "operationId" : "costeo",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ConsultaDeCosteo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreCosteoCalculado"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Costo, márgenes y precio sugerido",
        "tags" : [ "Productos" ]
      }
    },
    "/api/v0/consultas/cotizaciones" : {
      "post" : {
        "description" : "Las cotizaciones de más reciente a más antigua, con filtros por estado, cliente y fechas. Sin filtros, las vigentes. Como la de pedidos, no pagina: los filtros de fecha ya acotan lo que vuelve. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40901 y 40902.",
        "operationId" : "cotizaciones",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ConsultaDeCotizaciones"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreListCotizacion"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Las cotizaciones por fecha",
        "tags" : [ "Cotizaciones" ]
      }
    },
    "/api/v0/consultas/cuentas" : {
      "post" : {
        "description" : "Las cuentas de dinero sin anular, en el orden en que se pintan. Las piden los dos tipos de usuario: Operación elige por nombre la cuenta de un movimiento. Por eso no traen saldo: la caja es de Gerencia (01 §4) y los saldos tienen su propia consulta (tarea 3.12). Puede devolver 20000, 40100, 40101, 40102, 40103 y 40302.",
        "operationId" : "cuentas",
        "parameters" : [ {
          "description" : "UUID v4 nuevo en cada consulta; solo el reintento de esa misma consulta lo reutiliza (ADR-020, ADR-030). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreListCuenta"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Las cuentas de dinero, sin saldos",
        "tags" : [ "Cuentas" ]
      }
    },
    "/api/v0/consultas/desprendible" : {
      "post" : {
        "description" : "El desprendible de una liquidación, en PDF con el logo del negocio: encabezado, identificación, devengados con el detalle de las horas extra, descuentos con la fecha de cada adelanto, el neto en números y en letras, y el pie con la cuenta de origen (RF-57, 06 §8). Baja dentro del sobre, en data, como un Documento en base64 (tarea 8.11). Operación ve únicamente el suyo (RF-63, CU-20): la política nom_lectura no le devuelve la fila de otra persona, así que para la API esa liquidación no existe y responde 40400. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40400, 40901 y 40902.",
        "operationId" : "desprendible",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ConsultaDeDesprendible"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreDocumento"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "El desprendible de pago en PDF",
        "tags" : [ "Nómina" ]
      }
    },
    "/api/v0/consultas/division-de-retiro" : {
      "post" : {
        "description" : "La propuesta que el formulario de retiro pinta antes de guardar (CU-16, paso 3): cuánto del retiro sería pro-labore y cuánto distribución, con una explicación en palabras. Pro-labore hasta completar el del mes —el vigente en la fecha del retiro, menos lo que ya se retiró como pro-labore ese mes— y el resto distribución; sin pro-labore definido, todo es distribución. No escribe nada, y es lo mismo que hace PUT /api/v0/retiros/{id} cuando no se le dice cuánto es pro-labore. Solo Gerencia: sin la fila del pro-labore, que la base no le devuelve a Operación, la propuesta saldría equivocada. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40901, 40902, 42200 con data.errores y 42290.",
        "operationId" : "divisionDeRetiro",
        "parameters" : [ {
          "description" : "UUID v4 nuevo en cada consulta; solo el reintento de esa misma consulta lo reutiliza (ADR-020, ADR-030). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ConsultaDeDivisionDeRetiro"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreDivisionDeRetiro"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Cómo se parte un retiro",
        "tags" : [ "Capital" ]
      }
    },
    "/api/v0/consultas/empleados" : {
      "post" : {
        "description" : "Las fichas de nómina, por nombre. Lo que vuelve lo decide PostgreSQL y no la API: empleados lleva RLS y la política empleados_lectura le devuelve a cada persona su propia ficha y nada más; a Gerencia, todas (04 §7). Por eso una sesión de Operación recibe una lista de uno, o vacía si no está en nómina, y no un 40300 que la base nunca produce. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40901 y 40902.",
        "operationId" : "empleados",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ConsultaDeEmpleadas"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreListEmpleada"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Las empleadas",
        "tags" : [ "Nómina" ]
      }
    },
    "/api/v0/consultas/formularios" : {
      "post" : {
        "description" : "Qué campos tiene, en qué orden, y las reglas de cada uno como datos: obligatorio, mínimo, máximo y el texto que se muestra cuando no se cumplen. Ese texto es el mismo que devuelve data.errores cuando la regla falla en el servidor. Sin sesión solo responde el formulario «acceso»; los demás piden sesión y firma. Puede devolver 20000, 40100, 40101, 40102, 40103 y 40400.",
        "operationId" : "descriptor",
        "parameters" : [ {
          "description" : "UUID v4 nuevo en cada consulta; solo el reintento de esa misma consulta lo reutiliza (ADR-020, ADR-030). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101. Obligatoria con sesión; el formulario «acceso» se pide sin sesión y sin ella",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : false,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103. Obligatoria con sesión; el formulario «acceso» se pide sin sesión y sin ella",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : false,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102. Obligatoria con sesión; el formulario «acceso» se pide sin sesión y sin ella",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : false,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ConsultaDeFormulario"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreDescriptorDeFormulario"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "El descriptor de un formulario",
        "tags" : [ "Formularios" ]
      }
    },
    "/api/v0/consultas/importacion" : {
      "post" : {
        "description" : "Lee el archivo y no escribe ni una fila (RF-64, RF-65, CU-21, BDD-21-1, BDD-21-2). Sin mapeo devuelve las columnas que trae el archivo y las que el importador necesita, para que la persona las relacione; con mapeo devuelve además el informe fila por fila, con las válidas, las que no sirven y por qué, y las que coinciden con un movimiento que ya existe (BDD-21-3). El cuerpo es multipart/form-data con dos partes, «archivo» y «mapeo», y el archivo pasa por la API igual que el adjunto (07 §1). Solo Gerencia. Más de 5 MB responde 40070 y un archivo que no es CSV, 40071. Puede devolver 20000, 40002, 40070, 40071, 40100, 40101, 40102, 40103, 40300, 40302, 40901, 40902 y 42270.",
        "operationId" : "analizarImportacion",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "multipart/form-data" : {
              "encoding" : {
                "mapeo" : {
                  "contentType" : "application/json"
                }
              },
              "schema" : {
                "properties" : {
                  "archivo" : {
                    "description" : "El CSV del histórico, de 5 MB como máximo",
                    "format" : "binary",
                    "type" : "string"
                  },
                  "mapeo" : {
                    "additionalProperties" : {
                      "type" : "string"
                    },
                    "description" : "Qué columna del archivo es cada campo del movimiento: {\"clave del campo\": \"nombre de la columna\"}. Las claves salen de columnasEsperadas",
                    "type" : "object"
                  }
                },
                "required" : [ "archivo" ],
                "type" : "object"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreInformeDeImportacion"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Leer un CSV sin escribir nada",
        "tags" : [ "Importación" ]
      }
    },
    "/api/v0/consultas/movimientos" : {
      "post" : {
        "description" : "El libro único con sus filtros: rango de fechas del movimiento, tipo, categoría y cuenta, lo más reciente primero (RF-08 a RF-13, CU-13). Cada movimiento trae el nombre de su cuenta y de su categoría, y si llegó tarde: las tres las resuelve la API, porque el front no consulta de más ni cuenta días (ADR-018). Los anulados no salen salvo que se pidan, y pedirlos es solo de Gerencia (04 §5.5). Consultan los dos tipos de usuario. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40901 y 40902 y 42200 con data.errores.",
        "operationId" : "movimientos",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ConsultaDeMovimientos"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobrePaginaDeMovimientos"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "El libro de movimientos",
        "tags" : [ "Movimientos" ]
      }
    },
    "/api/v0/consultas/navegacion" : {
      "post" : {
        "description" : "Las secciones del menú lateral, en el orden en que se pintan, y las dos entradas del menú de la sesión que dependen del tipo (RF-103, 10 §2.1 y §5.3). El front pinta lo que llega y no decide qué ocultar: una sección que no viene no existe. Con vista=operacion responde lo que vería Operación y el aviso fijo de la vista previa (RF-92 a RF-94); solo la pide Gerencia, y el tipo real de la sesión no cambia. Puede devolver 20000, 40100, 40101, 40102, 40103, 40300 y 40302.",
        "operationId" : "navegacion",
        "parameters" : [ {
          "description" : "UUID v4 nuevo en cada consulta; solo el reintento de esa misma consulta lo reutiliza (ADR-020, ADR-030). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ConsultaDeNavegacion"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreNavegacion"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Lo que la sesión puede ver",
        "tags" : [ "Navegación" ]
      }
    },
    "/api/v0/consultas/nomina" : {
      "post" : {
        "description" : "Las liquidaciones del período, por nombre. Lo que vuelve lo decide PostgreSQL y no la API: nomina_detalle lleva RLS y la política nom_lectura le devuelve a cada persona solo la suya, que es el RF-63 impuesto desde la base y no desde la pantalla (ADR-006, CU-20). Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40901 y 40902.",
        "operationId" : "nomina",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ConsultaDeNomina"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreListLiquidacion"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Las liquidaciones de un período",
        "tags" : [ "Nómina" ]
      }
    },
    "/api/v0/consultas/patrimonio" : {
      "post" : {
        "description" : "Lo que se ha construido y lo que se ha sacado, como lo pinta 10 §4.5 (RF-48, RF-49, CU-16, CU-24): el patrimonio de 05 §6.4 —aportes menos distribuciones más utilidades acumuladas— con sus tres términos; las distribuciones y las utilidades de los últimos 12 meses, con la tasa de retiro que sale de ellas (05 §9.3); la lectura que la acompaña, que en nivel critica es la alerta de descapitalización; y cómo se partió el retiro del mes en curso. Todo lo calcula la API (ADR-018). Solo Gerencia (01 §4), pero esta vez no lo impide la base: las cifras salen del libro, que leen los dos tipos, y es la misma decisión pendiente que los saldos por cuenta. Hasta que se tome, lo rechaza la API con 40300. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40901 y 40902.",
        "operationId" : "patrimonio",
        "parameters" : [ {
          "description" : "UUID v4 nuevo en cada consulta; solo el reintento de esa misma consulta lo reutiliza (ADR-020, ADR-030). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobrePatrimonio"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "El patrimonio y la tasa de retiro",
        "tags" : [ "Capital" ]
      }
    },
    "/api/v0/consultas/pedidos" : {
      "post" : {
        "description" : "Los pedidos ordenados por fecha, los más recientes primero, con filtros por estado, cliente y rango de fechas (RF-20, CU-08). Cada pedido trae lo anticipado, el saldo pendiente y si está estancado: las tres las calcula la API (RF-23, RF-24), porque el front no calcula plata ni decide qué resaltar (ADR-018). Los anulados no salen salvo que se pidan. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40901 y 40902 y 42200 con data.errores.",
        "operationId" : "pedidos",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ConsultaDePedidos"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreListPedido"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Los pedidos por fecha",
        "tags" : [ "Pedidos" ]
      }
    },
    "/api/v0/consultas/periodos-de-nomina" : {
      "post" : {
        "description" : "Los meses de nómina, del más reciente al más antiguo, con cuántas liquidaciones lleva cada uno. Es de donde el formulario «liquidacion» saca su lista de meses. Sin filtros, los que siguen abiertos. Lo que vuelve lo decide PostgreSQL: nomina_periodos lleva RLS y la política nom_per_lectura le devuelve a cada persona solo los períodos en los que tiene desprendible propio; a Gerencia, todos (04 §7). Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40901 y 40902.",
        "operationId" : "periodosDeNomina",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ConsultaDePeriodosDeNomina"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreListPeriodoDeNomina"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Los meses de nómina",
        "tags" : [ "Nómina" ]
      }
    },
    "/api/v0/consultas/productividad" : {
      "post" : {
        "description" : "Cuánto tiempo pagado no se convirtió en ventas (RF-62, CU-27, BDD-27-1, 06 §9). Suma las horas de nómina y las del pro-labore, les resta las horas cargadas a lo entregado en el mes según el costeo, y pone precio a la diferencia. Es el contrapeso del simulador: antes de agregar horas pagadas conviene vender las que ya se pagan. Sin pro-labore definido responde 40950, por la misma razón que el simulador. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40901, 40902 y 40950.",
        "operationId" : "productividad",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ConsultaDeProductividad"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreProductividad"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Horas pagadas contra horas facturadas",
        "tags" : [ "Nómina" ]
      }
    },
    "/api/v0/consultas/productos" : {
      "post" : {
        "description" : "El catálogo con su costeo vigente, sus tres márgenes y lo que dicen frente al resto del taller, en el orden que se pida (RF-28, RF-30, RF-31, CU-09). Es el cuadro de 10 §4.4, y quien lo ordena y quien lo lee es la API: el front no vuelve a ordenar ni decide qué pinta en rojo (ADR-018). A una sesión de Operación le llegan el nombre, el tipo, la unidad y el precio, y ni el costeo ni los márgenes ni la lectura, porque costos_producto lleva RLS y PostgreSQL no le devuelve esas filas (ADR-006, RF-34, BDD-02-2); por eso a Operación el catálogo le llega por nombre. Los desactivados no salen salvo que se pidan. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40901, 40902 y 42200 con data.errores.",
        "operationId" : "productos",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ConsultaDeProductos"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreListProducto"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "El cuadro de productos y servicios",
        "tags" : [ "Productos" ]
      }
    },
    "/api/v0/consultas/prolabore" : {
      "post" : {
        "description" : "La definición del pro-labore que rige hoy, con la tarifa por hora que de ella toman los costeos (CU-25, 05 §7.1). Sin ninguna definida, data llega en null: es lo que bloquea el simulador de capacidad de pago (BDD-18-2) y el costeo (40940). El historial se conserva en la base y no se lee: ninguna pantalla lo pinta. A una sesión de Operación la base no le devuelve la fila, por prolabore_solo_gerencia (P-21), así que le llega lo mismo que si no hubiera pro-labore. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40901 y 40902.",
        "operationId" : "prolabore",
        "parameters" : [ {
          "description" : "UUID v4 nuevo en cada consulta; solo el reintento de esa misma consulta lo reutiliza (ADR-020, ADR-030). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreProlabore"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "El pro-labore vigente",
        "tags" : [ "Capital" ]
      }
    },
    "/api/v0/consultas/simulacion-de-contratacion" : {
      "post" : {
        "description" : "Responde con números si el negocio puede pagarle a alguien (RF-59 a RF-61, CU-18, BDD-18-1 a BDD-18-4). Parte de la utilidad operativa promedio de los últimos meses, obligatoriamente con el pro-labore ya descontado como gasto: usar la utilidad que no cuenta el trabajo propio es el error que lleva a contratar y quebrar (RN-09, 06 §3.2). Le resta la reserva de seguridad, compara contra el salario evaluado y, si falta, traduce el faltante a unidades de cada producto con el margen de contribución que excluye el costo del tiempo (RN-17, 06 §4.1) y propone las alternativas del 06 §5. Sin pro-labore definido no hay respuesta y se bloquea con 40950 (BDD-18-2). A Operación le responde lo mismo, y no un 40300: prolabore_config lleva RLS y le devuelve cero filas, así que desde la API los dos casos son indistinguibles y la API no vuelve a decidir un permiso que ya decide la base (ADR-006). El formulario es «simulacion». Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40901, 40902, 40950 y 42200 con data.errores.",
        "operationId" : "simulacionDeContratacion",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/Simulacion"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreSimulacionDeContratacion"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Simular si se puede contratar, y por cuánto",
        "tags" : [ "Nómina" ]
      }
    },
    "/api/v0/consultas/sobres" : {
      "post" : {
        "description" : "Los porcentajes que rigen hoy (RF-50, 05 §11). El historial se conserva en la base (RF-51) y no se lee: ninguna pantalla lo pinta. Lo asignado y lo usado de cada sobre es del panel del Inicio, que tiene su propio contrato (tareas 7.8 y 6.10). A una sesión de Operación la base no le devuelve la fila, por sobres_solo_gerencia (P-22), así que le llega data en null. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40901 y 40902.",
        "operationId" : "sobres",
        "parameters" : [ {
          "description" : "UUID v4 nuevo en cada consulta; solo el reintento de esa misma consulta lo reutiliza (ADR-020, ADR-030). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreConfiguracionDeSobres"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Los cuatro sobres vigentes",
        "tags" : [ "Capital" ]
      }
    },
    "/api/v0/consultas/usuarios" : {
      "post" : {
        "description" : "La tabla de Gestión de usuarios (RF-84, RF-87): activas primero y desactivadas al final, cada grupo en el orden en que se crearon, con el estado, la desactivación vigente y el último acceso en cada fila. Es de Gerencia: a una sesión de Operación, Row Level Security solo le deja ver su propia fila (04 §7, BDD-02-6). Puede devolver 20000, 40100, 40101, 40102, 40103 y 40302.",
        "operationId" : "usuarios",
        "parameters" : [ {
          "description" : "UUID v4 nuevo en cada consulta; solo el reintento de esa misma consulta lo reutiliza (ADR-020, ADR-030). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreListUsuario"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Las personas con acceso",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v0/consultas/version" : {
      "post" : {
        "description" : "No lleva sesión ni firma: el front lo consulta al arrancar, antes de que nadie entre, y si el MAJOR de la API no es el que espera se planta.",
        "operationId" : "version",
        "parameters" : [ {
          "description" : "UUID v4 nuevo en cada consulta; solo el reintento de esa misma consulta lo reutiliza (ADR-020, ADR-030). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreVersion"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Versión de la API, del esquema y ambiente",
        "tags" : [ "Versión" ]
      }
    },
    "/api/v0/cotizaciones/{id}" : {
      "put" : {
        "description" : "Emite lo que se le pasa al cliente antes de que haya pedido (CU-11). Sus líneas son las mismas de un pedido y comparten esquema. El valor total lo calcula la API de las líneas: el front no calcula plata (ADR-018). El id va en la ruta por lo mismo que en movimientos (ADR-020): el mismo dos veces responde 40900. La emiten los dos tipos de usuario, como el pedido. El formulario es «cotizacion»; sin líneas responde 42280 y una validez anterior a la emisión, 42281. Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40302, 40900, 40901, 40902, 42200 con data.errores, 42232, 42280 y 42281.",
        "operationId" : "emitirCotizacion",
        "parameters" : [ {
          "description" : "El id de la cotización, que genera quien la emite",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevaCotizacion"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreCotizacion"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Emitir una cotización",
        "tags" : [ "Cotizaciones" ]
      }
    },
    "/api/v0/cotizaciones/{id}/aceptacion" : {
      "post" : {
        "description" : "El cliente la aceptó: crea el pedido con las mismas líneas, el mismo cliente y el mismo anticipo, sin volver a digitar nada (RF-40, CU-05). Devuelve el pedido creado, en estado en_proceso, y deja la cotización en aceptada con su pedidoId. Los precios son los de la cotización y no los del catálogo de hoy: es el precio que se le sostuvo al cliente. No tiene formulario: no hay nada que escribir. Una ya aceptada responde 40980, y una anulada o vencida, 40981. La aceptan los dos tipos de usuario, como el pedido. Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40302, 40400, 40901, 40902, 40980 y 40981.",
        "operationId" : "aceptarCotizacion",
        "parameters" : [ {
          "description" : "El id de la cotización",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobrePedido"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Convertir la cotización en pedido",
        "tags" : [ "Cotizaciones" ]
      }
    },
    "/api/v0/cotizaciones/{id}/anulacion" : {
      "post" : {
        "description" : "Anula la cotización con motivo escrito y no borra nada (RN-13). Una ya aceptada no se anula —lo que hay que corregir es el pedido— y responde 40980; una ya anulada, 40900. Solo Gerencia. El formulario es «anulacion-de-cotizacion». Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901, 40902, 40980 y 42200 con data.errores.",
        "operationId" : "anularCotizacion",
        "parameters" : [ {
          "description" : "El id de la cotización",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/Motivo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreCotizacion"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Anular una cotización",
        "tags" : [ "Cotizaciones" ]
      }
    },
    "/api/v0/cotizaciones/{id}/documento" : {
      "post" : {
        "description" : "El PDF con el logo del negocio, listo para pasarlo por WhatsApp (RF-36, CU-11). Baja dentro del sobre, en data, como un Documento en base64 (tarea 8.11). No lleva costos ni márgenes: es el papel del cliente. Una cotización anulada responde 40981. Lo generan los dos tipos de usuario. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40400, 40901, 40902 y 40981.",
        "operationId" : "cotizacionEnPdf",
        "parameters" : [ {
          "description" : "El id de la cotización",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreDocumento"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "La cotización en PDF con el logo",
        "tags" : [ "Cotizaciones" ]
      }
    },
    "/api/v0/cuentas" : {
      "post" : {
        "description" : "Crea una cuenta de dinero: efectivo, Nequi, Daviplata, un banco (RF-06). Solo Gerencia, y se crea desde Movimientos (RF-97, 10 §4.3). El saldo que se escribe entra como saldo inicial de la cuenta, no como ingreso, así que no afecta la utilidad del mes. El formulario es «cuenta». Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40901, 40902 y 42200 con data.errores.",
        "operationId" : "crearCuenta",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevaCuenta"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreCuenta"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Crear una cuenta de dinero",
        "tags" : [ "Cuentas" ]
      }
    },
    "/api/v0/empleados/{id}" : {
      "put" : {
        "description" : "Da de alta a quien entra a nómina, con su salario acordado y su fecha de ingreso (RF-54, CU-19). No crea un usuario: se puede estar en nómina sin entrar al sistema, y por eso son dos tablas (04 §4.2). El id va en la ruta por lo mismo que en movimientos (ADR-020): el mismo dos veces responde 40900. Solo Gerencia, y lo rechaza la base con empleados_insercion (04 §7). El formulario es «empleada»; un usuario que ya tiene ficha responde 42253, que es empleados_usuario_id_key. Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40900, 40901, 40902, 42200 con data.errores y 42253.",
        "operationId" : "registrarEmpleada",
        "parameters" : [ {
          "description" : "El id de la empleada, que genera quien la registra",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevaEmpleada"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreEmpleada"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Dar de alta a una empleada",
        "tags" : [ "Nómina" ]
      }
    },
    "/api/v0/empleados/{id}/anulacion" : {
      "post" : {
        "description" : "Anula la ficha con motivo escrito y no borra nada (RN-13): guarda cuándo, quién y por qué. No es retirar: se anula lo que no debió registrarse, y se retira a quien trabajó y dejó de hacerlo. Solo Gerencia. Una ficha ya anulada responde 40900. El formulario es «anulacion-de-empleada». Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901, 40902 y 42200 con data.errores.",
        "operationId" : "anularEmpleada",
        "parameters" : [ {
          "description" : "El id de la empleada",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/Motivo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreEmpleada"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Anular la ficha de una empleada",
        "tags" : [ "Nómina" ]
      }
    },
    "/api/v0/empleados/{id}/edicion" : {
      "post" : {
        "description" : "Cambia el nombre, el documento, el salario acordado, las horas pactadas o el usuario al que se conecta. La fecha de ingreso no se edita: moverla cambiaría de mes liquidaciones ya hechas. El aumento se escribe encima —empleados no guarda historial de salario, a diferencia de costos_producto— y las liquidaciones anteriores conservan el suyo en nomina_detalle.salario_base. Solo Gerencia, y lo rechaza la base con empleados_actualizacion. El formulario es «edicion-de-empleada». Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40901, 40902, 42200 con data.errores y 42253.",
        "operationId" : "editarEmpleada",
        "parameters" : [ {
          "description" : "El id de la empleada",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/EdicionDeEmpleada"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreEmpleada"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Editar la ficha de una empleada",
        "tags" : [ "Nómina" ]
      }
    },
    "/api/v0/empleados/{id}/retiro" : {
      "post" : {
        "description" : "Pone la fecha de retiro: dejó de estar en nómina y no entra en las liquidaciones de los meses siguientes. No anula nada —sus liquidaciones y sus adelantos siguen ahí— y no es una liquidación definitiva de contrato, que este módulo no calcula (06 §1). Solo Gerencia. El formulario es «retiro-de-empleada»; una fecha anterior a la de ingreso responde 42250, y una ya retirada, 40900. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901, 40902, 42200 con data.errores y 42250.",
        "operationId" : "retirarEmpleada",
        "parameters" : [ {
          "description" : "El id de la empleada",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RetiroDeEmpleada"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreEmpleada"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Retirar a una empleada de la nómina",
        "tags" : [ "Nómina" ]
      }
    },
    "/api/v0/importaciones/{id}" : {
      "put" : {
        "description" : "Escribe los movimientos de las filas válidas y devuelve el mismo informe con lo que entró (RF-64, RF-65, CU-21). No hay estado guardado entre el análisis y esta llamada: el archivo y el mapeo vuelven a viajar, y por eso no hace falta una tabla de importaciones (tarea 8.11). El id va en la ruta por lo mismo que en movimientos (ADR-020): el mismo archivo confirmado dos veces responde 40900, que es lo que impide cargarlo repetido. Un archivo con filas que coinciden con movimientos que ya existen responde 40970 y no escribe nada, hasta que se confirme con aceptarDuplicados (BDD-21-3). Solo Gerencia. Si no queda ni una fila válida responde 42271. Puede devolver 20100, 40002, 40070, 40071, 40100, 40101, 40102, 40103, 40300, 40302, 40900, 40901, 40902, 40970, 42270 y 42271.",
        "operationId" : "confirmarImportacion",
        "parameters" : [ {
          "description" : "El id de la importación, que genera quien la confirma",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "multipart/form-data" : {
              "encoding" : {
                "mapeo" : {
                  "contentType" : "application/json"
                }
              },
              "schema" : {
                "properties" : {
                  "aceptarDuplicados" : {
                    "description" : "Con true entran también las filas que coinciden con un movimiento que ya existe. Sin él, un archivo con duplicados responde 40970 y no escribe nada (BDD-21-3)",
                    "type" : "boolean"
                  },
                  "archivo" : {
                    "description" : "El mismo CSV que se analizó, de 5 MB como máximo",
                    "format" : "binary",
                    "type" : "string"
                  },
                  "mapeo" : {
                    "additionalProperties" : {
                      "type" : "string"
                    },
                    "description" : "Qué columna del archivo es cada campo del movimiento: {\"clave del campo\": \"nombre de la columna\"}. Las claves salen de columnasEsperadas",
                    "type" : "object"
                  }
                },
                "required" : [ "archivo", "mapeo" ],
                "type" : "object"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreInformeDeImportacion"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Confirmar la importación y escribir las filas válidas",
        "tags" : [ "Importación" ]
      }
    },
    "/api/v0/movimientos/{id}" : {
      "put" : {
        "description" : "Registra un ingreso, un gasto o una transferencia con su fecha real y su fecha de digitación (RF-08 a RF-11, CU-01, CU-02). El id va en la ruta por lo mismo que en pedidos y clientes (ADR-020): lo genera quien registra al decidir la acción, así que en el cuerpo sobraría, y el mismo dos veces responde 40900 en vez de duplicar el libro. Quién registra lo pone la sesión y lo juzga mov_insercion (04 §7), no un if. Lo digitado más de 7 días después de ocurrido se guarda igual y llega marcado como registro tardío (RN-14). Registran los dos tipos de usuario. El formulario es «movimiento»; la fecha futura, la cuenta, la categoría y la cuenta de destino no caben en el descriptor y responden 42223, 42224, 42225 y 42226. Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40302, 40900, 40901, 40902, 42200 con data.errores, 42223, 42224, 42225 y 42226.",
        "operationId" : "registrarMovimiento",
        "parameters" : [ {
          "description" : "El id del movimiento, que genera quien registra",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevoMovimiento"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreMovimiento"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Registrar un movimiento",
        "tags" : [ "Movimientos" ]
      }
    },
    "/api/v0/movimientos/{id}/adjuntos" : {
      "post" : {
        "description" : "Sube la foto del recibo o el PDF de la factura y lo deja colgado del movimiento (RF-09, RF-12, CU-02). El archivo pasa por la API y nunca va directo al almacenamiento (07 §1). Máximo 5 MB, que responde 40020, y solo image/jpeg, image/png, image/webp o application/pdf, que responde 40021. Adjuntan los dos tipos de usuario. No tiene formulario: el cuerpo es multipart/form-data con una sola parte, «archivo». Devolver el archivo sigue sin operación, pero la forma en que bajaría ya está acordada desde el 0.15.0: es el esquema Documento, dentro del sobre. La operación es de la tarea 3.6. Puede devolver 20100, 40002, 40020, 40021, 40100, 40101, 40102, 40103, 40302, 40400, 40901 y 40902.",
        "operationId" : "adjuntarSoporte",
        "parameters" : [ {
          "description" : "El id del movimiento al que se le adjunta el soporte",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "multipart/form-data" : {
              "schema" : {
                "properties" : {
                  "archivo" : {
                    "description" : "La foto o el PDF, de 5 MB como máximo",
                    "format" : "binary",
                    "type" : "string"
                  }
                },
                "required" : [ "archivo" ],
                "type" : "object"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreAdjunto"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Adjuntar el soporte de un movimiento",
        "tags" : [ "Movimientos" ]
      }
    },
    "/api/v0/movimientos/{id}/anulacion" : {
      "post" : {
        "description" : "Anula el movimiento con motivo escrito y no borra nada (RF-14, RN-13, CU-03): la fila guarda cuándo, quién y por qué, y desde ese momento no suma en ninguna cifra. Solo Gerencia, y lo rechaza la base con mov_anulacion (04 §7). Para corregir un valor equivocado se registra un contra-asiento, que es otro movimiento, y el original queda intacto (RF-15, 04 §5.3). Uno ya anulado responde 40900. El formulario es «anulacion-de-movimiento». Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901, 40902 y 42200 con data.errores.",
        "operationId" : "anularMovimiento",
        "parameters" : [ {
          "description" : "El id del movimiento que se anula",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/Motivo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreMovimiento"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Anular un movimiento",
        "tags" : [ "Movimientos" ]
      }
    },
    "/api/v0/nomina/liquidaciones/{id}" : {
      "put" : {
        "description" : "Liquida el mes de una empleada (RF-55, RF-56, CU-19, BDD-26-2). La API no rehace los pasos: llama a fn_liquidar_nomina, que en una sola transacción escribe la fila de nomina_detalle, marca con descontado_en los adelantos pendientes de esa persona y escribe el movimiento de tipo gasto que reconoce el gasto de personal completo (04 §10, 06 §6.3). Las cifras son las del 06 §6.1: el valor de la hora ordinaria sale del salario acordado entre las horas pactadas, la extra lleva el recargo, y al devengado solo entran el salario del período y el total de las horas extra, no las tarifas por hora. El id va en la ruta por lo mismo que en movimientos (ADR-020): el mismo dos veces responde 40900. Solo Gerencia, y lo rechaza la base con nom_escritura. El formulario es «liquidacion»; un período cerrado responde 40951 y una empleada ya liquidada en él, 40952, que es nomina_detalle_periodo_empleado_key. Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40900, 40901, 40902, 40951, 40952, 42200 con data.errores, 42223, 42224 y 42251.",
        "operationId" : "liquidarNomina",
        "parameters" : [ {
          "description" : "El id de la liquidación, que genera quien la hace",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevaLiquidacion"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreLiquidacion"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Liquidar la nómina de una empleada",
        "tags" : [ "Nómina" ]
      }
    },
    "/api/v0/nomina/periodos/{id}" : {
      "put" : {
        "description" : "Abre el mes que se va a liquidar. Es único por año y mes: el segundo con la misma pareja responde 42252, que es nomina_periodos_anio_mes_key. No es el cierre mensual del negocio, que es otra tabla y otro sprint. Solo Gerencia, y lo rechaza la base con nom_per_insercion. El formulario es «periodo-de-nomina». Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40900, 40901, 40902, 42200 con data.errores y 42252.",
        "operationId" : "abrirPeriodoDeNomina",
        "parameters" : [ {
          "description" : "El id del período, que genera quien lo abre",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevoPeriodoDeNomina"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobrePeriodoDeNomina"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Abrir el período de nómina de un mes",
        "tags" : [ "Nómina" ]
      }
    },
    "/api/v0/nomina/periodos/{id}/cierre" : {
      "post" : {
        "description" : "Cierra el mes: guarda cuándo y quién, y desde ahí no admite más liquidaciones —una más responde 40951—. No tiene formulario: no hay nada que escribir. Un período ya cerrado responde 40900. Solo Gerencia, y lo rechaza la base con nom_per_actualizacion. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901 y 40902.",
        "operationId" : "cerrarPeriodoDeNomina",
        "parameters" : [ {
          "description" : "El id del período",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobrePeriodoDeNomina"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Cerrar el período de nómina",
        "tags" : [ "Nómina" ]
      }
    },
    "/api/v0/pedidos/{id}" : {
      "put" : {
        "description" : "Registra un pedido con sus líneas y lo deja en_proceso (RF-19, CU-05). El id va en la ruta por lo mismo que en clientes (ADR-020); el número visible lo pone la API y por eso no viaja en el cuerpo. El valor total sale de las líneas y tampoco se manda: mandarlo sería dejar que el front calcule plata (ADR-018). Un pedido sin líneas, con una cantidad que no es positiva o con un producto que no existe responde 42231; un cliente que no existe o está anulado, 42232. Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40302, 40900, 40901, 40902, 42200 con data.errores, 42231 y 42232.",
        "operationId" : "registrarPedido",
        "parameters" : [ {
          "description" : "El id del pedido, que genera quien pide",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevoPedido"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobrePedido"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Registrar un pedido",
        "tags" : [ "Pedidos" ]
      }
    },
    "/api/v0/pedidos/{id}/anticipos" : {
      "post" : {
        "description" : "Cobra un anticipo del pedido: sube la caja de la cuenta indicada y registra un pasivo, nunca un ingreso (RN-05, RF-21, CU-06). El movimiento que escribe es de tipo anticipo_recibido, y el anticipo queda por devengar hasta que se entregue. Solo sobre un pedido en_proceso: sobre cualquier otro estado responde 40930. Un anticipo que sumado a los ya cobrados supera el valor del pedido responde 42230, que es CU-06 A1; pagar el 100 % por adelantado sí se puede. Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40302, 40400, 40901, 40902, 40930, 42200 con data.errores, 42230, 42233 y 42234.",
        "operationId" : "cobrarAnticipo",
        "parameters" : [ {
          "description" : "El id del pedido",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevoAnticipo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobrePedido"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Cobrar un anticipo",
        "tags" : [ "Pedidos" ]
      }
    },
    "/api/v0/pedidos/{id}/anulacion" : {
      "post" : {
        "description" : "Anula el pedido con motivo escrito y no borra nada (RN-13): la fila guarda cuándo, quién y por qué, y sigue apareciendo en el panel de anulados. Solo Gerencia. No es cancelar: se anula lo que no debió registrarse, y se cancela lo que se registró bien y no va a entregarse. Un pedido ya anulado responde 40900. El formulario es «anulacion-de-pedido». Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901, 40902 y 42200 con data.errores.",
        "operationId" : "anularPedido",
        "parameters" : [ {
          "description" : "El id del pedido",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/Motivo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobrePedido"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Anular un pedido",
        "tags" : [ "Pedidos" ]
      }
    },
    "/api/v0/pedidos/{id}/cancelacion" : {
      "post" : {
        "description" : "Cancela un pedido que todavía no se entregó y dice qué pasa con el anticipo ya cobrado: se le devuelve al cliente o se convierte en ingreso, con motivo obligatorio (RF-26, CU-07 A3). Cancelar no es anular: cancelar es un paso del pedido y anular es sacar el registro de las cuentas (RN-13). Si el pedido tiene anticipos por devengar, destinoDelAnticipo es obligatorio y responde 42235 si falta; con devolucion hace falta además la cuenta de donde sale la plata. Sobre un pedido entregado o ya cancelado responde 40932. El formulario es «cancelacion». Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40400, 40901, 40902, 40932, 42200 con data.errores, 42233 y 42235.",
        "operationId" : "cancelarPedido",
        "parameters" : [ {
          "description" : "El id del pedido",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/Cancelacion"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobrePedido"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Cancelar un pedido",
        "tags" : [ "Pedidos" ]
      }
    },
    "/api/v0/pedidos/{id}/entrega" : {
      "post" : {
        "description" : "Causa la venta por el valor total con la fecha de entrega, causa el costo, devenga los anticipos, sube la caja por el saldo y deja el pedido entregado (RN-06, RF-22, CU-07). Las cinco cosas ocurren en una sola transacción porque las hace fn_entregar_pedido (04 §10), y el cuerpo es exactamente lo que esa función recibe: fecha y cuenta. Se puede entregar sin cobrar el saldo y la venta se causa igual, que es CU-07 A1. Entregado es final: sobre un pedido entregado o cancelado responde 40931. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40400, 40901, 40902, 40931, 42200 con data.errores, 42233 y 42234.",
        "operationId" : "entregarPedido",
        "parameters" : [ {
          "description" : "El id del pedido",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/Entrega"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobrePedido"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Entregar el pedido y cobrar el saldo",
        "tags" : [ "Pedidos" ]
      }
    },
    "/api/v0/pedidos/{id}/remision" : {
      "post" : {
        "description" : "El papel de lo que se entregó, en PDF con el logo (RF-37, CU-11). Cuelga del pedido y no de la cotización porque lo entregado es un pedido: una cotización puede no haberse aceptado nunca. Baja dentro del sobre, en data, como un Documento en base64 (tarea 8.11). Un pedido anulado responde 40900. La generan los dos tipos de usuario. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40302, 40400, 40900, 40901 y 40902.",
        "operationId" : "remisionEnPdf",
        "parameters" : [ {
          "description" : "El id del pedido",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreDocumento"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "La remisión de entrega en PDF",
        "tags" : [ "Cotizaciones" ]
      }
    },
    "/api/v0/productos/{id}" : {
      "put" : {
        "description" : "Da de alta algo que el taller vende, con su primer costeo (RF-28, RF-29, CU-09, CU-10). El id va en la ruta y no en el cuerpo: lo genera quien pide al decidir la acción, junto con la clave de idempotencia (ADR-020), así que reintentar la misma petición no crea dos productos, y si ese id ya existe responde 40900. El costo viaja con la ficha en una sola petición porque es una sola transacción (20 §5.1): la fila de productos y la primera de costos_producto caen juntas o no cae ninguna. La tarifa por hora no se manda: la API la saca del pro-labore vigente y la congela en el costeo (05 §7.1), y sin pro-labore definido responde 40940. Solo Gerencia. El formulario es «producto»; el tiempo con más de dos decimales no cabe en el descriptor y responde 42240. Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40900, 40901, 40902, 40940, 42200 con data.errores y 42240.",
        "operationId" : "crearProducto",
        "parameters" : [ {
          "description" : "El id del producto, que genera quien lo da de alta",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/DatosDeProducto"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreProducto"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Crear un producto o un servicio",
        "tags" : [ "Productos" ]
      }
    },
    "/api/v0/productos/{id}/desactivacion" : {
      "post" : {
        "description" : "Lo saca del catálogo con motivo escrito: deja de ofrecerse al cotizar y al crear pedidos, y no se borra (RN-13). Los pedidos que ya lo tienen siguen enteros, y el producto queda abajo con su motivo y su fecha, como en el mockup. En productos eso se escribe en anulado_en, anulado_por y anulado_motivo, que es la misma forma de los cargos (04 §4.2): la tabla no tiene columnas propias de desactivación. Uno ya inactivo responde 40900. Solo Gerencia. El formulario es «desactivacion-de-producto». Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901, 40902 y 42200 con data.errores.",
        "operationId" : "desactivarProducto",
        "parameters" : [ {
          "description" : "El id del producto",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/Motivo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreProducto"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Desactivar un producto",
        "tags" : [ "Productos" ]
      }
    },
    "/api/v0/productos/{id}/edicion" : {
      "post" : {
        "description" : "Guarda el formulario entero, como el botón «Guardar cambios» del mockup: corrige la ficha y, si el costo o el precio cambiaron, agrega una fila nueva a costos_producto con la fecha de hoy, porque un costeo no se sobrescribe nunca (04 §4.5, RF-35). Así un pedido viejo conserva el costo que tenía cuando se produjo. Editar dos veces el mismo día deja dos filas con la misma vigencia, y la vigente es la última registrada. Solo Gerencia. Un producto que no existe responde 40400. El formulario es «producto», el mismo que crea. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901, 40902, 40940, 42200 con data.errores y 42240.",
        "operationId" : "editarProducto",
        "parameters" : [ {
          "description" : "El id del producto",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/DatosDeProducto"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreProducto"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Guardar los cambios de un producto",
        "tags" : [ "Productos" ]
      }
    },
    "/api/v0/productos/{id}/reactivacion" : {
      "post" : {
        "description" : "Lo devuelve al catálogo para volver a ofrecerlo, como el botón «Reactivar» del mockup. No pide motivo, igual que en el mockup y que en los cargos. Uno activo responde 40900. Solo Gerencia. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901 y 40902.",
        "operationId" : "reactivarProducto",
        "parameters" : [ {
          "description" : "El id del producto",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreProducto"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Reactivar un producto",
        "tags" : [ "Productos" ]
      }
    },
    "/api/v0/prolabore/{id}" : {
      "put" : {
        "description" : "Define desde hoy cuánto vale al mes el trabajo de la gerencia en el taller y cuántas horas produce (RF-47, CU-25, RN-08). No edita la definición anterior: escribe una fila nueva de prolabore_config, vigente desde el día en que se guarda en Bogotá, así que el historial queda entero (04 §4.6). Desde ese día los costeos nuevos toman de aquí su tarifa por hora (05 §7.1) —los ya guardados conservan la suya—, y cada retiro se parte con el pro-labore vigente en su fecha. El mismo id dos veces responde 40900 (ADR-020). Solo Gerencia, y lo rechaza la base con prolabore_solo_gerencia (04 §7). El formulario es «prolabore»; las horas con más de dos decimales no caben en el descriptor y responden 42294. Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40900, 40901, 40902, 42200 con data.errores y 42294.",
        "operationId" : "definirProlabore",
        "parameters" : [ {
          "description" : "El id de esta definición del pro-labore, que genera quien la guarda",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevoProlabore"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreProlabore"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Definir el pro-labore",
        "tags" : [ "Capital" ]
      }
    },
    "/api/v0/retiros/{id}" : {
      "put" : {
        "description" : "Registra plata que la gerencia saca del negocio, partida en sus dos naturalezas (RF-47, CU-16, RN-07, RN-08): el pro-labore paga el trabajo en el taller y baja la utilidad; la distribución reparte una ganancia ya generada y no la toca. Las dos bajan la caja y el patrimonio (BDD-16-1, BDD-25-1). Escribe en una sola transacción un movimiento —retiro_prolabore o retiro_distribucion— y una fila de aportes_retiros por cada parte que no sea cero. Sin prolabore en el cuerpo, la API parte sola: pro-labore hasta completar el del mes, vigente en la fecha del retiro, y el resto distribución; es lo mismo que propone POST /api/v0/consultas/division-de-retiro. El mismo id dos veces responde 40900 (ADR-020). Solo Gerencia, y lo rechaza la base con ret_solo_gerencia (04 §7). El formulario es «retiro»; la fecha futura, la cuenta que no sirve y un pro-labore mayor que el retiro no caben en el descriptor y responden 42290, 42291 y 42292. Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40900, 40901, 40902, 42200 con data.errores, 42290, 42291 y 42292.",
        "operationId" : "registrarRetiro",
        "parameters" : [ {
          "description" : "El id del retiro, que genera quien lo registra",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevoRetiro"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreRetiro"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Registrar un retiro, partido en pro-labore y distribución",
        "tags" : [ "Capital" ]
      }
    },
    "/api/v0/sesiones" : {
      "post" : {
        "description" : "Entra con el nombre de usuario y la contraseña, sin correo (RF-01, CU-28). La API pasa el usuario a minúsculas, arma el correo sintético y llama al proveedor: el correo nunca viaja por aquí (ADR-009). No lleva sesión, firma ni Idempotency-Key: todavía no hay clave con qué firmar, y la respuesta trae secretos que no se guardan en la tabla de idempotencia. Responde el token, la clave de firma y quién entró; la renovación va aparte, en la cookie prisma_renovacion. Un usuario que no existe y una contraseña equivocada responden lo mismo, 40104, para no revelar quién trabaja en el taller; 40301 solo sale con la contraseña correcta. Escribe inicio_sesion o inicio_sesion_fallido en la auditoría, nunca con la contraseña tecleada. El formulario es «acceso». Puede devolver 20100, 40000, 40104, 40301 y 42200 con data.errores.",
        "operationId" : "iniciarSesion",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevaSesion"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreSesion"
                }
              }
            },
            "description" : "Created",
            "headers" : {
              "Set-Cookie" : {
                "description" : "La cookie prisma_renovacion: HttpOnly, Secure, SameSite=None, Path=/api/v0/sesiones y 30 días de vida que vuelven a empezar con cada renovación. El front nunca la lee. Trae la de esta sesión",
                "schema" : {
                  "type" : "string"
                }
              }
            }
          }
        },
        "summary" : "Iniciar sesión",
        "tags" : [ "Acceso" ]
      }
    },
    "/api/v0/sesiones/actual" : {
      "delete" : {
        "description" : "Cierra la sesión en el proveedor, vence la cookie prisma_renovacion y escribe cierre_sesion en la auditoría (CU-28, A5). La clave de firma de la sesión deja de servir. Responde aunque la sesión tenga pendiente el cambio de contraseña. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40901 y 40902.",
        "operationId" : "cerrarSesion",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreVoid"
                }
              }
            },
            "description" : "OK",
            "headers" : {
              "Set-Cookie" : {
                "description" : "La cookie prisma_renovacion, vencida",
                "schema" : {
                  "type" : "string"
                }
              }
            }
          }
        },
        "summary" : "Cerrar sesión",
        "tags" : [ "Acceso" ]
      }
    },
    "/api/v0/sesiones/actual/clave" : {
      "put" : {
        "description" : "La persona de la sesión cambia su propia contraseña (RF-79, RF-80, CU-32). «Crea tu contraseña», la del primer ingreso, y «Cambiar mi contraseña», la del menú de la sesión, son la misma operación, y como en el mockup no piden la contraseña actual. Pone debeCambiarClave en false, escribe clave_cambiada en la auditoría y responde la identidad al día; el token y la clave de firma siguen siendo los de la sesión. El formulario es «cambio-de-clave», y que las dos contraseñas coincidan lo comprueba la API. Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40901, 40902, 42200 y 42210; los dos últimos con data.errores.",
        "operationId" : "cambiarClave",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/CambioDeClave"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreIdentidad"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Cambiar mi contraseña",
        "tags" : [ "Acceso" ]
      }
    },
    "/api/v0/sesiones/renovacion" : {
      "post" : {
        "description" : "Cambia la cookie prisma_renovacion por un token nuevo y una clave de firma nueva. Es lo que el front pide al recargar la página —la clave de firma vivía en memoria y se fue con ella— y antes de que venza el token. Tampoco lleva firma ni Idempotency-Key, por la misma razón que el inicio de sesión. Cada renovación entrega una cookie nueva y la anterior deja de servir. Una sesión cerrada, sin cookie o sin uso en 30 días responde 40100 (RF-04); la de un usuario que Gerencia desactivó mientras tanto, 40301. Puede devolver 20000, 40100 y 40301.",
        "operationId" : "renovarSesion",
        "parameters" : [ {
          "description" : "La cookie prisma_renovacion: HttpOnly, Secure, SameSite=None, Path=/api/v0/sesiones y 30 días de vida que vuelven a empezar con cada renovación. El front nunca la lee. Sin ella la respuesta es 40100, no un 400: quien abre la aplicación por primera vez no armó mal la petición, es que todavía no ha entrado",
          "in" : "cookie",
          "name" : "prisma_renovacion",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreSesion"
                }
              }
            },
            "description" : "OK",
            "headers" : {
              "Set-Cookie" : {
                "description" : "La cookie prisma_renovacion: HttpOnly, Secure, SameSite=None, Path=/api/v0/sesiones y 30 días de vida que vuelven a empezar con cada renovación. El front nunca la lee. Trae la de esta sesión",
                "schema" : {
                  "type" : "string"
                }
              }
            }
          }
        },
        "summary" : "Renovar la sesión",
        "tags" : [ "Acceso" ]
      }
    },
    "/api/v0/sobres/{id}" : {
      "put" : {
        "description" : "Define desde hoy cómo se reparte cada peso que entra en efectivo: costo directo, gastos fijos, reserva y retiro (RF-50, CU-17, 05 §11). No edita la configuración anterior: escribe una fila nueva de sobres_config, vigente desde el día en que se guarda en Bogotá, y así se conserva el historial (RF-51, 04 §4.8). Cada sobre va de 0 a 100 y los cuatro suman 100: lo primero lo dice el descriptor, y lo segundo es la restricción suma_cien, que responde 42293. El mismo id dos veces responde 40900 (ADR-020). Solo Gerencia, y lo rechaza la base con sobres_solo_gerencia (04 §7, P-07). El formulario es «sobres». Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40900, 40901, 40902, 42200 con data.errores y 42293.",
        "operationId" : "configurarSobres",
        "parameters" : [ {
          "description" : "El id de esta configuración de los sobres, que genera quien la guarda",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevaConfiguracionDeSobres"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreConfiguracionDeSobres"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Configurar los cuatro sobres",
        "tags" : [ "Capital" ]
      }
    },
    "/api/v0/usuarios" : {
      "post" : {
        "description" : "Da acceso a una persona con una contraseña temporal que Gerencia le entrega en persona (RF-72 a RF-76, CU-29). El usuario queda activo y con debeCambiarClave en true. La API pasa el usuario a minúsculas antes de comprobarlo, así que si existe marcela, Marcela se rechaza con 42211 (BDD-29-2). Solo Gerencia, y lo rechaza la base (04 §7). El formulario es «usuario»; el formato del usuario, el usuario repetido y el cargo inactivo no caben en el descriptor y responden 42212, 42211 y 42213. Puede devolver 20100, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40901, 40902, 42200, 42211, 42212 y 42213; los cuatro últimos con data.errores.",
        "operationId" : "crearUsuario",
        "parameters" : [ {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/NuevoUsuario"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreUsuario"
                }
              }
            },
            "description" : "Created"
          }
        },
        "summary" : "Crear un usuario",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v0/usuarios/{id}" : {
      "put" : {
        "description" : "Cambia el nombre completo, el cargo y el tipo de una persona activa (RF-76); a una desactivada responde 40900. El usuario no se edita. Cada campo que cambia deja su propia entrada en la bitácora, para poder revertirlo solo. Conservar un cargo inactivo que la persona ya tenía se permite; cambiarlo por uno inactivo responde 42213. Quitarle el tipo Gerencia a la última Gerencia activa responde 40910, y lo rechaza la base (RF-82). El formulario es «edicion-de-usuario». Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901, 40902, 40910, 42200 y 42213; los dos últimos con data.errores.",
        "operationId" : "editarUsuario",
        "parameters" : [ {
          "description" : "El id del usuario",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/EdicionDeUsuario"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreUsuario"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Editar un usuario",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v0/usuarios/{id}/clave-temporal" : {
      "put" : {
        "description" : "Gerencia le pone una contraseña temporal a una persona activa y se la entrega en persona: no hay correo de recuperación (RF-78, CU-31). Pone debeCambiarClave en true y escribe clave_restablecida, el único cambio que no se puede revertir. La respuesta no devuelve la contraseña. A una persona desactivada responde 40900. El formulario es «clave-temporal». Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901, 40902 y 42200 con data.errores.",
        "operationId" : "restablecerClave",
        "parameters" : [ {
          "description" : "El id del usuario",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ClaveTemporal"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreUsuario"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Restablecer la contraseña de un usuario",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v0/usuarios/{id}/desactivacion" : {
      "post" : {
        "description" : "Le quita el acceso con motivo escrito y no borra nada (RF-77, RF-86, CU-30): la ficha guarda cuándo, quién y por qué, y la bitácora guarda la historia. Desde ese momento no puede iniciar ni renovar su sesión (40301). La última Gerencia activa no se desactiva: 40910, y lo rechaza la base. Una persona ya desactivada responde 40900. El formulario es «desactivacion-de-usuario». Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901, 40902, 40910 y 42200 con data.errores.",
        "operationId" : "desactivarUsuario",
        "parameters" : [ {
          "description" : "El id del usuario",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/Motivo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreUsuario"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Desactivar un usuario",
        "tags" : [ "Usuarios" ]
      }
    },
    "/api/v0/usuarios/{id}/reactivacion" : {
      "post" : {
        "description" : "Le devuelve el acceso con motivo escrito (RF-86, CU-34): limpia la desactivación, pone debeCambiarClave en true (RF-90) y deja la entrada «Reactivado» en la bitácora. Una persona activa responde 40900. El formulario es «reactivacion-de-usuario». Puede devolver 20000, 40002, 40100, 40101, 40102, 40103, 40300, 40302, 40400, 40900, 40901, 40902 y 42200 con data.errores.",
        "operationId" : "reactivarUsuario",
        "parameters" : [ {
          "description" : "El id del usuario",
          "in" : "path",
          "name" : "id",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 que se genera cuando la persona decide la acción y se reutiliza en cada reintento (ADR-020). Sin ella responde 40002",
          "in" : "header",
          "name" : "Idempotency-Key",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "HMAC-SHA256 en hexadecimal minúscula de las cinco líneas del canal firmado, con la clave de firma de la sesión (20 §6.2). Si falta o no cuadra, responde 40101",
          "in" : "header",
          "name" : "X-Prisma-Firma",
          "required" : true,
          "schema" : {
            "pattern" : "^[0-9a-f]{64}$",
            "type" : "string"
          }
        }, {
          "description" : "UUID v4 nuevo en cada petición, también en cada reintento de la misma intención. Uno ya visto dentro de la ventana responde 40103",
          "in" : "header",
          "name" : "X-Prisma-Nonce",
          "required" : true,
          "schema" : {
            "format" : "uuid",
            "type" : "string"
          }
        }, {
          "description" : "Cuándo se firmó, ISO 8601 en UTC: 2026-09-17T19:32:07Z. Fuera de ±5 minutos del reloj de la API responde 40102",
          "in" : "header",
          "name" : "X-Prisma-Timestamp",
          "required" : true,
          "schema" : {
            "format" : "date-time",
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/Motivo"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SobreUsuario"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Reactivar un usuario",
        "tags" : [ "Usuarios" ]
      }
    }
  },
  "servers" : [ {
    "description" : "El mismo servidor que sirve esta documentación",
    "url" : "/"
  } ],
  "tags" : [ {
    "description" : "Iniciar, renovar y cerrar la sesión, y cambiar la propia contraseña",
    "name" : "Acceso"
  }, {
    "description" : "Quién cambió qué sobre usuarios y cargos, y cómo se deshace sin borrar",
    "name" : "Bitácora"
  }, {
    "description" : "Inversiones en activos, aportes y retiros de la propiedad, el pro-labore, el patrimonio y los cuatro sobres",
    "name" : "Capital"
  }, {
    "description" : "El catálogo de cargos: describe el oficio, no da permisos",
    "name" : "Cargos"
  }, {
    "description" : "El árbol de categorías de ingreso y gasto",
    "name" : "Categorías"
  }, {
    "description" : "A quién se le entrega: nombre, contacto y notas",
    "name" : "Clientes"
  }, {
    "description" : "Lo que se le entrega al cliente en papel: la cotización que se le pasa por WhatsApp y la remisión de lo entregado",
    "name" : "Cotizaciones"
  }, {
    "description" : "Las cuentas de dinero donde se registra cada movimiento",
    "name" : "Cuentas"
  }, {
    "description" : "Los campos de cada formulario y sus reglas, como datos",
    "name" : "Formularios"
  }, {
    "description" : "El histórico que entra desde un CSV, con su mapeo de columnas y su informe fila por fila",
    "name" : "Importación"
  }, {
    "description" : "El libro único: lo que entra, lo que sale y lo que solo cambia de cuenta",
    "name" : "Movimientos"
  }, {
    "description" : "Lo que la sesión puede ver, dictado por la API",
    "name" : "Navegación"
  }, {
    "description" : "Las empleadas, sus adelantos, la liquidación del mes y el simulador que responde si se puede contratar",
    "name" : "Nómina"
  }, {
    "description" : "Los pedidos del taller, y la entrega que causa la venta",
    "name" : "Pedidos"
  }, {
    "description" : "El catálogo de lo que el taller vende, con su costo y su margen por hora",
    "name" : "Productos"
  }, {
    "description" : "Las personas con acceso, su tipo y su cargo",
    "name" : "Usuarios"
  }, {
    "description" : "Versión de la API, del esquema y ambiente en que corre",
    "name" : "Versión"
  } ],
  "x-prisma-codigos" : [ {
    "cuando" : "Cualquier consulta que devuelve datos",
    "http" : 200,
    "mensaje" : "Consulta correcta.",
    "modulo" : "GENERICO",
    "nombre" : "OK",
    "status" : 20000
  }, {
    "cuando" : "Cualquier operación que crea un recurso",
    "http" : 201,
    "mensaje" : "Registro creado.",
    "modulo" : "GENERICO",
    "nombre" : "CREADO",
    "status" : 20100
  }, {
    "cuando" : "JSON inválido, método que la ruta no acepta, tipo de contenido o parámetro equivocado",
    "http" : 400,
    "mensaje" : "La petición no se entiende.",
    "modulo" : "GENERICO",
    "nombre" : "PETICION_MAL_FORMADA",
    "status" : 40000
  }, {
    "cuando" : "Una petición llegó sin la cabecera Idempotency-Key, o con una que no es un UUID v4",
    "http" : 400,
    "mensaje" : "No se pudo enviar la operación. Intenta de nuevo.",
    "modulo" : "SESION_Y_TRANSPORTE",
    "nombre" : "FALTA_CLAVE_DE_IDEMPOTENCIA",
    "status" : 40002
  }, {
    "cuando" : "No hay token, está vencido o no se puede verificar",
    "http" : 401,
    "mensaje" : "Inicia sesión para continuar.",
    "modulo" : "GENERICO",
    "nombre" : "NO_AUTENTICADO",
    "status" : 40100
  }, {
    "cuando" : "Le faltan las cabeceras de firma a una petición con sesión, o el HMAC no corresponde con la clave de firma de la sesión",
    "http" : 401,
    "mensaje" : "Inicia sesión para continuar.",
    "modulo" : "SESION_Y_TRANSPORTE",
    "nombre" : "FIRMA_INVALIDA",
    "status" : 40101
  }, {
    "cuando" : "La marca de tiempo de la petición cayó fuera de la ventana de ±5 minutos",
    "http" : 401,
    "mensaje" : "Revisa la hora de tu dispositivo e intenta de nuevo.",
    "modulo" : "SESION_Y_TRANSPORTE",
    "nombre" : "MARCA_DE_TIEMPO_FUERA_DE_VENTANA",
    "status" : 40102
  }, {
    "cuando" : "Llegó una petición con un nonce ya usado dentro de la ventana: es un reenvío",
    "http" : 401,
    "mensaje" : "Esa operación ya se envió.",
    "modulo" : "SESION_Y_TRANSPORTE",
    "nombre" : "NONCE_REPETIDO",
    "status" : 40103
  }, {
    "cuando" : "Inicio de sesión con un usuario que no existe o con una contraseña equivocada: las dos cosas responden igual, para no revelar quién trabaja en el taller",
    "http" : 401,
    "mensaje" : "Usuario o contraseña incorrectos.",
    "modulo" : "SESION_Y_TRANSPORTE",
    "nombre" : "CREDENCIALES_INCORRECTAS",
    "status" : 40104
  }, {
    "cuando" : "El tipo de usuario de la sesión no alcanza para la operación",
    "http" : 403,
    "mensaje" : "No tienes permiso para hacer esto.",
    "modulo" : "GENERICO",
    "nombre" : "SIN_PERMISO",
    "status" : 40300
  }, {
    "cuando" : "Inicia o renueva la sesión un usuario desactivado. Al iniciarla, solo con la contraseña correcta: con una equivocada responde 40104",
    "http" : 403,
    "mensaje" : "Este usuario está desactivado. Habla con Gerencia.",
    "modulo" : "SESION_Y_TRANSPORTE",
    "nombre" : "USUARIO_DESACTIVADO",
    "status" : 40301
  }, {
    "cuando" : "La sesión entró con una contraseña temporal y pide algo distinto de cambiarla, renovar o cerrar la sesión, o un formulario",
    "http" : 403,
    "mensaje" : "Crea tu contraseña para continuar.",
    "modulo" : "SESION_Y_TRANSPORTE",
    "nombre" : "DEBE_CAMBIAR_CLAVE",
    "status" : 40302
  }, {
    "cuando" : "La ruta o el recurso pedido no existe, o fue anulado",
    "http" : 404,
    "mensaje" : "No se encontró lo que buscas.",
    "modulo" : "GENERICO",
    "nombre" : "NO_EXISTE",
    "status" : 40400
  }, {
    "cuando" : "La operación choca con el estado en que está el recurso",
    "http" : 409,
    "mensaje" : "Eso no se puede hacer en el estado actual.",
    "modulo" : "GENERICO",
    "nombre" : "CONFLICTO_DE_ESTADO",
    "status" : 40900
  }, {
    "cuando" : "Misma Idempotency-Key con una huella de petición distinta",
    "http" : 409,
    "mensaje" : "Esa operación ya se registró con otros datos.",
    "modulo" : "SESION_Y_TRANSPORTE",
    "nombre" : "CLAVE_REPETIDA_OTROS_DATOS",
    "status" : 40901
  }, {
    "cuando" : "Misma Idempotency-Key, y la primera petición todavía no termina",
    "http" : 409,
    "mensaje" : "Esa operación se está procesando. Espera un momento.",
    "modulo" : "SESION_Y_TRANSPORTE",
    "nombre" : "OPERACION_EN_CURSO",
    "status" : 40902
  }, {
    "cuando" : "Desactivar, quitarle el tipo Gerencia o revertir un cambio sobre el último usuario activo de Gerencia. Lo rechaza la base",
    "http" : 409,
    "mensaje" : "No se puede desactivar ni degradar al último usuario de Gerencia.",
    "modulo" : "USUARIOS_Y_CARGOS",
    "nombre" : "ULTIMA_GERENCIA",
    "status" : 40910
  }, {
    "cuando" : "Desactivar un cargo, o revertir su creación o su reactivación, mientras lo tengan personas activas",
    "http" : 409,
    "mensaje" : "Ese cargo todavía lo tienen personas activas. Cámbiales el cargo antes de sacarlo del catálogo.",
    "modulo" : "USUARIOS_Y_CARGOS",
    "nombre" : "CARGO_CON_PERSONAS_ACTIVAS",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 2.8: catálogo de cargos",
      "sprint" : 2
    },
    "status" : 40911
  }, {
    "cuando" : "Revertir una clave restablecida: el sistema solo guarda el hash, no la contraseña anterior",
    "http" : 409,
    "mensaje" : "Una clave restablecida no se puede revertir: el sistema no guarda la contraseña anterior. Restablécela otra vez.",
    "modulo" : "USUARIOS_Y_CARGOS",
    "nombre" : "CAMBIO_NO_REVERSIBLE",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 2.16: bitácora y reversión",
      "sprint" : 2
    },
    "status" : 40912
  }, {
    "cuando" : "Revertir la creación del usuario que tiene la sesión abierta",
    "http" : 409,
    "mensaje" : "No puedes revertir la creación de tu propio usuario: te quedarías por fuera del sistema.",
    "modulo" : "USUARIOS_Y_CARGOS",
    "nombre" : "REVERSION_DE_LA_PROPIA_CREACION",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 2.16: bitácora y reversión",
      "sprint" : 2
    },
    "status" : 40913
  }, {
    "cuando" : "Cobrar un anticipo sobre un pedido que no está en proceso: cotizado, parcial, entregado o cancelado",
    "http" : 409,
    "mensaje" : "Ese pedido no está en proceso, así que no se le puede cobrar anticipo.",
    "modulo" : "PEDIDOS_Y_CLIENTES",
    "nombre" : "PEDIDO_NO_ESTA_EN_PROCESO",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 4.4: cobrar anticipo",
      "sprint" : 4
    },
    "status" : 40930
  }, {
    "cuando" : "Entregar un pedido que ya está entregado o cancelado, que son los dos estados finales",
    "http" : 409,
    "mensaje" : "Ese pedido ya está cerrado: no se puede volver a entregar.",
    "modulo" : "PEDIDOS_Y_CLIENTES",
    "nombre" : "PEDIDO_YA_ESTA_CERRADO",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 4.5: entregar y cobrar saldo",
      "sprint" : 4
    },
    "status" : 40931
  }, {
    "cuando" : "Cancelar un pedido ya entregado o ya cancelado",
    "http" : 409,
    "mensaje" : "Ese pedido ya está cerrado: lo que haya que corregir va por contra-asiento.",
    "modulo" : "PEDIDOS_Y_CLIENTES",
    "nombre" : "PEDIDO_NO_SE_PUEDE_CANCELAR",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 4.9: cancelación con destino del anticipo",
      "sprint" : 4
    },
    "status" : 40932
  }, {
    "cuando" : "Registrar o editar un costeo cuando no hay pro-labore definido con horas productivas: sin él la hora de trabajo no tiene precio, y el costo unitario no se puede calcular (05 §7.1, RN-08)",
    "http" : 409,
    "mensaje" : "Todavía no está definido el pro-labore, así que el trabajo no tiene precio. Defínelo antes de costear.",
    "modulo" : "PRODUCTOS_Y_COSTEO",
    "nombre" : "SIN_TARIFA_POR_HORA",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 5.2: catálogo de productos y servicios",
      "sprint" : 5
    },
    "status" : 40940
  }, {
    "cuando" : "Los datos llegaron bien formados pero no pasan las reglas; cada campo va en data.errores",
    "http" : 422,
    "mensaje" : "Revisa los datos.",
    "modulo" : "GENERICO",
    "nombre" : "DATOS_INVALIDOS",
    "status" : 42200
  }, {
    "cuando" : "Cambio de contraseña con dos contraseñas distintas. El error va en data.errores, sobre repeticion",
    "http" : 422,
    "mensaje" : "Las dos contraseñas no coinciden. Escríbelas otra vez.",
    "modulo" : "USUARIOS_Y_CARGOS",
    "nombre" : "CLAVES_NO_COINCIDEN",
    "status" : 42210
  }, {
    "cuando" : "Crear un usuario con un nombre de usuario que ya existe, sin distinguir mayúsculas. El error va en data.errores, sobre usuario",
    "http" : 422,
    "mensaje" : "Ya hay alguien con ese usuario. Elige otro.",
    "modulo" : "USUARIOS_Y_CARGOS",
    "nombre" : "USUARIO_YA_EXISTE",
    "status" : 42211
  }, {
    "cuando" : "Crear un usuario cuyo nombre de usuario, ya en minúsculas, no cumple el formato. El error va en data.errores, sobre usuario",
    "http" : 422,
    "mensaje" : "El usuario va en minúsculas, de 3 a 20 caracteres: letras sin tilde, números, punto, guion y guion bajo, y empieza con una letra o un número.",
    "modulo" : "USUARIOS_Y_CARGOS",
    "nombre" : "USUARIO_CON_FORMATO_INVALIDO",
    "status" : 42212
  }, {
    "cuando" : "Asignar un cargo que no existe o que está inactivo. El error va en data.errores, sobre cargoId",
    "http" : 422,
    "mensaje" : "Elige un cargo activo del catálogo.",
    "modulo" : "USUARIOS_Y_CARGOS",
    "nombre" : "CARGO_NO_ASIGNABLE",
    "status" : 42213
  }, {
    "cuando" : "Crear o renombrar un cargo con el nombre de otro, sin distinguir mayúsculas. El error va en data.errores, sobre nombre",
    "http" : 422,
    "mensaje" : "Ya existe un cargo con ese nombre.",
    "modulo" : "USUARIOS_Y_CARGOS",
    "nombre" : "CARGO_YA_EXISTE",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 2.8: catálogo de cargos",
      "sprint" : 2
    },
    "status" : 42214
  }, {
    "cuando" : "El archivo que se adjunta a un movimiento pasa de 5 MB",
    "http" : 400,
    "mensaje" : "Esa foto pesa demasiado. El máximo son 5 MB.",
    "modulo" : "MOVIMIENTOS_Y_CUENTAS",
    "nombre" : "ADJUNTO_DEMASIADO_GRANDE",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 3.6: adjuntar la foto del recibo",
      "sprint" : 3
    },
    "status" : 40020
  }, {
    "cuando" : "El archivo que se adjunta no es image/jpeg, image/png, image/webp ni application/pdf",
    "http" : 400,
    "mensaje" : "Solo se pueden adjuntar fotos o archivos PDF.",
    "modulo" : "MOVIMIENTOS_Y_CUENTAS",
    "nombre" : "ADJUNTO_NO_ES_FOTO_NI_PDF",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 3.6: adjuntar la foto del recibo",
      "sprint" : 3
    },
    "status" : 40021
  }, {
    "cuando" : "Se crea una categoría dentro de otra que no existe o que fue anulada. El error va en data.errores, sobre padreId",
    "http" : 422,
    "mensaje" : "La categoría madre que elegiste no existe.",
    "modulo" : "MOVIMIENTOS_Y_CUENTAS",
    "nombre" : "CATEGORIA_MADRE_NO_EXISTE",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 1.10: alta de categorías",
      "sprint" : 1
    },
    "status" : 42220
  }, {
    "cuando" : "Se crea una categoría de gasto dentro de una de ingreso, o al revés. El error va en data.errores, sobre padreId",
    "http" : 422,
    "mensaje" : "Una categoría tiene que ser de la misma naturaleza que su categoría madre.",
    "modulo" : "MOVIMIENTOS_Y_CUENTAS",
    "nombre" : "CATEGORIA_MADRE_DE_OTRA_NATURALEZA",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 1.10: alta de categorías",
      "sprint" : 1
    },
    "status" : 42221
  }, {
    "cuando" : "Se marca como fija una categoría de ingreso. El error va en data.errores, sobre esFijo",
    "http" : 422,
    "mensaje" : "Solo una categoría de gasto puede ser fija.",
    "modulo" : "MOVIMIENTOS_Y_CUENTAS",
    "nombre" : "SOLO_UN_GASTO_ES_FIJO",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 1.10: alta de categorías",
      "sprint" : 1
    },
    "status" : 42222
  }, {
    "cuando" : "La fecha del movimiento es posterior a hoy en America/Bogota. El error va en data.errores, sobre fechaMovimiento",
    "http" : 422,
    "mensaje" : "Esa fecha todavía no llega. Aquí va cuándo ocurrió de verdad, no cuándo se va a pagar.",
    "modulo" : "MOVIMIENTOS_Y_CUENTAS",
    "nombre" : "FECHA_DEL_MOVIMIENTO_INVALIDA",
    "status" : 42223
  }, {
    "cuando" : "La cuenta del movimiento no existe o está anulada. El error va en data.errores, sobre cuentaId",
    "http" : 422,
    "mensaje" : "Elige una cuenta del catálogo.",
    "modulo" : "MOVIMIENTOS_Y_CUENTAS",
    "nombre" : "CUENTA_NO_ASIGNABLE",
    "status" : 42224
  }, {
    "cuando" : "La categoría del movimiento no existe o está anulada. El error va en data.errores, sobre categoriaId",
    "http" : 422,
    "mensaje" : "Elige una categoría del catálogo.",
    "modulo" : "MOVIMIENTOS_Y_CUENTAS",
    "nombre" : "CATEGORIA_NO_ASIGNABLE",
    "status" : 42225
  }, {
    "cuando" : "Una transferencia sin cuenta de destino, una con la misma de origen, o cualquier otro tipo que la traiga. El error va en data.errores, sobre cuentaDestinoId",
    "http" : 422,
    "mensaje" : "Una transferencia va de una cuenta a otra distinta, y ningún otro movimiento tiene cuenta de destino.",
    "modulo" : "MOVIMIENTOS_Y_CUENTAS",
    "nombre" : "CUENTA_DE_DESTINO_QUE_NO_CORRESPONDE",
    "status" : 42226
  }, {
    "cuando" : "El anticipo, sumado a los ya cobrados del pedido, supera su valor total, o no es mayor que cero. El error va en data.errores, sobre valor",
    "http" : 422,
    "mensaje" : "Ese anticipo no cabe en el pedido: revisa el valor.",
    "modulo" : "PEDIDOS_Y_CLIENTES",
    "nombre" : "ANTICIPO_NO_CABE",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 4.4: cobrar anticipo",
      "sprint" : 4
    },
    "status" : 42230
  }, {
    "cuando" : "El pedido llega sin líneas, con una cantidad que no es positiva, o con un producto que no existe o está anulado. El error va en data.errores, sobre lineas",
    "http" : 422,
    "mensaje" : "Revisa las líneas del pedido.",
    "modulo" : "PEDIDOS_Y_CLIENTES",
    "nombre" : "LINEAS_DEL_PEDIDO_INVALIDAS",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 4.3: registrar pedido con líneas",
      "sprint" : 4
    },
    "status" : 42231
  }, {
    "cuando" : "El cliente del pedido no existe o está anulado. El error va en data.errores, sobre clienteId",
    "http" : 422,
    "mensaje" : "Elige un cliente del catálogo.",
    "modulo" : "PEDIDOS_Y_CLIENTES",
    "nombre" : "CLIENTE_NO_ASIGNABLE",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 4.3: registrar pedido con líneas",
      "sprint" : 4
    },
    "status" : 42232
  }, {
    "cuando" : "La cuenta del anticipo, de la entrega o de la devolución no existe o está anulada. El error va en data.errores, sobre cuentaId",
    "http" : 422,
    "mensaje" : "Elige una cuenta del catálogo.",
    "modulo" : "PEDIDOS_Y_CLIENTES",
    "nombre" : "CUENTA_DEL_PEDIDO_NO_ASIGNABLE",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 4.4: cobrar anticipo",
      "sprint" : 4
    },
    "status" : 42233
  }, {
    "cuando" : "La fecha del anticipo o de la entrega es futura, o es anterior a la fecha del pedido. El error va en data.errores, sobre fecha",
    "http" : 422,
    "mensaje" : "Revisa la fecha: no puede ser futura ni anterior a la del pedido.",
    "modulo" : "PEDIDOS_Y_CLIENTES",
    "nombre" : "FECHA_DEL_PEDIDO_INVALIDA",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 4.4: cobrar anticipo",
      "sprint" : 4
    },
    "status" : 42234
  }, {
    "cuando" : "Se cancela un pedido con anticipos por devengar sin decir qué se hace con ellos. El error va en data.errores, sobre destinoDelAnticipo",
    "http" : 422,
    "mensaje" : "Di qué pasa con el anticipo: se le devuelve al cliente o se convierte en ingreso.",
    "modulo" : "PEDIDOS_Y_CLIENTES",
    "nombre" : "FALTA_EL_DESTINO_DEL_ANTICIPO",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 4.9: cancelación con destino del anticipo",
      "sprint" : 4
    },
    "status" : 42235
  }, {
    "cuando" : "Los minutos de trabajo llegan con más de dos decimales, que es lo que guarda la columna NUMERIC(6,2). El error va en data.errores, sobre minutosTrabajo",
    "http" : 422,
    "mensaje" : "El tiempo de trabajo se registra con dos decimales como máximo.",
    "modulo" : "PRODUCTOS_Y_COSTEO",
    "nombre" : "TIEMPO_DE_TRABAJO_INVALIDO",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 5.2: catálogo de productos y servicios",
      "sprint" : 5
    },
    "status" : 42240
  }, {
    "cuando" : "La fecha de la compra de un activo, de un aporte o de un retiro es posterior a hoy en America/Bogota. El error va en data.errores, sobre fechaCompra o fecha",
    "http" : 422,
    "mensaje" : "Esa fecha todavía no llega. Aquí va cuándo ocurrió de verdad.",
    "modulo" : "CAPITAL_Y_PATRIMONIO",
    "nombre" : "FECHA_DE_CAPITAL_INVALIDA",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 7.1: registro de inversiones en activos",
      "sprint" : 7
    },
    "status" : 42290
  }, {
    "cuando" : "La cuenta de un activo, de un aporte o de un retiro no existe o está anulada. El error va en data.errores, sobre cuentaId",
    "http" : 422,
    "mensaje" : "Elige una cuenta del catálogo.",
    "modulo" : "CAPITAL_Y_PATRIMONIO",
    "nombre" : "CUENTA_DE_CAPITAL_NO_ASIGNABLE",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 7.1: registro de inversiones en activos",
      "sprint" : 7
    },
    "status" : 42291
  }, {
    "cuando" : "La parte de pro-labore de un retiro es mayor que el retiro entero. El error va en data.errores, sobre prolabore",
    "http" : 422,
    "mensaje" : "El pro-labore no puede ser mayor que el retiro.",
    "modulo" : "CAPITAL_Y_PATRIMONIO",
    "nombre" : "PROLABORE_MAYOR_QUE_EL_RETIRO",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 7.4: retiro con división automática",
      "sprint" : 7
    },
    "status" : 42292
  }, {
    "cuando" : "Los cuatro porcentajes de los sobres no suman 100: es la restricción suma_cien de sobres_config (04 §4.8). El error va en data.errores, sobre pctRetiro",
    "http" : 422,
    "mensaje" : "Los cuatro sobres tienen que sumar 100 %.",
    "modulo" : "CAPITAL_Y_PATRIMONIO",
    "nombre" : "SOBRES_QUE_NO_SUMAN_CIEN",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 7.7: los cuatro sobres con historial",
      "sprint" : 7
    },
    "status" : 42293
  }, {
    "cuando" : "Las horas productivas del pro-labore llegan con más de dos decimales, que es lo que guarda la columna NUMERIC(6,2). El error va en data.errores, sobre horasMensuales",
    "http" : 422,
    "mensaje" : "Las horas se registran con dos decimales como máximo.",
    "modulo" : "CAPITAL_Y_PATRIMONIO",
    "nombre" : "HORAS_DEL_PROLABORE_INVALIDAS",
    "pendienteDeEmitir" : {
      "quien" : "Tarea 7.3: pro-labore con justificación",
      "sprint" : 7
    },
    "status" : 42294
  }, {
    "cuando" : "El simulador o la productividad, cuando no hay pro-labore vigente. También es lo que recibe Operación, a quien prolabore_config no le devuelve ninguna fila",
    "http" : 409,
    "mensaje" : "Define primero el pro-labore de la gerencia. Sin él, el simulador daría una cifra inflada.",
    "modulo" : "NOMINA_Y_ADELANTOS",
    "nombre" : "SIN_PROLABORE_DEFINIDO",
    "pendienteDeEmitir" : true,
    "status" : 40950
  }, {
    "cuando" : "Liquidar o volver a cerrar un período que ya tiene cerrado_en",
    "http" : 409,
    "mensaje" : "Ese mes de nómina ya está cerrado.",
    "modulo" : "NOMINA_Y_ADELANTOS",
    "nombre" : "PERIODO_DE_NOMINA_CERRADO",
    "pendienteDeEmitir" : true,
    "status" : 40951
  }, {
    "cuando" : "nomina_detalle_periodo_empleado_key: la segunda liquidación de la misma pareja",
    "http" : 409,
    "mensaje" : "Esa persona ya está liquidada en ese mes.",
    "modulo" : "NOMINA_Y_ADELANTOS",
    "nombre" : "EMPLEADA_YA_LIQUIDADA",
    "pendienteDeEmitir" : true,
    "status" : 40952
  }, {
    "cuando" : "Retirar a una empleada con una fecha anterior a su fecha_ingreso",
    "http" : 422,
    "mensaje" : "La fecha de retiro no puede ser anterior a la de ingreso.",
    "modulo" : "NOMINA_Y_ADELANTOS",
    "nombre" : "FECHA_DE_RETIRO_ANTERIOR_AL_INGRESO",
    "pendienteDeEmitir" : true,
    "status" : 42250
  }, {
    "cuando" : "La llave foránea empleado_id de adelantos o de nomina_detalle: no existe, está anulada o ya se retiró",
    "http" : 422,
    "mensaje" : "Esa persona no está en nómina.",
    "modulo" : "NOMINA_Y_ADELANTOS",
    "nombre" : "EMPLEADA_NO_ASIGNABLE",
    "pendienteDeEmitir" : true,
    "status" : 42251
  }, {
    "cuando" : "nomina_periodos_anio_mes_key: ya hay un período con ese año y ese mes",
    "http" : 422,
    "mensaje" : "Ese mes de nómina ya está abierto.",
    "modulo" : "NOMINA_Y_ADELANTOS",
    "nombre" : "PERIODO_DE_NOMINA_YA_EXISTE",
    "pendienteDeEmitir" : true,
    "status" : 42252
  }, {
    "cuando" : "empleados_usuario_id_key: dos fichas no cuelgan del mismo usuario",
    "http" : 422,
    "mensaje" : "Ese usuario ya tiene ficha de nómina.",
    "modulo" : "NOMINA_Y_ADELANTOS",
    "nombre" : "EMPLEADA_YA_TIENE_USUARIO",
    "pendienteDeEmitir" : true,
    "status" : 42253
  }, {
    "cuando" : "Aceptar o anular una cotización que ya tiene pedidoId",
    "http" : 409,
    "mensaje" : "Esa cotización ya se convirtió en pedido.",
    "modulo" : "COTIZACIONES",
    "nombre" : "COTIZACION_YA_ACEPTADA",
    "pendienteDeEmitir" : true,
    "status" : 40980
  }, {
    "cuando" : "Aceptar o pedir el PDF de una cotización anulada o vencida",
    "http" : 409,
    "mensaje" : "Esa cotización ya no está vigente.",
    "modulo" : "COTIZACIONES",
    "nombre" : "COTIZACION_YA_ESTA_CERRADA",
    "pendienteDeEmitir" : true,
    "status" : 40981
  }, {
    "cuando" : "El validador de anticipo mínimo cuando una línea apunta a un producto sin fila en costos_producto. También es lo que recibe Operación, a quien esa tabla no le devuelve nada",
    "http" : 409,
    "mensaje" : "Uno de los productos no tiene costo registrado, así que no hay de dónde sacar el anticipo mínimo.",
    "modulo" : "COTIZACIONES",
    "nombre" : "PRODUCTO_SIN_COSTO",
    "pendienteDeEmitir" : true,
    "status" : 40982
  }, {
    "cuando" : "Emitir una cotización sin líneas, o con una línea de cantidad o precio que no sirve",
    "http" : 422,
    "mensaje" : "La cotización necesita al menos un producto con cantidad y precio.",
    "modulo" : "COTIZACIONES",
    "nombre" : "LINEAS_DE_LA_COTIZACION_INVALIDAS",
    "pendienteDeEmitir" : true,
    "status" : 42280
  }, {
    "cuando" : "validaHasta anterior a fechaEmision",
    "http" : 422,
    "mensaje" : "La cotización no puede vencer antes de emitirse.",
    "modulo" : "COTIZACIONES",
    "nombre" : "FECHA_DE_LA_COTIZACION_INVALIDA",
    "pendienteDeEmitir" : true,
    "status" : 42281
  }, {
    "cuando" : "El CSV que se sube a importar supera el tope",
    "http" : 400,
    "mensaje" : "El archivo pesa más de 5 MB.",
    "modulo" : "EXPORTACION_IMPORTACION_Y_RESPALDO",
    "nombre" : "ARCHIVO_DE_IMPORTACION_DEMASIADO_GRANDE",
    "pendienteDeEmitir" : true,
    "status" : 40070
  }, {
    "cuando" : "El archivo que se sube a importar no es text/csv",
    "http" : 400,
    "mensaje" : "Solo se puede importar un archivo CSV.",
    "modulo" : "EXPORTACION_IMPORTACION_Y_RESPALDO",
    "nombre" : "ARCHIVO_DE_IMPORTACION_NO_ES_CSV",
    "pendienteDeEmitir" : true,
    "status" : 40071
  }, {
    "cuando" : "Confirmar una importación con filas que coinciden con movimientos existentes, sin aceptarDuplicados",
    "http" : 409,
    "mensaje" : "El archivo trae movimientos que ya están registrados. Confirma si quieres cargarlos de todos modos.",
    "modulo" : "EXPORTACION_IMPORTACION_Y_RESPALDO",
    "nombre" : "IMPORTACION_CON_DUPLICADOS",
    "pendienteDeEmitir" : true,
    "status" : 40970
  }, {
    "cuando" : "El mapeo no cubre alguna de las columnas obligatorias",
    "http" : 422,
    "mensaje" : "Falta decir qué columna del archivo es cada dato.",
    "modulo" : "EXPORTACION_IMPORTACION_Y_RESPALDO",
    "nombre" : "MAPEO_DE_COLUMNAS_INCOMPLETO",
    "pendienteDeEmitir" : true,
    "status" : 42270
  }, {
    "cuando" : "Confirmar una importación en la que todas las filas quedaron inválidas",
    "http" : 422,
    "mensaje" : "Ninguna fila del archivo se puede importar.",
    "modulo" : "EXPORTACION_IMPORTACION_Y_RESPALDO",
    "nombre" : "CSV_SIN_FILAS_VALIDAS",
    "pendienteDeEmitir" : true,
    "status" : 42271
  }, {
    "cuando" : "Cualquier fallo no contemplado. Nunca expone la traza ni el texto de la excepción",
    "http" : 500,
    "mensaje" : "Algo salió mal. Intenta de nuevo en un momento.",
    "modulo" : "GENERICO",
    "nombre" : "ERROR_NO_PREVISTO",
    "status" : 50000
  }, {
    "cuando" : "Un servicio del que la API depende no contestó, o le falta una variable para hablar con él. No es un fallo imprevisto y reintentar no lo arregla: por eso no sale como 50000",
    "http" : 503,
    "mensaje" : "El sistema no está disponible en este momento. Avisa a quien administra PRISMA.",
    "modulo" : "GENERICO",
    "nombre" : "SERVICIO_NO_DISPONIBLE",
    "status" : 50300
  } ]
}
